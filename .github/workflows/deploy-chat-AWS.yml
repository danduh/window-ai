name: Deploy to AWS S3 and CloudFront

on: workflow_dispatch

env:
  NODE_VERSION: "20.x"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: "package-lock.json"

      - name: Install dependencies
        run: |
          npm i --legacy-peer-deps

      - name: Build Chat site
        run: |
            npx nx build chat

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Deploy to S3
        run: |
          aws s3 sync dist/chat/ s3://${{ vars.S3_BUCKET_NAME }}/windowai --delete --exact-timestamps
        env:
          AWS_S3_BUCKET: ${{ vars.S3_BUCKET_NAME }}

      - name: Invalidate CloudFront distribution
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ vars.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"
        env:
          CLOUDFRONT_DISTRIBUTION_ID: ${{ vars.CLOUDFRONT_DISTRIBUTION_ID }}

      - name: Display deployment info
        run: |
          echo "🚀 Deployment completed!"
          echo "📦 S3 Bucket: ${{ vars.S3_BUCKET_NAME }}"
          echo "🌐 CloudFront Distribution: ${{ vars.CLOUDFRONT_DISTRIBUTION_ID }}"
