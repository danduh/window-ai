---
phase: 10-multimodal-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - chat/src/app/services/MultimodalService.ts
  - chat/src/app/components/Multimodal/MultimodalPage.tsx
  - chat/src/app/components/Multimodal/MultimodalHeader.tsx
  - chat/src/app/AppRouter.tsx
  - chat/src/app/hooks/useSEOData.ts
  - chat/scripts/prerender-react.js
autonomous: true
requirements: [MULTI-01, MULTI-06]
must_haves:
  truths:
    - "Navigating to /multimodal from main nav loads a Multimodal page styled with Tailwind theme + dark-mode"
    - "Nav link 'Multimodal' appears after 'Proofreader' in both desktop and mobile nav"
    - "When LanguageModel.availability({expectedInputs:[{type:'image'}]}) returns 'unavailable' or throws, MissingFlagBanner renders above the header with the two flag URLs"
    - "When availability returns 'available', the page enters 'ready' state and the Chat tab content area is visible"
    - "Tabs ordering shows Docs first (placeholder copy), Chat second (panel placeholder)"
    - "MultimodalService.promptWithImage / getAvailability / createWithProgress / destroyAllSessions are exported and callable from the page"
    - "useSEOData and prerender-react.js both contain seoConfigs.multimodal and seoConfigs.multimodalDocs with identical title and description strings"
  artifacts:
    - path: chat/src/app/services/MultimodalService.ts
      provides: "Module-scope pooled LanguageModel session + promptWithImage streaming wrapper"
      exports: ["promptWithImage", "getAvailability", "createWithProgress", "destroyAllSessions", "AvailabilityState", "MultimodalContentPart"]
    - path: chat/src/app/components/Multimodal/MultimodalPage.tsx
      provides: "Top-level page shell with PageState machine, mount-effect availability check, Tabs, banner conditional, object URL ref"
      exports: ["MultimodalPage", "Message", "PageState"]
    - path: chat/src/app/components/Multimodal/MultimodalHeader.tsx
      provides: "Static header with camera icon + title + tagline"
      exports: ["MultimodalHeader"]
    - path: chat/src/app/AppRouter.tsx
      provides: "MultimodalPage import + desktop nav link + mobile nav link + 2 Route entries"
      contains: "<Route path=\"/multimodal\""
    - path: chat/src/app/hooks/useSEOData.ts
      provides: "seoConfigs.multimodal + seoConfigs.multimodalDocs entries"
      contains: "multimodal:"
    - path: chat/scripts/prerender-react.js
      provides: "routes array entries + seoConfigs entries for /multimodal and /multimodal/docs"
      contains: "'/multimodal':"
  key_links:
    - from: "chat/src/app/AppRouter.tsx"
      to: "chat/src/app/components/Multimodal/MultimodalPage.tsx"
      via: "import { MultimodalPage } from './components/Multimodal/MultimodalPage'"
      pattern: "MultimodalPage"
    - from: "chat/src/app/components/Multimodal/MultimodalPage.tsx"
      to: "chat/src/app/services/MultimodalService.ts"
      via: "import { getAvailability, createWithProgress, destroyAllSessions } from '../../services/MultimodalService'"
      pattern: "MultimodalService"
    - from: "chat/src/app/components/Multimodal/MultimodalPage.tsx"
      to: "chat/src/app/hooks/useSEOData.ts"
      via: "useSEOData(seoConfigs.multimodal | seoConfigs.multimodalDocs, ...)"
      pattern: "seoConfigs\\.multimodal"
    - from: "chat/src/app/hooks/useSEOData.ts seoConfigs.multimodal"
      to: "chat/scripts/prerender-react.js seoConfigs['/multimodal']"
      via: "byte-identical title + description strings"
      pattern: "Multimodal — Ask Gemini Nano about images on-device"
---

<objective>
Ship the Multimodal demo foundation: routing, page shell, service wrapper, SEO entries, and the MissingFlagBanner gating path — without any chat/drop/paste/transcript yet. Plan 02 layers those onto the shell.

Purpose: De-risk the routing + service + availability boilerplate first so Plan 02 is purely about the chat-panel UX. Mirrors Phase 8 Proofreader plan structure (which split shell from output modes).

Output:
- `MultimodalService.ts` with the four-function public API
- `MultimodalPage.tsx` shell with PageState machine + banner + Tabs (Docs first, Chat placeholder second)
- `MultimodalHeader.tsx` static header
- `AppRouter.tsx` wiring (import + 2 nav links + 2 routes)
- `useSEOData.ts` + `prerender-react.js` SEO entries (byte-identical title/description per CONTEXT.md)

After this plan: visiting `/multimodal` on Chrome 148+ shows the page with the Chat tab empty (placeholder text "Chat panel coming in plan 02"); on a browser without image-input availability, the MissingFlagBanner is visible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-multimodal-foundation/10-CONTEXT.md
@.planning/phases/10-multimodal-foundation/10-RESEARCH.md
@.planning/phases/10-multimodal-foundation/10-PATTERNS.md
@.planning/phases/10-multimodal-foundation/10-UI-SPEC.md

# Closest analogs (read these before writing any new file)
@chat/src/app/services/ProofreaderService.ts
@chat/src/app/components/Proofreader/ProofreaderPage.tsx
@chat/src/app/components/Proofreader/ProofreaderHeader.tsx
@chat/src/app/components/MissingFlagBanner.tsx
@chat/src/app/components/Tabs.tsx
@chat/src/app/hooks/useSEOData.ts
@chat/src/app/AppRouter.tsx
@chat/scripts/prerender-react.js
@chat/src/app/types/dom-chromium-ai.d.ts

<interfaces>
Key types and contracts the executor needs. Extracted from the codebase.

From chat/src/app/types/dom-chromium-ai.d.ts:
- `interface LanguageModel` declares `prompt(input: string, ...)` and `promptStreaming(input: string, ...)` — string-only overloads
- Static `LanguageModel.availability(options?)` returns 'available' | 'downloadable' | 'downloading' | 'unavailable'
- `LanguageModel.create(options)` accepts `expectedInputs: Array<{type: 'text'|'image'|'audio'|...}>`, `expectedOutputs`, `outputLanguage`, `monitor`
- `AICreateMonitor` has `addEventListener('downloadprogress', (e: ProgressEvent) => ...)`
- DO NOT modify dom-chromium-ai.d.ts in Phase 10. The multimodal `promptStreaming(Array)` overload lives only inside MultimodalService.ts as a local interface.

From chat/src/app/services/ProofreaderService.ts (the template):
- Module-scope `sessionPromise: Promise<Proofreader> | null` cache pattern
- `getOrCreateSession()` stores promise BEFORE await; `.catch` sets back to null
- `getAvailability()` returns the availability state union
- `createWithProgress(onProgress)` wires the `monitor` callback to a `downloadprogress` listener
- `destroyAllSessions()` calls `.then(s => s.destroy()).catch(() => {})` and nulls the cache

From chat/src/app/components/MissingFlagBanner.tsx:
- Props: `title: string`, `body: string`, `flags: Array<{name: string; url: string; note?: string}>`, `browserRequirement: string`
- Component is reused unchanged in Phase 10 — no edits

From chat/src/app/components/Tabs.tsx:
- Matches via `currentPath.includes(tab.path)` — empty-path tab matches everything, so Docs (path '/docs') MUST come first; Chat (path '') second
- Props: `basePath: string`, `defaultTab: string`, `tabs: Array<{id, label, path, content}>`

From chat/src/app/hooks/useSEOData.ts (existing seoConfigs shape):
```
export const seoConfigs = {
  proofreader: { title, description, keywords },
  proofreaderDocs: { title, description, keywords },
} as const;
```
Insert `multimodal` and `multimodalDocs` entries after `proofreaderDocs` (current closing brace is at the last line).

From chat/src/app/AppRouter.tsx:
- Existing Proofreader import at line 12: `import {ProofreaderPage} from './components/Proofreader/ProofreaderPage';`
- Desktop Proofreader nav `<Link>` ends at line 80
- Mobile Proofreader nav `<Link>` ends at line 176
- Proofreader routes are at lines 253-255 (`/* Proofreader routes */` comment + 2 Route lines)

From chat/scripts/prerender-react.js:
- Routes array entry for `/proofreader/docs` is at line 66
- seoConfigs `/proofreader/docs` block closing `},` is at line ~437 (followed by `};` then `return seoConfigs[routePath] || seoConfigs['/']`)
</interfaces>

<patterns>
# 10-PATTERNS.md is the load-bearing reference. For every file below, the analog
# file + line numbers are documented there. Executor MUST read 10-PATTERNS.md
# alongside the analog source files before writing any new file.
</patterns>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: MultimodalService.ts (service + types + session pool)</name>
  <files>chat/src/app/services/MultimodalService.ts</files>
  <read_first>
    - chat/src/app/services/ProofreaderService.ts (the direct template — read in full)
    - chat/src/app/services/ChatAIService.ts (module-scope session caching with `any` cast workaround for absent ambient overload — lines 14-31)
    - chat/src/app/services/WriterService.ts (ReadableStream<string> streaming return shape)
    - chat/src/app/types/dom-chromium-ai.d.ts (confirm string-only promptStreaming overload at line 63 — DO NOT MODIFY this file)
    - .planning/phases/10-multimodal-foundation/10-PATTERNS.md (§MultimodalService.ts — function-by-function pattern with line refs into ProofreaderService.ts)
    - .planning/phases/10-multimodal-foundation/10-RESEARCH.md (Pattern 1, Pattern 2, Pattern 3, Pitfall 3, plus Code Examples section)
    - .planning/phases/10-multimodal-foundation/10-CONTEXT.md (§MultimodalService API Surface — locked function signatures)
  </read_first>
  <action>
    Create `chat/src/app/services/MultimodalService.ts` mirroring ProofreaderService.ts structure, simplified to a SINGLE session (no Map, no language key).

    Exports (locked per CONTEXT.md):
    - `type AvailabilityState = 'available' | 'downloadable' | 'downloading' | 'unavailable'`
    - `interface MultimodalContentPart { type: 'text' | 'image'; value: string | Blob | ImageBitmap }` (local helper; do NOT add to dom-chromium-ai.d.ts)
    - `async function promptWithImage(text: string, image: Blob | ImageBitmap, opts?: { signal?: AbortSignal }): Promise<ReadableStream<string>>`
    - `async function getAvailability(): Promise<AvailabilityState>`
    - `async function createWithProgress(onProgress: (pct: number) => void): Promise<LanguageModel>`
    - `function destroyAllSessions(): void`

    Implementation (mirror 10-PATTERNS.md §MultimodalService.ts code blocks):
    1. Module-scope `let sessionPromise: Promise<LanguageModel> | null = null;`
    2. Private `getOrCreateSession()` calls `LanguageModel.create({ expectedInputs: [{type:'image'},{type:'text'}], expectedOutputs: [{type:'text', languages:['en']}], outputLanguage:'en' })`; stores promise BEFORE await; `.catch` nulls the cache and rethrows
    3. Local interface `MultimodalLanguageModel extends LanguageModel` with the array-input `promptStreaming` overload (workaround for dom-chromium-ai.d.ts line 63 string-only declaration — same pattern ChatAIService.ts uses for `params()`)
    4. `promptWithImage` awaits session, casts via `as unknown as MultimodalLanguageModel`, calls `session.promptStreaming([{ role: 'user', content: [{ type: 'text', value: text }, { type: 'image', value: image }] }], { signal: opts?.signal })`
    5. `getAvailability` — return `'unavailable'` if `typeof LanguageModel === 'undefined'`; otherwise `try { return await LanguageModel.availability({ expectedInputs: [{ type: 'image' }] }) as AvailabilityState } catch { return 'unavailable' }` (Pitfall 3: older Canary throws on the options argument)
    6. `createWithProgress` — invokes `LanguageModel.create({ ...same options..., monitor(m) { m.addEventListener('downloadprogress', (e: ProgressEvent) => { onProgress(e.loaded != null ? e.loaded * 100 : 0); }); } })`; assigns the result to `sessionPromise` and returns the promise
    7. `destroyAllSessions` — if `sessionPromise`, call `sessionPromise.then(s => s.destroy()).catch(() => {})` and set `sessionPromise = null`

    Type strictness: no `any` at exports. The single cast is `as unknown as MultimodalLanguageModel` at the promptStreaming call site only — same workaround pattern as ChatAIService.ts.

    Do NOT add ImageBitmap to the prompt content array as a separate code path — pass it through as-is in the `value` field; the runtime accepts both Blob and ImageBitmap.
  </action>
  <verify>
    <automated>cd /Users/danielos/dev/window-ai && npx nx run chat:typecheck 2>&1 | grep -E "MultimodalService|error TS" | head -20 || echo "no type errors"</automated>
    <automated>grep -c "^export " chat/src/app/services/MultimodalService.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `chat/src/app/services/MultimodalService.ts`
    - `grep -E "^export (async function|function|const|type) (promptWithImage|getAvailability|createWithProgress|destroyAllSessions|AvailabilityState)" chat/src/app/services/MultimodalService.ts` returns ≥ 5 lines (all four functions + the AvailabilityState type)
    - `grep -c "LanguageModel.create" chat/src/app/services/MultimodalService.ts` returns 2 (one in getOrCreateSession, one in createWithProgress)
    - `grep -c "expectedInputs: \[{ type: 'image'" chat/src/app/services/MultimodalService.ts` returns ≥ 3 (two creates + one availability call)
    - `grep -c "try {" chat/src/app/services/MultimodalService.ts` returns ≥ 1 (the availability fallback per Pitfall 3)
    - `grep -c ": any" chat/src/app/services/MultimodalService.ts` returns 0 (no `: any` annotations; the only cast is `as unknown as MultimodalLanguageModel`)
    - `npx nx run chat:lint --files=chat/src/app/services/MultimodalService.ts` passes (no new lint errors)
    - `npx nx run chat:typecheck` (or equivalent `tsc --noEmit` on chat) reports no errors involving this file
  </acceptance_criteria>
  <done>
    MultimodalService.ts compiles clean and exports the four-function public API per CONTEXT.md. Plan 02 will import these for actual prompting.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MultimodalHeader.tsx + MultimodalPage.tsx shell (PageState + Tabs + banner + mount effect)</name>
  <files>chat/src/app/components/Multimodal/MultimodalHeader.tsx, chat/src/app/components/Multimodal/MultimodalPage.tsx</files>
  <read_first>
    - chat/src/app/components/Proofreader/ProofreaderHeader.tsx (direct visual template for MultimodalHeader — full read)
    - chat/src/app/components/Proofreader/ProofreaderPage.tsx (PageState machine, mount-effect cancelled-flag pattern, Tabs ordering with comment, JSX shell — full read; lines 20, 80-120, 197-218, 220-249 are the critical sections per 10-PATTERNS.md)
    - chat/src/app/components/MissingFlagBanner.tsx (confirm props shape — do NOT modify)
    - chat/src/app/components/Tabs.tsx (confirm includes-match behavior that forces Docs-first ordering)
    - chat/src/app/services/MultimodalService.ts (the file written in Task 1 — import getAvailability, createWithProgress, destroyAllSessions)
    - .planning/phases/10-multimodal-foundation/10-PATTERNS.md (§MultimodalPage.tsx + §MultimodalHeader.tsx — copy class strings verbatim)
    - .planning/phases/10-multimodal-foundation/10-UI-SPEC.md (§MultimodalHeader, §MultimodalPage page shell, §MissingFlagBanner Configuration — locked copy)
    - .planning/phases/10-multimodal-foundation/10-CONTEXT.md (§Availability Detection + MissingFlagBanner, §Tabs, §SEO + Page Metadata — locked title/description/banner props)
  </read_first>
  <action>
    Create both files.

    **File A: `chat/src/app/components/Multimodal/MultimodalHeader.tsx`**

    Mirror ProofreaderHeader.tsx EXACTLY in structure (10-PATTERNS.md §MultimodalHeader.tsx has the full target):
    - `import React from 'react'`
    - `export const MultimodalHeader: React.FC = () => ( ... )` arrow function returning `<header className="mb-8">`
    - Outer flex row: `<div className="flex items-center space-x-4">`
    - Icon badge: `<div className="flex-shrink-0 w-12 h-12 rounded-xl bg-gradient-to-br from-primary-500 to-purple-600 flex items-center justify-center text-white shadow-sm">` containing an INLINE camera SVG `w-6 h-6` `aria-hidden="true"` (use the standard Heroicons-style camera outline: 24x24 viewBox, two paths — body + lens — match the SVG style used elsewhere in the codebase by reading ProofreaderHeader.tsx for the SVG conventions)
    - Title block:
      - `<h1 className="text-3xl font-bold text-gray-900 dark:text-white leading-tight">Multimodal</h1>`
      - `<p className="mt-2 text-base font-medium text-gray-600 dark:text-gray-400">On-device image understanding with Gemini Nano — drag, paste, or capture an image and ask about it. Chrome 148+ stable, zero network.</p>`

    **File B: `chat/src/app/components/Multimodal/MultimodalPage.tsx`**

    Implements the SHELL only — no chat panel logic, no drag/paste, no transcript (those come in Plan 02). The Chat tab content is a PLACEHOLDER until Plan 02.

    Top-of-file exports + types (per 10-PATTERNS.md §MultimodalPage.tsx):
    ```
    export type PageState = 'idle' | 'unavailable' | 'downloading' | 'ready' | 'prompting' | 'error';
    export type Message = {
      id: string;                  // crypto.randomUUID()
      role: 'user' | 'assistant';
      text: string;
      attachedImageUrl?: string;
      error?: string;              // assistant-only; set when promptWithImage throws (Plan 02 wires this)
    };
    ```

    Imports (per 10-PATTERNS.md):
    ```
    import React, { useEffect, useMemo, useRef, useState } from 'react';
    import { useLocation } from 'react-router-dom';
    import { useSEOData, seoConfigs } from '../../hooks/useSEOData';
    import { MissingFlagBanner } from '../MissingFlagBanner';
    import Tabs from '../Tabs';
    import { MultimodalHeader } from './MultimodalHeader';
    import { getAvailability, createWithProgress, destroyAllSessions } from '../../services/MultimodalService';
    ```

    Inside `export const MultimodalPage: React.FC = () => { ... }`:
    1. State: `pageState` (default `'idle'`), `downloadPct` (default 0), `error` (default `null`), `messages` (default `[]` — used by Plan 02; declare it here so the shell type is final and Plan 02 doesn't add new exports)
    2. `objectUrlSetRef = useRef<Set<string>>(new Set())` — used by Plan 02; declare it now so cleanup is wired
    3. `location = useLocation()` then `isDocs = location.pathname.startsWith('/multimodal/docs')` (startsWith, NOT includes — Pitfall 6)
    4. Call `useSEOData(isDocs ? seoConfigs.multimodalDocs : seoConfigs.multimodal, isDocs ? '/multimodal/docs' : '/multimodal')`
    5. Mount effect — copy ProofreaderPage.tsx lines 80-120 verbatim (10-PATTERNS.md has the full block), swap Proofreader service imports for Multimodal. Cleanup MUST call `destroyAllSessions()` AND `objectUrlSetRef.current.forEach(url => URL.revokeObjectURL(url))`.
    6. `tabs = useMemo(...)` with Docs FIRST then Chat. Include the verbatim comment `// Tabs ordering: Docs FIRST, Chat SECOND. Tabs.tsx matches currentPath.includes(tab.path) — the chat tab has path '' which matches everything, so the docs tab (path '/docs') MUST come first.`
       - Docs tab content (placeholder for Phase 12): `<div className="max-w-none p-4 text-sm font-medium text-gray-500 dark:text-gray-400">Documentation coming in Phase 12.</div>`
       - Chat tab content (placeholder for Plan 02): `<div className="max-w-none p-4 text-sm font-medium text-gray-500 dark:text-gray-400">Chat panel coming in plan 10-02.</div>`
    7. JSX return — copy ProofreaderPage.tsx lines 220-249 shell verbatim with these substitutions (10-UI-SPEC.md §MultimodalPage page shell + §MissingFlagBanner Configuration):
       - Outer: `<div className="min-h-screen bg-white dark:bg-gray-800 transition-colors duration-200">` → `<div className="max-w-6xl mx-auto p-4">`
       - Banner block — when `pageState === 'unavailable'`:
         ```
         <MissingFlagBanner
           title="Multimodal image input isn't available."
           body="Update to Chrome 148+ stable, or enable the flags below in Chrome 146+ Canary, then reload."
           flags={[
             { name: 'Optimization Guide On Device', url: 'chrome://flags/#optimization-guide-on-device-model', note: 'set to "Enabled BypassPerfRequirement"' },
             { name: 'Prompt API multimodal input', url: 'chrome://flags/#prompt-api-for-gemini-nano-multimodal-input', note: 'set to "Enabled"' },
           ]}
           browserRequirement="Chrome 148+ stable (no flags) or Chrome 146+ Canary"
         />
         ```
       - `<MultimodalHeader />`
       - `<Tabs basePath="/multimodal" defaultTab="docs" tabs={tabs} />`
       - Footer: `<p className="mt-4 text-center text-xs font-medium text-gray-500 dark:text-gray-400">🔒 Zero network during demo — open DevTools → Network tab</p>`

    Notes for the executor:
    - StrictMode pitfall (Pitfall 5): the cleanup function MUST be idempotent — `destroyAllSessions()` already handles double-call; the URL-set forEach is also idempotent (revoking a stale URL is a no-op).
    - The `messages` state and `objectUrlSetRef` are declared here but Plan 02 owns the read/write paths (passed down to MultimodalChatPanel as props). Declaring them here finalizes the export surface so Plan 02 is purely additive to MultimodalChatPanel.tsx/MultimodalTranscript.tsx/MultimodalInput.tsx.
  </action>
  <verify>
    <automated>cd /Users/danielos/dev/window-ai && npx nx run chat:typecheck 2>&1 | grep -E "MultimodalPage|MultimodalHeader|error TS" | head -20 || echo "no type errors"</automated>
    <automated>grep -c "Tabs ordering: Docs FIRST" chat/src/app/components/Multimodal/MultimodalPage.tsx</automated>
    <automated>grep -c "startsWith('/multimodal/docs')" chat/src/app/components/Multimodal/MultimodalPage.tsx</automated>
    <automated>grep -c "destroyAllSessions()" chat/src/app/components/Multimodal/MultimodalPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at the paths above
    - `grep -E "^export (const|type) (MultimodalPage|Message|PageState)" chat/src/app/components/Multimodal/MultimodalPage.tsx` returns 3 lines (component + Message + PageState)
    - `grep -c "import.*MissingFlagBanner" chat/src/app/components/Multimodal/MultimodalPage.tsx` returns 1
    - `grep -c "useSEOData" chat/src/app/components/Multimodal/MultimodalPage.tsx` returns 1
    - `grep -c "chrome://flags/#optimization-guide-on-device-model" chat/src/app/components/Multimodal/MultimodalPage.tsx` returns 1
    - `grep -c "chrome://flags/#prompt-api-for-gemini-nano-multimodal-input" chat/src/app/components/Multimodal/MultimodalPage.tsx` returns 1
    - `grep -c "startsWith('/multimodal/docs')" chat/src/app/components/Multimodal/MultimodalPage.tsx` returns 1 (NOT includes — Pitfall 6)
    - `grep -c "id: 'docs'" chat/src/app/components/Multimodal/MultimodalPage.tsx` returns 1 AND the docs entry appears at a lower line number than the `id: 'chat'` entry (Docs-first ordering — verify with: `grep -n "id: 'docs'\\|id: 'chat'" chat/src/app/components/Multimodal/MultimodalPage.tsx` and confirm `docs` line < `chat` line)
    - `grep -c "destroyAllSessions()" chat/src/app/components/Multimodal/MultimodalPage.tsx` returns 1 (in cleanup)
    - `grep -c "URL.revokeObjectURL" chat/src/app/components/Multimodal/MultimodalPage.tsx` returns 1 (in cleanup)
    - `grep -c "objectUrlSetRef" chat/src/app/components/Multimodal/MultimodalPage.tsx` returns ≥ 2 (declaration + cleanup)
    - `grep -c "max-w-6xl mx-auto p-4" chat/src/app/components/Multimodal/MultimodalPage.tsx` returns 1
    - `grep -c "Multimodal" chat/src/app/components/Multimodal/MultimodalHeader.tsx` returns ≥ 1 (the h1)
    - `grep -c "from-primary-500 to-purple-600" chat/src/app/components/Multimodal/MultimodalHeader.tsx` returns 1
    - `grep -v '^[[:space:]]*\\(//\\|\\*\\)' chat/src/app/components/Multimodal/MultimodalPage.tsx | grep -c ": any"` returns 0 (no `: any` in non-comment lines)
    - `npx nx run chat:typecheck` reports no errors involving either file
  </acceptance_criteria>
  <done>
    MultimodalPage renders the shell. With `pageState === 'unavailable'`, MissingFlagBanner appears above the header. Tabs render with Docs first (placeholder content) and Chat second (placeholder). All Plan 02 hooks (`messages`, `objectUrlSetRef`) are declared so the next plan is purely additive.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Routing + Nav links + SEO dual-write</name>
  <files>chat/src/app/AppRouter.tsx, chat/src/app/hooks/useSEOData.ts, chat/scripts/prerender-react.js</files>
  <read_first>
    - chat/src/app/AppRouter.tsx (read lines 1-30 imports, 70-90 desktop nav, 170-180 mobile nav, 240-260 routes block)
    - chat/src/app/hooks/useSEOData.ts (read full — small file; existing proofreader/proofreaderDocs entries at lines 94-105 are the direct template)
    - chat/scripts/prerender-react.js (read lines 1-70 for routes array; lines 410-440 for the proofreader/proofreaderDocs seoConfigs entries and the closing `}` of seoConfigs)
    - .planning/phases/10-multimodal-foundation/10-PATTERNS.md (§AppRouter.tsx + §useSEOData.ts + §prerender-react.js — exact insertion strings with line refs)
    - .planning/phases/10-multimodal-foundation/10-CONTEXT.md (§Routing + Nav Link, §SEO + Page Metadata — locked title/description strings)
  </read_first>
  <action>
    Three coordinated edits across the three files. Use Edit (not Write); each file already exists.

    **Edit A: `chat/src/app/AppRouter.tsx`**

    1. Import insertion — after the existing line 12 `import {ProofreaderPage} from './components/Proofreader/ProofreaderPage';`, add:
       ```
       import {MultimodalPage} from './components/Multimodal/MultimodalPage';
       ```

    2. Desktop nav — directly after the Proofreader `<Link>` closing at line 80 (`...font-medium transition-colors duration-200">Proofreader</Link>`), insert:
       ```
       <Link to="/multimodal"
             onClick={() => trackUserInteraction('navigation_click', 'multimodal_link')}
             className="text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">Multimodal</Link>
       ```

    3. Mobile nav — directly after the Proofreader mobile `<Link>` closing at line 176, insert:
       ```
       <Link to="/multimodal"
             onClick={() => trackUserInteraction('navigation_click', 'multimodal_link')}
             className="text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white block px-3 py-2 rounded-md text-base font-medium transition-colors duration-200">Multimodal</Link>
       ```

    4. Routes block — directly after the Proofreader routes at line 255 (`<Route path="/proofreader/docs" .../>`), insert:
       ```
       {/* Multimodal routes */}
       <Route path="/multimodal" element={<MultimodalPage/>}/>
       <Route path="/multimodal/docs" element={<MultimodalPage/>}/>
       ```

    **Edit B: `chat/src/app/hooks/useSEOData.ts`**

    After the existing `proofreaderDocs` entry (last entry before the closing `} as const;`), add two entries (mirror the proofreader block style, including the leading comment):

    ```
    // Must match prerender-react.js seoConfigs['/multimodal'] verbatim — Phase 12 grep -F audits.
    multimodal: {
      title: 'Multimodal — Ask Gemini Nano about images on-device | Chrome AI APIs',
      description: 'Drag, paste, or capture an image — Gemini Nano answers your questions about it on-device with zero network. Chrome 148+ stable or flag-gated Canary.',
      keywords: 'Multimodal AI, Gemini Nano, image input, LanguageModel, expectedInputs, on-device AI, Chrome 148, drag and drop image, clipboard paste, WebGPU'
    },
    // Must match prerender-react.js seoConfigs['/multimodal/docs'] verbatim — Phase 12 grep -F audits.
    multimodalDocs: {
      title: 'Multimodal API Docs — expectedInputs, image types, webcam-live pattern | Chrome AI APIs',
      description: 'Drag, paste, or capture an image — Gemini Nano answers your questions about it on-device with zero network. Chrome 148+ stable or flag-gated Canary.',
      keywords: 'Multimodal API docs, expectedInputs, image types, webcam-live, LanguageModel, promptStreaming, content parts, ImageBitmap, Chrome 148'
    }
    ```

    Add a trailing comma after the existing `proofreaderDocs` block's closing `}` so the new entries parse — verify by reading the file first; the existing `proofreaderDocs` entry currently closes with `}` (no comma) because it is the last entry. After insertion, `proofreaderDocs: { ... },` (now followed by multimodal/multimodalDocs) and `multimodalDocs: { ... }` is the new last entry (no trailing comma — matches existing style).

    **Edit C: `chat/scripts/prerender-react.js`**

    1. Routes array — after line 66 (`{ path: '/proofreader/docs', filename: 'proofreader-docs.html' },`), insert:
       ```
       // Multimodal routes
       { path: '/multimodal', filename: 'multimodal.html' },
       { path: '/multimodal/docs', filename: 'multimodal-docs.html' },
       ```

    2. seoConfigs object — after the `/proofreader/docs` closing `},` (line ~437, the entry whose `structuredData.name` is `'Proofreader API Documentation'`), insert before the seoConfigs object's closing `};`:
       ```
       '/multimodal': {
         title: 'Multimodal — Ask Gemini Nano about images on-device | Chrome AI APIs',
         description: 'Drag, paste, or capture an image — Gemini Nano answers your questions about it on-device with zero network. Chrome 148+ stable or flag-gated Canary.',
         keywords: 'Multimodal AI, Gemini Nano, image input, LanguageModel, expectedInputs, on-device AI, Chrome 148, drag and drop image, clipboard paste, WebGPU',
         structuredData: {
           '@context': 'https://schema.org',
           '@type': 'WebPage',
           name: 'Multimodal Demo',
           description: 'On-device image understanding with Gemini Nano',
         },
       },
       '/multimodal/docs': {
         title: 'Multimodal API Docs — expectedInputs, image types, webcam-live pattern | Chrome AI APIs',
         description: 'Drag, paste, or capture an image — Gemini Nano answers your questions about it on-device with zero network. Chrome 148+ stable or flag-gated Canary.',
         keywords: 'Multimodal API docs, expectedInputs, image types, webcam-live, LanguageModel, promptStreaming, content parts, ImageBitmap, Chrome 148',
         structuredData: {
           '@context': 'https://schema.org',
           '@type': 'TechArticle',
           name: 'Multimodal API Documentation',
           description: 'Technical documentation for the LanguageModel multimodal API surface, expectedInputs, and image types',
         },
       },
       ```

    Phase 12 will run `grep -F "<title-string>" useSEOData.ts prerender-react.js` and expect 1 match in each file. Title and description strings MUST be byte-identical between the two files — copy-paste, do not retype.
  </action>
  <verify>
    <automated>cd /Users/danielos/dev/window-ai && npx nx run chat:typecheck 2>&1 | grep -E "AppRouter|useSEOData|error TS" | head -20 || echo "no type errors"</automated>
    <automated>grep -c "MultimodalPage" chat/src/app/AppRouter.tsx</automated>
    <automated>grep -c "multimodal_link" chat/src/app/AppRouter.tsx</automated>
    <automated>grep -c "Route path=\"/multimodal" chat/src/app/AppRouter.tsx</automated>
    <automated>grep -c "multimodal:" chat/src/app/hooks/useSEOData.ts</automated>
    <automated>grep -c "multimodalDocs:" chat/src/app/hooks/useSEOData.ts</automated>
    <automated>grep -c "/multimodal'" chat/scripts/prerender-react.js</automated>
    <automated>grep -c "/multimodal/docs'" chat/scripts/prerender-react.js</automated>
    <automated>grep -Fc "Multimodal — Ask Gemini Nano about images on-device" chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js | sort</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MultimodalPage" chat/src/app/AppRouter.tsx` returns ≥ 3 (1 import + 2 routes)
    - `grep -c "multimodal_link" chat/src/app/AppRouter.tsx` returns 2 (desktop + mobile nav GA events)
    - `grep -cE "Route path=\"/multimodal(/docs)?\"" chat/src/app/AppRouter.tsx` returns 2
    - `grep -nE "^\\s*<Link to=\"/(proofreader|multimodal)\"" chat/src/app/AppRouter.tsx` shows the Multimodal `<Link>` line numbers AFTER the Proofreader `<Link>` line numbers in both desktop and mobile nav sections (Multimodal placed AFTER Proofreader per CONTEXT.md)
    - `grep -c "multimodal:" chat/src/app/hooks/useSEOData.ts` returns 1
    - `grep -c "multimodalDocs:" chat/src/app/hooks/useSEOData.ts` returns 1
    - `grep -c "'/multimodal':" chat/scripts/prerender-react.js` returns 1 (seoConfigs entry)
    - `grep -c "'/multimodal/docs':" chat/scripts/prerender-react.js` returns 1 (seoConfigs entry)
    - `grep -c "path: '/multimodal'" chat/scripts/prerender-react.js` returns 1 (routes array entry)
    - `grep -c "path: '/multimodal/docs'" chat/scripts/prerender-react.js` returns 1 (routes array entry)
    - Byte-identical SEO check: `diff <(grep -F "Multimodal — Ask Gemini Nano about images on-device" chat/src/app/hooks/useSEOData.ts | sed 's/^ *//' | sed "s/[\"']//g") <(grep -F "Multimodal — Ask Gemini Nano about images on-device" chat/scripts/prerender-react.js | sed 's/^ *//' | sed "s/[\"']//g")` shows the same string content (whitespace + quote style may differ, but the title string is identical character-for-character inside quotes)
    - `grep -Fc "Multimodal API Docs — expectedInputs, image types, webcam-live pattern | Chrome AI APIs" chat/src/app/hooks/useSEOData.ts` returns 1
    - `grep -Fc "Multimodal API Docs — expectedInputs, image types, webcam-live pattern | Chrome AI APIs" chat/scripts/prerender-react.js` returns 1
    - `npx nx run chat:typecheck` passes for all three files
    - `node -c chat/scripts/prerender-react.js` (or equivalent JS syntax check) does not error
  </acceptance_criteria>
  <done>
    Navigating to `/multimodal` from main nav loads the Multimodal page shell. The mobile and desktop nav links both work. The SEO entries are present in both files and Phase 12's `grep -F` audit will find byte-identical title/description strings. The page enters `'unavailable'` state on a browser without image-input availability and shows the MissingFlagBanner.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, the executor must perform a manual smoke test:

1. `npx nx serve chat` — server starts cleanly with no compile errors
2. Browser → http://localhost:4300/ — main page loads, "Multimodal" link visible in desktop nav after "Proofreader"
3. Click "Multimodal" → URL becomes `/multimodal`, MultimodalHeader renders ("Multimodal" title + tagline), Tabs render (Docs first, Chat second), zero-network footer kicker visible
4. Default tab is Docs with placeholder text "Documentation coming in Phase 12."
5. Click Chat tab → placeholder "Chat panel coming in plan 10-02."
6. Navigate to `/multimodal/docs` directly → Docs tab is selected (NOT Chat — confirms startsWith match works)
7. Open the page on a browser without multimodal capability (or temporarily stub `LanguageModel.availability` to return `'unavailable'` in DevTools) → MissingFlagBanner appears above the header with both `chrome://flags/...` URLs as `<code>` elements; Docs tab remains usable
8. Mobile viewport check (DevTools 375px) → hamburger menu opens, "Multimodal" link appears after "Proofreader"
9. DevTools console — no errors during mount, no errors on Tab switching, no errors on cleanup when navigating away

Automated cross-checks (run from repo root):

```bash
# 1. SEO strings byte-identical
grep -Fc "Multimodal — Ask Gemini Nano about images on-device | Chrome AI APIs" \
  chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js
# Expected: both files report 1

# 2. Service exports compile
grep -E "^export (async function|function|const|type)" chat/src/app/services/MultimodalService.ts
# Expected: 5 lines (promptWithImage, getAvailability, createWithProgress, destroyAllSessions, AvailabilityState)

# 3. Tabs ordering safety
grep -n "id: 'docs'\\|id: 'chat'" chat/src/app/components/Multimodal/MultimodalPage.tsx
# Expected: docs line < chat line

# 4. Routing
grep -cE "Route path=\"/multimodal(/docs)?\"" chat/src/app/AppRouter.tsx
# Expected: 2
```
</verification>

<success_criteria>
- `/multimodal` route mounts MultimodalPage on Chrome 148+ and the page renders header + Tabs (Docs first/Chat second) + zero-network footer (MULTI-01)
- "Multimodal" nav link appears AFTER "Proofreader" in both desktop and mobile nav (MULTI-01)
- When `LanguageModel.availability({expectedInputs:[{type:'image'}]})` returns `'unavailable'` or throws, MissingFlagBanner renders above the header with both flag URLs as copy-pasteable `<code>` elements; Docs tab remains usable (MULTI-01)
- `MultimodalService.promptWithImage(text, imageBlob | ImageBitmap, opts?)` exists and returns `Promise<ReadableStream<string>>` (MULTI-06)
- `MultimodalService.getAvailability` / `createWithProgress` / `destroyAllSessions` exist with the signatures locked in CONTEXT.md (MULTI-06)
- `seoConfigs.multimodal` and `seoConfigs.multimodalDocs` exist in BOTH `useSEOData.ts` and `prerender-react.js` with byte-identical title + description strings (Phase 12 audits)
- No `: any` annotations in `MultimodalService.ts` exports or `MultimodalPage.tsx` (TS strict — CLAUDE.md rule)
- Typecheck + lint pass for all 6 modified files
- Plan 02 can layer the chat panel onto this shell with zero additional changes to `MultimodalPage.tsx`'s declared state surface
</success_criteria>

<output>
After completion, create `.planning/phases/10-multimodal-foundation/10-01-SUMMARY.md` covering:
- Files created (4 new) + files modified (3 wiring files)
- Exact line counts and a confirmation that all interfaces (Message, PageState, AvailabilityState) are exported
- Smoke-test results from the verification section
- Any deviations from CONTEXT.md decisions (expected: none; record reasons if any)
- Hand-off note for Plan 02: confirms `messages` state and `objectUrlSetRef` are declared and ready to be passed to MultimodalChatPanel
</output>
