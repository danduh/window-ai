---
phase: 04-v1-1-foundation-page-shell-store-seed
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - chat/src/app/components/GenerativeUIPage.tsx
  - chat/src/app/components/GenerativeUI/GenerativeUIHeader.tsx
  - chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx
  - chat/src/app/components/GenerativeUI/MealPlanColumn.tsx
  - chat/src/app/AppRouter.tsx
  - chat/src/app/hooks/useSEOData.ts
  - chat/scripts/prerender-react.js
autonomous: true
requirements: [GENUI-01, GENUI-02, GENUI-03]
tags: [routing, react, ui, seo, prerender, brownfield, tailwind, dark-mode]

must_haves:
  truths:
    - "Clicking the new 'Generative UI' nav link (desktop or mobile) loads `/generative-ui` with the existing Tailwind theme + dark-mode toggle"
    - "The 'Generative UI' link sits immediately after 'WebMCP' and before 'Writer/Rewriter' in both desktop nav and mobile drawer"
    - "`/generative-ui` renders header + left ChatPlaceholder + right MealPlanColumn (320px wide on lg+, stacks vertically on mobile)"
    - "Hard-reloading `/generative-ui` shows the page (no console errors); the meal-plan column shows the empty-state copy"
    - "When `navigator.modelContext` is undefined, `MissingFlagBanner` renders above the two-column layout; the rest of the page stays usable"
    - "Mounting `/generative-ui` triggers `seedIfMissing(SEED_RECIPES)` exactly once; opening DevTools > Application > IndexedDB > window-ai-recipes shows both `recipes` and `meal-plan` stores after the first visit"
    - "The existing `/webmcp` route still renders the v1.0 Recipe Workbench unchanged (no regression)"
    - "`grep -F 'Generative UI — MCP Apps demo'` finds the same title in both `chat/src/app/hooks/useSEOData.ts` and `chat/scripts/prerender-react.js`"
  artifacts:
    - path: "chat/src/app/components/GenerativeUIPage.tsx"
      provides: "Top-level page: banner + header + two-column layout; mount-time seedIfMissing"
      min_lines: 50
    - path: "chat/src/app/components/GenerativeUI/GenerativeUIHeader.tsx"
      provides: "Page title 'Generative UI' + tagline; primary-500 accent icon block"
    - path: "chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx"
      provides: "Left column static empty-state copy"
    - path: "chat/src/app/components/GenerativeUI/MealPlanColumn.tsx"
      provides: "Right column 320px; calls useMealPlan(); renders empty-state + loading + error"
      exports: ["MealPlanColumn"]
    - path: "chat/src/app/AppRouter.tsx"
      provides: "Adds /generative-ui route + desktop nav link + mobile nav link + GenerativeUIPage import"
      contains: "<Route path=\"/generative-ui\""
    - path: "chat/src/app/hooks/useSEOData.ts"
      provides: "seoConfigs.generativeUI entry with title/description/keywords"
      contains: "generativeUI:"
    - path: "chat/scripts/prerender-react.js"
      provides: "Adds /generative-ui to routes array + seoConfigs map (byte-identical title/description/keywords to useSEOData.ts)"
      contains: "'/generative-ui':"
  key_links:
    - from: "chat/src/app/components/GenerativeUIPage.tsx"
      to: "chat/src/app/services/RecipePersistence.ts"
      via: "useEffect mount-time call to seedIfMissing(SEED_RECIPES)"
      pattern: "seedIfMissing\\("
    - from: "chat/src/app/components/GenerativeUI/MealPlanColumn.tsx"
      to: "chat/src/app/hooks/useMealPlan.ts"
      via: "calls useMealPlan() to read plan + loading"
      pattern: "useMealPlan\\("
    - from: "chat/src/app/AppRouter.tsx"
      to: "chat/src/app/components/GenerativeUIPage.tsx"
      via: "imports GenerativeUIPage and renders it for /generative-ui"
      pattern: "import.*GenerativeUIPage"
    - from: "chat/src/app/hooks/useSEOData.ts"
      to: "chat/scripts/prerender-react.js"
      via: "title/description/keywords byte-identical (grep -F drift check)"
      pattern: "Generative UI — MCP Apps demo"
---

<objective>
Land the v1.1 `/generative-ui` page shell on top of the persistence foundation from Plan 04-01. Create the page component plus 3 subcomponents under `chat/src/app/components/GenerativeUI/`, register the new route in `AppRouter.tsx` mirroring the existing `/webmcp` block (desktop nav + mobile nav + Route), add the `seoConfigs.generativeUI` entry to `useSEOData.ts`, and add a byte-identical mirror entry (route array + `seoConfigs` map) to `chat/scripts/prerender-react.js`.

Purpose: with the data layer already shipped in Plan 04-01, this plan delivers the visible tracer-bullet skeleton — a navigable empty-state page that survives hard reload, renders `MissingFlagBanner` when `navigator.modelContext` is undefined, and shows the meal-plan column ready to re-render the moment Phase 6's `commitRecipeToPlan` writes through `MealPlanStore.addToPlan`.

Output: 4 new files (page + 3 subcomponents) and 3 edited files (`AppRouter.tsx`, `useSEOData.ts`, `prerender-react.js`). All v1.0 routes/demos remain untouched. Brownfield discipline: do NOT edit `RecipeWorkbenchPage.tsx` or anything under `RecipeWorkbench/` other than importing `MissingFlagBanner` as-is.

Covers requirements GENUI-01 (route + nav + banner) and confirms GENUI-02 (mount-time `seedIfMissing` invocation) and GENUI-03 (MealPlanColumn consumes `useMealPlan` and survives reload).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-v1-1-foundation-page-shell-store-seed/04-CONTEXT.md
@.planning/phases/04-v1-1-foundation-page-shell-store-seed/04-RESEARCH.md
@.planning/phases/04-v1-1-foundation-page-shell-store-seed/04-UI-SPEC.md
@.planning/phases/04-v1-1-foundation-page-shell-store-seed/04-01-PLAN.md

<interfaces>
<!-- Critical contracts for this plan. Executor does NOT need to re-read these files unless modifying them. -->

From Plan 04-01 outputs (already shipped):
- `RecipePersistence.ts` exports: `getDB`, `seedIfMissing(seeds: Recipe[]): Promise<number>`, `PlanEntry`, `Recipe` (with new optional `totalMinutes?`, `searchableIngredients?` fields)
- `recipeSeed.ts` exports: `SEED_RECIPES: Recipe[]` (12 entries)
- `MealPlanStore.ts` exports: `getPlan`, `addToPlan`, `removeFromPlan`, `clearPlan`, `subscribeMealPlanStore`, `notifyMealPlanStore`, `type PlanEntry` (re-exported)
- `useMealPlan.ts` exports: `useMealPlan(): { plan: PlanEntry[]; loading: boolean }`, `interface UseMealPlanResult`

From chat/src/app/components/RecipeWorkbench/MissingFlagBanner.tsx (REUSE AS-IS, do not modify):
- `export const MissingFlagBanner: React.FC` — zero-prop, zero-state, renders banner with chrome://flags/#WebMCP copy.
- Import path from new components under `chat/src/app/components/`: `./RecipeWorkbench/MissingFlagBanner`

From chat/src/app/AppRouter.tsx (current layout — see read_first for exact lines):
- Line 70-72 area: desktop nav `<Link to="/webmcp" ... className="...font-medium...">WebMCP</Link>` is the block to clone for Generative UI
- Line 160-162 area: mobile nav `<Link to="/webmcp" ... className="...font-medium...">WebMCP</Link>` is the mobile block to clone
- Line 232-233 area: `<Route path="/webmcp" element={<RecipeWorkbenchPage/>}/>` is the route block to follow with `/generative-ui`
- The catch-all `<Route path="*" element={<Navigate to="/" replace/>}/>` at line 239 MUST stay last

From chat/src/app/hooks/useSEOData.ts (existing pattern — the entry to clone):
- Lines 68-72 show the `webmcp:` config with title, description, keywords. Add `generativeUI:` AFTER `webmcpDocs:` BEFORE the closing `} as const;`

From chat/scripts/prerender-react.js (the dual-write target — drift hazard):
- `routes` array lines 25-59: add `{ path: '/generative-ui', filename: 'generative-ui.html' }` AFTER the WebMCP routes block
- `getSEODataForRoute()` inner `seoConfigs` map lines 117-386: add `'/generative-ui':` entry AFTER `'/webmcp/docs':` block; title/description/keywords MUST match useSEOData.ts byte-for-byte

From RESEARCH.md Finding 6 (exact insertion points + Pitfall 4: use `font-medium`, NOT `font-normal`):
- Desktop nav className: `"text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200"`
- Mobile nav className adds `block` prefix and uses `text-base` instead of `text-sm`
- trackUserInteraction keys: `'generative_ui_link'` (desktop) and `'generative_ui_link_mobile'` (mobile)

From RESEARCH.md Finding 10 (exact SEO strings — byte-identical between the two files):
- title: `Generative UI — MCP Apps demo with on-device recipe cards | Chrome AI APIs`
- description: `A Chrome 146 Canary demo of the MCP Apps pattern: the in-page chat calls searchRecipes, an interactive recipe-card carousel renders in the chat bubble via a sandboxed iframe, and clicking Pick updates the meal-plan column live — all on-device, no network.`
- keywords: `MCP Apps, generative UI, navigator.modelContext, Chrome AI, on-device AI, recipe cards, sandboxed iframe, WebMCP, SEP-1865, meal plan`
- structuredData (prerender only): `{ '@context': 'https://schema.org', '@type': 'WebPage', name: 'Generative UI — MCP Apps Demo', description: 'On-device recipe-card carousel via sandboxed iframe and WebMCP tool results' }`

From UI-SPEC.md (Layout Contract — locked):
- Page wrapper: `min-h-screen bg-white dark:bg-gray-800 transition-colors duration-200`
- Inner container is the existing `<main className="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">` from AppRouter (no need to recreate it inside the page component)
- Two-column wrapper: `flex flex-col lg:flex-row gap-6`
- Left column: `flex-1 min-w-0`
- Right column: `lg:w-80 flex-shrink-0`

From UI-SPEC.md (Copywriting Contract — exact strings):
- Nav label: `Generative UI`
- Page title h1: `Generative UI`
- Page tagline: `On-device recipes, generated and picked right in the chat — no server, no network.`
- Chat placeholder heading: `Chat coming in a future phase`
- Chat placeholder body: `Ask for a recipe here and an interactive card carousel will appear — powered by Chrome's built-in AI, all on-device.`
- Meal-plan empty state heading: `Your meal plan is empty`
- Meal-plan empty state body: `Recipes you pick from the chat will appear here. Come back once chat is wired up.`
- Loading copy: `Loading plan…`
- IDB load failure copy: `Failed to load meal plan. Reload the page to try again.`
- Seed log message: `seedIfMissing: inserted {N} new recipes` (console.log only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the three GenerativeUI subcomponents (Header, ChatPlaceholder, MealPlanColumn)</name>
  <files>chat/src/app/components/GenerativeUI/GenerativeUIHeader.tsx, chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx, chat/src/app/components/GenerativeUI/MealPlanColumn.tsx</files>
  <read_first>
    - .planning/phases/04-v1-1-foundation-page-shell-store-seed/04-UI-SPEC.md (entire file — copy strings, layout classes, color tokens, component inventory)
    - chat/src/app/hooks/useMealPlan.ts (post Plan 04-01 — the hook API the MealPlanColumn consumes)
    - chat/src/app/components/RecipeWorkbenchPage.tsx (lines 285-330 area — for the established page header pattern with primary-500 gradient icon block to mirror in GenerativeUIHeader)
    - .planning/phases/04-v1-1-foundation-page-shell-store-seed/04-CONTEXT.md (Page Shell Layout decisions, Claude's Discretion notes for empty-state visual)
  </read_first>
  <action>
    Create the `chat/src/app/components/GenerativeUI/` directory and three new files inside it. Every component is a React 19 functional component, default-exported as a named export (not default export), TypeScript strict, dark-mode-aware via `dark:` Tailwind classes. No `any`, no `as any`, no `console.log` left in production code paths.

    **File 1: `chat/src/app/components/GenerativeUI/GenerativeUIHeader.tsx`**
    - Named export: `export const GenerativeUIHeader: React.FC = () => { ... }`. No props.
    - Wrapper: a `<header>` with `mb-8` bottom margin (matches UI-SPEC xl spacing).
    - Layout: flex row with a gradient icon block on the left (mirrors `RecipeWorkbenchPage.tsx` header pattern), title + tagline on the right.
    - Icon block: a `div` with classes `flex-shrink-0 w-12 h-12 rounded-xl bg-gradient-to-br from-primary-500 to-purple-600 flex items-center justify-center text-white shadow-sm` containing an inline SVG icon (executor's choice — a chat/sparkles/grid icon, 24x24 viewBox, currentColor). The accent gradient is the ONLY use of the primary color on this page (UI-SPEC Color section).
    - Title `<h1>`: text `Generative UI` with classes `text-3xl font-bold text-gray-900 dark:text-white leading-tight`.
    - Tagline `<p>`: exact text `On-device recipes, generated and picked right in the chat — no server, no network.` with classes `mt-2 text-base text-gray-600 dark:text-gray-400`.

    **File 2: `chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx`**
    - Named export: `export const ChatPlaceholder: React.FC = () => { ... }`. No props. No state. No interactivity.
    - Card wrapper classes: `bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-6 min-h-[400px] flex flex-col items-center justify-center text-center`.
    - Heading `<h2>` exact text: `Chat coming in a future phase` with classes `text-xl font-bold text-gray-800 dark:text-gray-100`.
    - Body `<p>` exact text: `Ask for a recipe here and an interactive card carousel will appear — powered by Chrome's built-in AI, all on-device.` with classes `mt-3 max-w-md text-sm text-gray-500 dark:text-gray-400`.
    - No buttons. No inputs. No links.

    **File 3: `chat/src/app/components/GenerativeUI/MealPlanColumn.tsx`**
    - Named export: `export const MealPlanColumn: React.FC = () => { ... }`. No props.
    - Imports: `import { useMealPlan } from '../../hooks/useMealPlan';`.
    - Body: `const { plan, loading } = useMealPlan();`.
    - Card wrapper classes: `bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-6`.
    - Column heading `<h2>`: text `Meal Plan` with classes `text-lg font-bold text-gray-800 dark:text-gray-100 mb-4`.
    - Render the three states:
      - `loading` true: render `<p className="text-sm text-gray-500 dark:text-gray-400">Loading plan…</p>`.
      - `plan.length === 0` (after load): render a centered empty-state block — a muted inline-SVG icon (executor's choice, gray-400, 32x32-ish viewBox) above a heading `<h3 className="mt-3 text-base font-bold text-gray-800 dark:text-gray-100">Your meal plan is empty</h3>` and body `<p className="mt-2 text-sm text-gray-500 dark:text-gray-400 max-w-xs">Recipes you pick from the chat will appear here. Come back once chat is wired up.</p>`. Wrap in `flex flex-col items-center text-center py-6`.
      - `plan.length > 0`: render a `<ul>` of plan entries showing `recipeId` and `addedAt` (formatted via `new Date(addedAt).toLocaleString()`). No interactive affordances yet (no remove buttons — Phase 6). Each `<li>` uses classes `py-2 border-b border-gray-100 dark:border-gray-700 last:border-b-0 text-sm text-gray-700 dark:text-gray-300`. Key by `entry.id`.
    - No error UI is rendered in Phase 4 (per UI-SPEC: error-states-visible-in-Phase-4 column says IDB open failure is non-blocking; the `useMealPlan` hook already swallows the error and just leaves `plan` empty + `loading` false). If the executor wants belt-and-suspenders, an optional inline `text-sm text-red-500 dark:text-red-400` paragraph CAN be added, but the hook's catch already keeps the UI from breaking — additional UI is at executor discretion.

    Common to all three files:
    - Top-of-file imports: `import React from 'react';` (or use `import type { FC } from 'react'` if executor prefers).
    - No default exports (use named exports for tree-shake clarity, matching the rest of the codebase).
    - No `data-testid` attributes unless the executor adds tests; this phase has no test scaffold requirement.
  </action>
  <verify>
    <automated>cd /Users/danielos/dev/window-ai && [ -f chat/src/app/components/GenerativeUI/GenerativeUIHeader.tsx ] && [ -f chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx ] && [ -f chat/src/app/components/GenerativeUI/MealPlanColumn.tsx ] && npx nx typecheck chat 2>&1 | tail -10 && grep -l "Generative UI" chat/src/app/components/GenerativeUI/GenerativeUIHeader.tsx && grep -l "Chat coming in a future phase" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx && grep -l "Your meal plan is empty" chat/src/app/components/GenerativeUI/MealPlanColumn.tsx && grep -q "useMealPlan" chat/src/app/components/GenerativeUI/MealPlanColumn.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: all three files exist at the listed paths.
    - Source: `grep -F "Generative UI" chat/src/app/components/GenerativeUI/GenerativeUIHeader.tsx` matches the page title.
    - Source: `grep -F "On-device recipes, generated and picked right in the chat — no server, no network." chat/src/app/components/GenerativeUI/GenerativeUIHeader.tsx` matches the tagline byte-for-byte.
    - Source: `grep -F "Chat coming in a future phase" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx` matches.
    - Source: `grep -F "Ask for a recipe here and an interactive card carousel will appear" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx` matches.
    - Source: `grep -F "Your meal plan is empty" chat/src/app/components/GenerativeUI/MealPlanColumn.tsx` matches.
    - Source: `grep -F "Recipes you pick from the chat will appear here." chat/src/app/components/GenerativeUI/MealPlanColumn.tsx` matches.
    - Source: `grep -F "Loading plan…" chat/src/app/components/GenerativeUI/MealPlanColumn.tsx` matches.
    - Source: `grep -c "useMealPlan" chat/src/app/components/GenerativeUI/MealPlanColumn.tsx` returns >=2 (import + call).
    - Source: `grep -c "from-primary-500 to-purple-600" chat/src/app/components/GenerativeUI/GenerativeUIHeader.tsx` returns 1 (the only place primary accent is used on this page).
    - Source: `grep -cE "export const (GenerativeUIHeader|ChatPlaceholder|MealPlanColumn): React\.FC" chat/src/app/components/GenerativeUI/*.tsx` returns `3` (one named export per file).
    - Behavior: `npx nx typecheck chat` exits 0.
    - Behavior: `npx nx lint chat` shows no new errors in the three new files.
  </acceptance_criteria>
  <done>
    Three new files under `chat/src/app/components/GenerativeUI/`, each a named-export React FC with the locked UI-SPEC copy and class names. `MealPlanColumn` calls `useMealPlan()` and renders the three branches (loading / empty / populated). Typecheck and lint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GenerativeUIPage.tsx — top-level page with banner + two-column shell + mount-time seedIfMissing</name>
  <files>chat/src/app/components/GenerativeUIPage.tsx</files>
  <read_first>
    - chat/src/app/components/RecipeWorkbenchPage.tsx (lines 113-150 mount-time seed effect with cancelled flag — the StrictMode-safe pattern to mirror)
    - chat/src/app/components/RecipeWorkbench/MissingFlagBanner.tsx (entire file — confirm it's a zero-prop named export)
    - chat/src/app/services/RecipePersistence.ts (post Plan 04-01 — `seedIfMissing` signature)
    - chat/src/app/services/recipeSeed.ts (post Plan 04-01 — `SEED_RECIPES` 12 entries)
    - .planning/phases/04-v1-1-foundation-page-shell-store-seed/04-UI-SPEC.md (Layout Contract + Interaction Contract for banner placement)
    - .planning/phases/04-v1-1-foundation-page-shell-store-seed/04-RESEARCH.md (Finding 3 mount-effect example, Finding 7 MissingFlagBanner import path)
  </read_first>
  <action>
    Create NEW file `chat/src/app/components/GenerativeUIPage.tsx`. This is the top-level route handler for `/generative-ui`. It composes the four child components (MissingFlagBanner + GenerativeUIHeader + ChatPlaceholder + MealPlanColumn), runs `seedIfMissing(SEED_RECIPES)` once on mount, and applies the locked UI-SPEC layout classes.

    1. Imports:
       - `import React, { useEffect, useState } from 'react';`
       - `import { useSEOData, seoConfigs } from '../hooks/useSEOData';`
       - `import { seedIfMissing } from '../services/RecipePersistence';`
       - `import { SEED_RECIPES } from '../services/recipeSeed';`
       - `import { MissingFlagBanner } from './RecipeWorkbench/MissingFlagBanner';`
       - `import { GenerativeUIHeader } from './GenerativeUI/GenerativeUIHeader';`
       - `import { ChatPlaceholder } from './GenerativeUI/ChatPlaceholder';`
       - `import { MealPlanColumn } from './GenerativeUI/MealPlanColumn';`

    2. Named export: `export const GenerativeUIPage: React.FC = () => { ... }`.

    3. SEO wire: at the top of the component body, call `useSEOData(seoConfigs.generativeUI)`. (The `seoConfigs.generativeUI` entry is added in Task 4.)

    4. Banner gating: derive `const hasModelContext = typeof (globalThis as { navigator?: { modelContext?: unknown } }).navigator?.modelContext !== 'undefined';` — OR mirror whatever exact pattern `RecipeWorkbenchPage.tsx` uses (executor's discretion — match the v1.0 pattern verbatim if available). Result: a boolean `showBanner = !hasModelContext` used to conditionally render `<MissingFlagBanner />`. The banner check MUST be lazy (inside `useEffect` or computed inside the component body) so it works in JSDOM prerender (where `navigator.modelContext` is undefined and the prerendered HTML correctly includes the banner shell).

    5. Mount-time seed effect (RESEARCH.md Code Examples — Mount-time seed effect):
       ```
       useEffect(() => {
         let cancelled = false;
         (async () => {
           try {
             const inserted = await seedIfMissing(SEED_RECIPES);
             if (inserted > 0 && !cancelled) {
               // eslint-disable-next-line no-console
               console.log(`[GenerativeUI] seedIfMissing: inserted ${inserted} new recipes`);
             }
           } catch (err) {
             const message = err instanceof Error ? err.message : 'Unknown error';
             // eslint-disable-next-line no-console
             console.error('[GenerativeUI] Failed to seed recipe library:', message);
           }
         })();
         return () => { cancelled = true; };
       }, []);
       ```

    6. JSX layout — match the locked UI-SPEC Layout Contract:
       - Outer fragment OR a `<div>` with no wrapper classes (the `<main className="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">` from `AppRouter.tsx:204` already wraps every page, so do NOT recreate it here).
       - First child: `{!hasModelContext && <MissingFlagBanner />}` (banner above the rest).
       - Second child: `<GenerativeUIHeader />`.
       - Third child: the two-column wrapper `<div className="flex flex-col lg:flex-row gap-6">` containing:
         - `<div className="flex-1 min-w-0"><ChatPlaceholder /></div>`
         - `<div className="lg:w-80 flex-shrink-0"><MealPlanColumn /></div>`

    7. Do NOT add: a route registration (Task 3), an SEO config object (Task 4), a tool registration (Phase 6), an iframe (Phase 5), a chat panel (Phase 6).
  </action>
  <verify>
    <automated>cd /Users/danielos/dev/window-ai && [ -f chat/src/app/components/GenerativeUIPage.tsx ] && npx nx typecheck chat 2>&1 | tail -10 && grep -c "seedIfMissing" chat/src/app/components/GenerativeUIPage.tsx && grep -c "MissingFlagBanner" chat/src/app/components/GenerativeUIPage.tsx && grep -c "GenerativeUIHeader\|ChatPlaceholder\|MealPlanColumn" chat/src/app/components/GenerativeUIPage.tsx && grep -q "useSEOData" chat/src/app/components/GenerativeUIPage.tsx && grep -q "let cancelled = false" chat/src/app/components/GenerativeUIPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: file `chat/src/app/components/GenerativeUIPage.tsx` exists.
    - Source: `grep -c "export const GenerativeUIPage" chat/src/app/components/GenerativeUIPage.tsx` returns `1`.
    - Source: `grep -c "seedIfMissing(SEED_RECIPES)" chat/src/app/components/GenerativeUIPage.tsx` returns `1` (the mount-time call).
    - Source: `grep -c "MissingFlagBanner" chat/src/app/components/GenerativeUIPage.tsx` returns >=2 (import + render).
    - Source: `grep -c "GenerativeUIHeader" chat/src/app/components/GenerativeUIPage.tsx` returns >=2 (import + render).
    - Source: `grep -c "ChatPlaceholder" chat/src/app/components/GenerativeUIPage.tsx` returns >=2 (import + render).
    - Source: `grep -c "MealPlanColumn" chat/src/app/components/GenerativeUIPage.tsx` returns >=2 (import + render).
    - Source: `grep -c "useSEOData" chat/src/app/components/GenerativeUIPage.tsx` returns >=1 (the SEO hook call).
    - Source: `grep -c "let cancelled = false" chat/src/app/components/GenerativeUIPage.tsx` returns `1` (StrictMode-safe mount effect).
    - Source: `grep -E "flex flex-col lg:flex-row" chat/src/app/components/GenerativeUIPage.tsx` matches (two-column wrapper).
    - Source: `grep -E "lg:w-80" chat/src/app/components/GenerativeUIPage.tsx` matches (right column 320px).
    - Behavior: `npx nx typecheck chat` exits 0.
    - Behavior: `npx nx lint chat` shows no new errors in `GenerativeUIPage.tsx`.
  </acceptance_criteria>
  <done>
    `GenerativeUIPage.tsx` exists as a named React FC; renders banner (conditional) + header + two-column shell; runs `seedIfMissing(SEED_RECIPES)` once on mount with StrictMode-safe `cancelled` flag; calls `useSEOData(seoConfigs.generativeUI)`. Typecheck and lint clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire AppRouter.tsx — desktop nav link + mobile nav link + /generative-ui route</name>
  <files>chat/src/app/AppRouter.tsx</files>
  <read_first>
    - chat/src/app/AppRouter.tsx (entire file — current line numbers for nav and route insertion)
    - .planning/phases/04-v1-1-foundation-page-shell-store-seed/04-RESEARCH.md (Finding 6 — exact insertion points, exact className strings, Pitfall 4 use `font-medium` not `font-normal`)
    - .planning/phases/04-v1-1-foundation-page-shell-store-seed/04-UI-SPEC.md (Interaction Contract → Nav Link section)
  </read_first>
  <action>
    Edit `chat/src/app/AppRouter.tsx` additively. Three insertions and one new import. Do NOT reorder existing nav links. Do NOT modify the `<Route path="*">` catch-all (it stays last).

    1. **Add import** near the existing `RecipeWorkbenchPage` import (around line 10):
       `import { GenerativeUIPage } from './components/GenerativeUIPage';`

    2. **Desktop nav link** — insert immediately AFTER the `<Link to="/webmcp">…WebMCP</Link>` block (which currently ends at line 72) and BEFORE the next `<Link to="/writer">` block (line 73). Use this exact element:
       `<Link to="/generative-ui"`
         `      onClick={() => trackUserInteraction('navigation_click', 'generative_ui_link')}`
         `      className="text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">Generative UI</Link>`
       The `font-medium` class matches the rest of the desktop nav (Pitfall 4 in RESEARCH.md — UI-SPEC's `font-normal` was incorrect; existing links use `font-medium`).

    3. **Mobile nav link** — insert immediately AFTER the `<Link to="/webmcp">…WebMCP</Link>` mobile block (currently ends ~line 162) and BEFORE the `<Link to="/writer">` mobile block. Use this exact element:
       `<Link to="/generative-ui"`
         `      onClick={() => trackUserInteraction('navigation_click', 'generative_ui_link_mobile')}`
         `      className="text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white block px-3 py-2 rounded-md text-base font-medium transition-colors duration-200">Generative UI</Link>`

    4. **Route registration** — insert immediately AFTER the `<Route path="/webmcp/docs" .../>` block (currently line 233) and BEFORE the `{/* Writer/Rewriter routes */}` comment (line 235). Use this block:
       `{/* Generative UI routes */}`
       `<Route path="/generative-ui" element={<GenerativeUIPage/>}/>`

    5. Do NOT add: any other route variants (e.g., `/generative-ui/docs` is Phase 7; do NOT register it now). Do NOT modify the existing `/webmcp` route, the existing nav links, the catch-all route, or any other part of the file.

    6. Spacing/formatting: match the existing style (the existing `<Link>` blocks use 2-space indentation continuation; the existing `<Route>` block is single-line). If the executor's editor auto-formats with Prettier on save, ensure the result matches the surrounding code's quote style (single quotes) and trailing-comma behavior.
  </action>
  <verify>
    <automated>cd /Users/danielos/dev/window-ai && npx nx typecheck chat 2>&1 | tail -10 && grep -c "to=\"/generative-ui\"" chat/src/app/AppRouter.tsx && grep -c "GenerativeUIPage" chat/src/app/AppRouter.tsx && grep -c "generative_ui_link" chat/src/app/AppRouter.tsx && grep -c "to=\"/webmcp\"" chat/src/app/AppRouter.tsx && grep -n "path=\"\\*\"" chat/src/app/AppRouter.tsx | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "to=\"/generative-ui\"" chat/src/app/AppRouter.tsx` returns `2` (one desktop + one mobile nav link).
    - Source: `grep -c "GenerativeUIPage" chat/src/app/AppRouter.tsx` returns `2` (one import + one Route element).
    - Source: `grep -c "generative_ui_link'" chat/src/app/AppRouter.tsx` returns `1` (desktop tracking key).
    - Source: `grep -c "generative_ui_link_mobile" chat/src/app/AppRouter.tsx` returns `1` (mobile tracking key).
    - Source: `grep -c "path=\"/generative-ui\"" chat/src/app/AppRouter.tsx` returns `1` (single route, no docs variant).
    - Source: `grep -c "to=\"/webmcp\"" chat/src/app/AppRouter.tsx` returns `2` (existing WebMCP nav links still present — not regressed).
    - Source: `grep -c "<Route path=\"\\*\"" chat/src/app/AppRouter.tsx` returns `1` (catch-all still last).
    - Source: `grep -n "to=\"/generative-ui\"" chat/src/app/AppRouter.tsx` shows two line numbers — the first must be smaller than the line number for the desktop `<Link to="/writer">` (i.e., link sits between `/webmcp` and `/writer` in desktop nav).
    - Source: `grep -c "font-normal" chat/src/app/AppRouter.tsx` returns `0` for the new lines (Pitfall 4 — must use `font-medium`).
    - Behavior: `npx nx typecheck chat` exits 0.
    - Behavior: Manual or automated: `npx nx serve chat` then visit `http://localhost:4300/generative-ui` — page renders with the new nav link visible; clicking it routes to the page without console errors. Returning to `/webmcp` still renders the v1.0 Recipe Workbench (no regression).
  </acceptance_criteria>
  <done>
    `AppRouter.tsx` has one new import, two new nav `<Link>` blocks (desktop + mobile) wedged between `/webmcp` and `/writer`, and one new `<Route path="/generative-ui">`. All existing routes and links untouched. Typecheck clean. Catch-all route remains last.
  </done>
</task>

<task type="auto">
  <name>Task 4: Mirror SEO config — useSEOData.ts + prerender-react.js (byte-identical title/description/keywords)</name>
  <files>chat/src/app/hooks/useSEOData.ts, chat/scripts/prerender-react.js</files>
  <read_first>
    - chat/src/app/hooks/useSEOData.ts (entire file — pattern of `webmcp:` and `webmcpDocs:` entries; placement before the closing `} as const;`)
    - chat/scripts/prerender-react.js (entire file — `routes` array structure lines 25-59; `getSEODataForRoute()` inner `seoConfigs` map structure ending with `/webmcp/docs` block)
    - .planning/phases/04-v1-1-foundation-page-shell-store-seed/04-RESEARCH.md (Finding 10 — exact strings; Pitfall 5 — drift hazard from Phase 3 hotfix)
  </read_first>
  <action>
    Add a new SEO entry for `/generative-ui` in BOTH files. The `title`, `description`, and `keywords` strings MUST be byte-identical between the two files (verifiable via `grep -F`). This is the dual-write hazard called out in RESEARCH.md Pitfall 5 + Phase 3 D-08/D-12 hotfix history.

    **File A: `chat/src/app/hooks/useSEOData.ts`**

    Insert a new `generativeUI` entry inside the `seoConfigs` object, BEFORE the closing `} as const;` line. Place it AFTER the existing `webmcpDocs` entry. Use exactly these strings:

    ```
    // Must match prerender-react.js seoConfigs['/generative-ui'] verbatim.
    // See Phase 3 D-08 + D-12 — prerender drift caused a Phase 3 hotfix.
    generativeUI: {
      title: 'Generative UI — MCP Apps demo with on-device recipe cards | Chrome AI APIs',
      description: 'A Chrome 146 Canary demo of the MCP Apps pattern: the in-page chat calls searchRecipes, an interactive recipe-card carousel renders in the chat bubble via a sandboxed iframe, and clicking Pick updates the meal-plan column live — all on-device, no network.',
      keywords: 'MCP Apps, generative UI, navigator.modelContext, Chrome AI, on-device AI, recipe cards, sandboxed iframe, WebMCP, SEP-1865, meal plan'
    }
    ```

    Note: the trailing comma between the new entry and the closing `} as const;` depends on whether the prior entry already has a trailing comma — match the surrounding style (the existing `webmcpDocs` entry has no trailing comma, so a leading comma at the start of the new entry's preceding line is the cleanest insertion).

    **File B: `chat/scripts/prerender-react.js`**

    Two insertions:

    1. **`routes` array** — insert after the existing `{ path: '/webmcp/docs', filename: 'webmcp-docs.html' },` line (currently around line 58), BEFORE the closing `]` of the array:
       ```
       // Generative UI route
       { path: '/generative-ui', filename: 'generative-ui.html' },
       ```

    2. **`getSEODataForRoute()` inner `seoConfigs` map** — insert after the existing `'/webmcp/docs':` block (which closes at line 385), BEFORE the closing `}` of the `seoConfigs` object (line 386). Use exactly these strings (title/description/keywords IDENTICAL to File A):
       ```
       '/generative-ui': {
         title: 'Generative UI — MCP Apps demo with on-device recipe cards | Chrome AI APIs',
         description: 'A Chrome 146 Canary demo of the MCP Apps pattern: the in-page chat calls searchRecipes, an interactive recipe-card carousel renders in the chat bubble via a sandboxed iframe, and clicking Pick updates the meal-plan column live — all on-device, no network.',
         keywords: 'MCP Apps, generative UI, navigator.modelContext, Chrome AI, on-device AI, recipe cards, sandboxed iframe, WebMCP, SEP-1865, meal plan',
         structuredData: {
           '@context': 'https://schema.org',
           '@type': 'WebPage',
           name: 'Generative UI — MCP Apps Demo',
           description: 'On-device recipe-card carousel via sandboxed iframe and WebMCP tool results',
         },
       },
       ```

    Do NOT modify: any other SEO entries; any other routes; the `createSitemap`/`createRobotsTxt`/`createEnhancedHTML` functions; the top-of-file imports.

    After both files are updated, run the byte-identical check immediately as part of this task (do not defer to verification):
    `grep -F 'Generative UI — MCP Apps demo with on-device recipe cards | Chrome AI APIs' chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js`
    This MUST output exactly 2 lines (one per file). If output is 1 line or 0, the entries are misaligned — fix before declaring the task done.
  </action>
  <verify>
    <automated>cd /Users/danielos/dev/window-ai && npx nx typecheck chat 2>&1 | tail -10 && [ "$(grep -F 'Generative UI — MCP Apps demo with on-device recipe cards | Chrome AI APIs' chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js | wc -l | tr -d ' ')" = "2" ] && [ "$(grep -F 'A Chrome 146 Canary demo of the MCP Apps pattern' chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js | wc -l | tr -d ' ')" = "2" ] && [ "$(grep -F 'MCP Apps, generative UI, navigator.modelContext, Chrome AI, on-device AI, recipe cards, sandboxed iframe, WebMCP, SEP-1865, meal plan' chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js | wc -l | tr -d ' ')" = "2" ] && grep -q "generativeUI:" chat/src/app/hooks/useSEOData.ts && grep -q "'/generative-ui':" chat/scripts/prerender-react.js && grep -q "filename: 'generative-ui.html'" chat/scripts/prerender-react.js</automated>
  </verify>
  <acceptance_criteria>
    - Drift check (the most important assertion — Pitfall 5): `grep -F 'Generative UI — MCP Apps demo with on-device recipe cards | Chrome AI APIs' chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js` outputs exactly 2 lines.
    - Drift check description: `grep -F 'A Chrome 146 Canary demo of the MCP Apps pattern' chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js` outputs 2 lines.
    - Drift check keywords: `grep -F 'MCP Apps, generative UI, navigator.modelContext, Chrome AI, on-device AI, recipe cards, sandboxed iframe, WebMCP, SEP-1865, meal plan' chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js` outputs 2 lines.
    - Source useSEOData.ts: `grep -c "generativeUI:" chat/src/app/hooks/useSEOData.ts` returns `1`.
    - Source prerender-react.js: `grep -c "'/generative-ui':" chat/scripts/prerender-react.js` returns `1` (the seoConfigs map entry).
    - Source prerender-react.js: `grep -c "path: '/generative-ui'" chat/scripts/prerender-react.js` returns `1` (the routes array entry).
    - Source prerender-react.js: `grep -c "filename: 'generative-ui.html'" chat/scripts/prerender-react.js` returns `1`.
    - Source: existing `webmcp` and `webmcpDocs` entries are still present and unchanged — `grep -c "webmcp:" chat/src/app/hooks/useSEOData.ts` returns `1`, `grep -c "webmcpDocs:" chat/src/app/hooks/useSEOData.ts` returns `1`.
    - Behavior: `npx nx typecheck chat` exits 0.
    - Behavior: `npx nx build chat` succeeds (optional — confirms the prerender script can parse the JS file, but only worth running if `chat` build is fast in this monorepo).
  </acceptance_criteria>
  <done>
    `useSEOData.ts` has a new `generativeUI` config entry. `prerender-react.js` has a matching `/generative-ui` entry in BOTH the `routes` array and the `getSEODataForRoute()` map. Title/description/keywords are byte-identical across the two files (verified via grep -F). No other entries modified. Typecheck clean.
  </done>
</task>

</tasks>

<verification>
End-to-end checks after all four tasks complete:

1. **Typecheck:** `npx nx typecheck chat` exits 0.
2. **Lint:** `npx nx lint chat` shows no new errors in the seven touched files.
3. **Tests:** `npx nx test chat` introduces no new failures.
4. **Prerender drift:** `grep -F 'Generative UI — MCP Apps demo' chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js` outputs 2 lines (byte-identical title).
5. **Smoke (dev server):** `npx nx serve chat` starts on port 4300; visit `http://localhost:4300/generative-ui` and confirm:
   - Page renders header + ChatPlaceholder (left) + MealPlanColumn (right, "Your meal plan is empty" copy after brief Loading flash).
   - If running on a browser without `navigator.modelContext`, `MissingFlagBanner` is visible above the columns.
   - Hard refresh (Cmd+Shift+R) — page reloads without console errors.
   - DevTools → Application → IndexedDB → `window-ai-recipes` shows BOTH `recipes` (≥12 entries) AND `meal-plan` (0 entries) object stores after first visit.
6. **No regression:** `http://localhost:4300/webmcp` still renders the v1.0 Recipe Workbench. The existing 2 v1.0 recipes (`buttermilk-pancakes`, `tomato-pasta`) are still present in the picker.
7. **Nav placement:** in desktop nav, `Generative UI` link sits between `WebMCP` and `Writer/Rewriter`. In mobile nav (open hamburger menu), same order.
8. **Build (optional but recommended):** `npx nx build chat` succeeds and the prerender script generates a `generative-ui.html` artifact in `dist/chat/` with the correct title in the `<title>` tag.
</verification>

<success_criteria>
- GENUI-01 satisfied: `/generative-ui` is a registered route, nav link exists (desktop + mobile) between WebMCP and Writer/Rewriter, `MissingFlagBanner` renders when `navigator.modelContext` is undefined while the rest of the page stays usable.
- GENUI-02 satisfied at the integration layer: mounting `/generative-ui` calls `seedIfMissing(SEED_RECIPES)` once; IDB inspection after first visit shows 12 recipes including the v1.0 two preserved.
- GENUI-03 satisfied at the integration layer: `MealPlanColumn` subscribes via `useMealPlan()` and renders empty-state; entries written via `MealPlanStore.addToPlan` would re-render the column (testable in DevTools console: `import('./services/MealPlanStore').then(m => m.addToPlan({ id: crypto.randomUUID(), recipeId: 'tomato-pasta', addedAt: Date.now() }))` — the column re-renders with the new entry, and a hard reload preserves it).
- All seven files written/edited; no edits to `RecipeWorkbenchPage.tsx`, `MissingFlagBanner.tsx`, `mcp/`, `mcp-client/`, `devops/awsweb/`.
- Prerender drift gate passes (`grep -F` returns 2 lines for title + description + keywords).
- Typecheck, lint, and existing tests are clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-v1-1-foundation-page-shell-store-seed/04-02-SUMMARY.md` describing:
- Files created vs modified (7 total: 4 created, 3 edited)
- Smoke test results (URL hit, IDB inspection, regression check on `/webmcp`)
- The exact line numbers where nav links and the route landed in `AppRouter.tsx` (post-edit)
- Any deviation from the locked UI-SPEC copy (none expected; flag if any)
- Confirmation that `grep -F 'Generative UI — MCP Apps demo' chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js` outputs 2 lines
- Anything Phase 5/6/7 needs to know — e.g., "GenerativeUIPage's banner gate uses `typeof navigator.modelContext` — Phase 6 should reuse this guard rather than reimplementing"
</output>
</content>
</invoke>