---
phase: 4
slug: v1-1-foundation-page-shell-store-seed
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-18
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for Phase 4: v1.1 Foundation — Page shell, store, seed.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Tailwind CSS 3.4.3 with custom `primary` color scale (source: `chat/tailwind.config.js`) |
| Preset | not applicable |
| Component library | none — inline JSX components, no Radix/Base UI |
| Icon library | none — inline SVG (project-wide convention, all existing pages) |
| Font | system-ui stack (inherited from browser; no custom font loaded) |

No `components.json` found in project root. No shadcn initialization needed — existing pages are built directly on Tailwind CSS with hand-rolled components. New `/generative-ui` page mirrors this exactly.

---

## Spacing Scale

Declared values (must be multiples of 4). Source: established Tailwind classes observed across `RecipeWorkbenchPage.tsx`, `HomePage.tsx`, `MissingFlagBanner.tsx`.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (`p-1`, `gap-1`) | Icon-text gaps, fine inline spacing |
| sm | 8px (`p-2`, `gap-2`, `space-x-2`) | Compact element padding, icon gutters |
| md | 16px (`p-4`, `gap-4`, `px-4`) | Default card padding, section inner padding |
| lg | 24px (`p-6`, `gap-6`) | Card/panel padding, between-section gaps |
| xl | 32px (`py-8`, `mb-8`) | Page header bottom margin, major section breaks |
| 2xl | 48px (`py-12`) | Full-page vertical breathing room on spacious screens |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Touch targets (mobile hamburger button, nav links in mobile drawer): minimum 44px height via `py-2` + `text-base` line-height — matches existing mobile nav in `AppRouter.tsx`.
- Meal-plan column fixed width: `lg:w-80` (320px) to `lg:w-96` (384px) — executor chooses one value; 320px is the lower bound per CONTEXT.md.

---

## Typography

Source: observed classes in `RecipeWorkbenchPage.tsx`, `HomePage.tsx`, `AppRouter.tsx`.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 (`font-normal` / default) | 1.5 (Tailwind default for `text-sm`) |
| Label / nav link | 14px (`text-sm`) — desktop nav; 16px (`text-base`) — mobile nav | 500 (`font-medium`) | 1.5 |
| Heading (page title) | 30px (`text-3xl`) | 700 (`font-bold`) | 1.2 (`leading-tight` or browser default) |
| Subheading / tagline | 16px (`text-base`) | 400 (`font-normal`) | 1.5 |

Sizes are exactly 4 values. Weights are exactly 2 (400 regular + 700 bold; `font-medium` (500) is used only for nav links and pills — counts as part of the label role). All sizes match what is already in use project-wide; no new type sizes are introduced in Phase 4.

---

## Color

Source: `chat/tailwind.config.js` (primary palette), `MissingFlagBanner.tsx` (alert yellow), `RecipeWorkbenchPage.tsx`, `HomePage.tsx` (surface colors and text colors). `darkMode: 'class'` — all color tokens below have a `dark:` counterpart.

| Role | Light value | Dark value | Usage |
|------|-------------|------------|-------|
| Dominant (60%) | `white` / `gray-50` (`bg-white`) | `gray-800` (`dark:bg-gray-800`) | Page background, main surface |
| Secondary (30%) | `white` with `border-gray-200` (`bg-white border`) | `gray-800` with `border-gray-700` (`dark:bg-gray-800 dark:border-gray-700`) | Cards, column panels (`MealPlanColumn`, chat placeholder panel) |
| Accent (10%) | `primary-500` (#3b82f6) | `primary-400` (#60a5fa) | Gradient icon block in page header only; primary CTA `Add to Plan` button (Phase 6 — not rendered in Phase 4) |
| Alert / banner | `yellow-50` + `yellow-200` border | `yellow-900/20` + `yellow-800` border | `MissingFlagBanner` — reused as-is, no changes |
| Destructive | `red-600` / `red-500` | `red-400` | Remove-from-plan button (Phase 4 renders no plan entries, so no destructive UI appears in this phase; token reserved for Phase 6) |

Accent reserved for:
1. Page header icon block background (gradient `from-primary-500 to-purple-600`) — one element per page, matching the `RecipeWorkbenchPage` and `HomePage` pattern
2. Focus rings (`focus:ring-primary-500`) on all interactive elements

The accent color does NOT apply to: body text, card borders, labels, empty-state copy, column backgrounds, or the `MissingFlagBanner`.

---

## Component Inventory

All components are new files under `chat/src/app/components/GenerativeUI/`. No existing component is modified in Phase 4 (brownfield discipline).

| Component | File | Description |
|-----------|------|-------------|
| `GenerativeUIPage` | `chat/src/app/components/GenerativeUIPage.tsx` | Top-level page component; mounts banner, header, two-column shell |
| `ChatPlaceholder` | `chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx` | Left-column empty area; static copy hinting chat will appear here |
| `MealPlanColumn` | `chat/src/app/components/GenerativeUI/MealPlanColumn.tsx` | Right-column 320–360px; renders empty-state when plan is empty, list when populated (Phase 6) |
| `MissingFlagBanner` | `chat/src/app/components/RecipeWorkbench/MissingFlagBanner.tsx` | Reused as-is — no modifications |

---

## Layout Contract

### Two-Column Page Shell

```
┌─────────────────────────────────────────────────────────┐
│  [Nav chrome — unchanged, AppRouter.tsx wraps all pages] │
├─────────────────────────────────────────────────────────┤
│  [MissingFlagBanner — conditional, full width]          │
├────────────────────────────────┬────────────────────────┤
│  Left column (flex-1)          │  Right column (lg:w-80) │
│  ChatPlaceholder               │  MealPlanColumn         │
│  (empty state in Phase 4)      │  (empty state in Ph 4)  │
└────────────────────────────────┴────────────────────────┘
```

**Tailwind class contract:**

- Page wrapper: `min-h-screen bg-white dark:bg-gray-800 transition-colors duration-200`
- Inner container: `max-w-7xl mx-auto py-6 sm:px-6 lg:px-8` (matches `AppRouter.tsx` `<main>` wrapper)
- Two-column flex: `flex flex-col lg:flex-row gap-6`
- Left column: `flex-1 min-w-0`
- Right column: `lg:w-80 flex-shrink-0` (320px fixed)
- Mobile: columns stack vertically (`flex-col`), right column appears below left column

**Laptop projector** (`lg:` breakpoint = 1024px) is the dominant target per CONTEXT.md. Mobile responsive is required but secondary.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Nav link label (desktop + mobile) | `Generative UI` |
| Page title (`h1`) | `Generative UI` |
| Page tagline (below h1) | `On-device recipes, generated and picked right in the chat — no server, no network.` |
| Chat placeholder heading | `Chat coming in a future phase` |
| Chat placeholder body | `Ask for a recipe here and an interactive card carousel will appear — powered by Chrome's built-in AI, all on-device.` |
| Meal-plan empty state heading | `Your meal plan is empty` |
| Meal-plan empty state body | `Recipes you pick from the chat will appear here. Come back once chat is wired up.` |
| `MissingFlagBanner` heading | `WebMCP isn't enabled in this browser.` (unchanged from v1.0) |
| `MissingFlagBanner` body | `The recipe browser still works. To register tools and run the agent demo, open this page in Chrome 146+ Canary with the WebMCP flag enabled.` (unchanged) |
| IDB seed log message | `seedIfMissing: inserted {N} new recipes` (console.log only, not visible in UI) |

No destructive UI is rendered in Phase 4 (meal plan is always empty; no remove/clear buttons). Destructive copy is deferred to Phase 6 when `removeFromPlan` becomes invokable.

Error states visible in Phase 4:

| Element | Copy |
|---------|------|
| IDB open failure | `Failed to load meal plan. Reload the page to try again.` (rendered inline in `MealPlanColumn` if `getPlan()` rejects) |
| Recipe seed failure | `Failed to seed recipe library. Reload to retry.` (console.error; not shown in UI — seed failure is non-blocking in Phase 4) |

---

## Interaction Contract

### Nav Link

- **Desktop:** `<Link to="/generative-ui">` inserted immediately after the `/webmcp` `<Link>` in `AppRouter.tsx` desktop nav block (lines ~70–72). Class: `text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-white px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200`. Includes `onClick={() => trackUserInteraction('navigation_click', 'generative_ui_link')}`.
- **Mobile:** Identical `<Link>` in mobile nav block (lines ~160–162). Class adds `block` prefix. Includes matching `trackUserInteraction` call.

### MissingFlagBanner

- Renders at top of `GenerativeUIPage` when `typeof navigator.modelContext === 'undefined'`.
- Does NOT unmount or hide when the user scrolls — it stays above the two-column layout.
- The rest of the page (column shell, empty states) remains fully visible and usable while banner is shown.
- No dismiss/close affordance — matches v1.0 behavior exactly.

### ChatPlaceholder

- Static, non-interactive. No buttons, no inputs.
- Renders the empty-state heading and body copy only.
- Visual treatment: `bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-6` — matches existing card style.
- Minimum height: `min-h-[400px]` on desktop to prevent the left column from collapsing before Phase 6 adds real content.

### MealPlanColumn

- Renders empty-state when `useMealPlan()` returns an empty array.
- Empty-state visual treatment: centered flex column, muted icon (inline SVG, gray-400), empty-state heading + body in `text-gray-500 dark:text-gray-400`.
- No interactive affordances in Phase 4 (no add/remove buttons — those ship in Phase 6).
- Subscribes to `MealPlanStore` via `useMealPlan()` hook — ready to re-render when Phase 6 writes entries, no refactor needed.
- Loading state: while IDB `getPlan()` is in-flight, render a single line `text-gray-500 dark:text-gray-400 text-sm` reading `Loading plan…` — matches the RecipeWorkbench loading copy pattern.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No third-party component registries are used in this phase. All components are hand-rolled Tailwind TSX matching existing project patterns.

---

## Source Traceability

| Decision | Source |
|----------|--------|
| Two-column `flex-col lg:flex-row` layout | CONTEXT.md — Page Shell Layout section |
| Right column 320–360px | CONTEXT.md — Page Shell Layout section |
| `MissingFlagBanner` reused as-is | CONTEXT.md — Reusable Assets + Phase context |
| `GenerativeUI/` component subdir | CONTEXT.md — overrides earlier `MCPApps/` proposal |
| Nav label "Generative UI" | CONTEXT.md — phase_context block |
| `trackUserInteraction` on nav click | Code scan — AppRouter.tsx nav link pattern |
| `text-3xl font-bold` page title | Code scan — RecipeWorkbenchPage.tsx:301 |
| `text-gray-600 dark:text-gray-400` tagline | Code scan — RecipeWorkbenchPage.tsx:302 |
| `bg-white dark:bg-gray-800` surface | Code scan — HomePage.tsx + RecipeWorkbenchPage.tsx |
| `primary-500` accent only on header icon | Code scan — RecipeWorkbenchPage.tsx:295, HomePage.tsx:20 |
| `max-w-7xl mx-auto` container | Code scan — AppRouter.tsx:43,204 |
| No shadcn | Code scan — no components.json in repo root |
| Tagline copy | Claude's Discretion (CONTEXT.md) — no tagline specified in REQUIREMENTS or CONTEXT |
| Chat placeholder copy | Claude's Discretion (CONTEXT.md) — exact placeholder copy delegated to researcher |
| Meal-plan empty state copy | Claude's Discretion (CONTEXT.md) — exact treatment delegated to researcher |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
