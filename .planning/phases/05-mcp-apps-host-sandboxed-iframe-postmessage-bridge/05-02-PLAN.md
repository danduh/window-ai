---
phase: 05-mcp-apps-host-sandboxed-iframe-postmessage-bridge
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - chat/src/app/components/GenerativeUI/ChatBubbleContainer.tsx
  - chat/src/app/components/GenerativeUI/UIResourceFrame.tsx
  - chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx
  - chat/src/app/components/GenerativeUIPage.tsx
autonomous: false
requirements:
  - GENUI-06
  - GENUI-07
  - GENUI-08
tags: [mcp-apps, react, iframe, sandbox, postmessage, sep-1865, webmcp, brownfield, tailwind, dark-mode]
must_haves:
  truths:
    - "A user on /generative-ui sees a 'Show demo carousel' primary-600 button beneath the existing ChatPlaceholder empty-state copy"
    - "Clicking 'Show demo carousel' mounts a ChatBubbleContainer below the button, the button disables, and within ~200ms the iframe handshake completes and the recipe-card carousel renders in the locked UI-SPEC inline CSS subset"
    - "Each recipe card in the iframe has a 'Pick' button; clicking it sends a JSON-RPC tools/call over postMessage and the right-column MealPlanColumn re-renders within one animation frame with a new entry"
    - "Toggling dark mode while the iframe is mounted re-themes the iframe content (cards switch to dark palette) within one animation frame, with no flash-of-wrong-theme on initial mount"
    - "If the iframe handshake does not complete within 1000ms, an inline red-bordered error card appears with the locked copy 'Couldn't load app' and a 'Try again' button; clicking 'Try again' destroys and re-mounts the iframe"
    - "When navigator.modelContext is undefined the existing MissingFlagBanner still renders at the top of the page, the 'Show demo carousel' button still mounts the carousel, but clicking Pick inside the iframe shows 'Tools unavailable' on the button (graceful degradation)"
    - "GenerativeUIPage registers genUITools on mount and aborts the controller on unmount — verified by a mount-effect that calls registerGenUITools() and returns ctrl?.abort() as cleanup"
    - "Only one UIResourceFrame is mounted at a time (single-slot policy via the Show-demo-carousel button's disabled state while showFrame is truthy)"
    - "Lint passes, typecheck passes, no edits anywhere outside the four files in files_modified, no edits under chat/src/app/components/RecipeWorkbench/, mcp/, mcp-client/, devops/awsweb/"
  artifacts:
    - path: "chat/src/app/components/GenerativeUI/ChatBubbleContainer.tsx"
      provides: "Presentational wrapper card hosting the UIResourceFrame"
      exports: ["ChatBubbleContainer"]
      min_lines: 8
    - path: "chat/src/app/components/GenerativeUI/UIResourceFrame.tsx"
      provides: "React component owning outer iframe DOM, bridge lifecycle, loading/error/ready state machine, theme propagation, height sync"
      exports: ["UIResourceFrame"]
      min_lines: 80
    - path: "chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx"
      provides: "Empty-state + 'Show demo carousel' trigger + conditional UIResourceFrame slot"
      exports: ["ChatPlaceholder"]
    - path: "chat/src/app/components/GenerativeUIPage.tsx"
      provides: "Existing page extended with mount-time registerGenUITools() effect"
      contains: "registerGenUITools"
  key_links:
    - from: "chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx"
      to: "chat/src/app/services/RecipePersistence.ts"
      via: "getRecipes() call inside searchRecipesLocal handler"
      pattern: "getRecipes\\("
    - from: "chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx"
      to: "chat/src/app/components/GenerativeUI/UIResourceFrame.tsx"
      via: "JSX render of UIResourceFrame inside ChatBubbleContainer when showFrame is true"
      pattern: "<UIResourceFrame"
    - from: "chat/src/app/components/GenerativeUI/UIResourceFrame.tsx"
      to: "chat/src/app/components/GenerativeUI/iframe/bridge.ts"
      via: "createUIResourceBridge factory call inside a useRef setup"
      pattern: "createUIResourceBridge"
    - from: "chat/src/app/components/GenerativeUI/UIResourceFrame.tsx"
      to: "chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts"
      via: "renderCarouselHTML(recipes) provides the srcdoc value (passed in via prop or generated inside on mount)"
      pattern: "renderCarouselHTML"
    - from: "chat/src/app/components/GenerativeUI/UIResourceFrame.tsx"
      to: "chat/src/app/context/ThemeContext.tsx"
      via: "useTheme() hook + useEffect sending host-context-changed on theme change"
      pattern: "useTheme\\(\\)"
    - from: "chat/src/app/components/GenerativeUIPage.tsx"
      to: "chat/src/app/services/genUITools.ts"
      via: "registerGenUITools() invoked inside a mount-time useEffect, cleanup aborts the controller"
      pattern: "registerGenUITools"
---

<objective>
Wire the Plan 05-01 primitives into a working React surface on `/generative-ui`. Three new visible artifacts:
1. A `ChatBubbleContainer` presentational wrapper (Tailwind card matching UI-SPEC).
2. A `UIResourceFrame` React component that owns the outer iframe DOM, instantiates the bridge from Plan 05-01, manages the `mounting | ready | error` state machine, propagates theme changes, and resizes on `ui/notifications/size-changed`.
3. A modified `ChatPlaceholder` that adds the "Show demo carousel" trigger button and conditionally renders the iframe stack.

Plus one modification to `GenerativeUIPage.tsx`: a mount-time `useEffect` that calls `registerGenUITools()` and aborts on unmount.

After this plan, the Phase 5 success criteria are all browser-observable through the developer trigger button (no chat panel yet — that is Phase 6).

Purpose: Collapse all browser-observable Phase 5 behavior into a single plan so the user verification step at the end exercises the full handshake → carousel → Pick → MealPlanColumn round-trip in one session.

Output: The four files listed in `files_modified`, plus a passing TypeScript build, passing lint, and a browser smoke check approved by the user via a `checkpoint:human-verify` task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-CONTEXT.md
@.planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-RESEARCH.md
@.planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-UI-SPEC.md
@.planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-01-SUMMARY.md
@.planning/phases/04-v1-1-foundation-page-shell-store-seed/04-02-SUMMARY.md
@chat/src/app/context/ThemeContext.tsx
@chat/src/app/components/GenerativeUIPage.tsx
@chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx
@chat/src/app/components/GenerativeUI/MealPlanColumn.tsx
@chat/src/app/services/RecipePersistence.ts
@chat/src/app/services/MealPlanStore.ts
@chat/src/app/components/RecipeWorkbench/MissingFlagBanner.tsx

<interfaces>
<!-- Contracts produced by Plan 05-01 — import directly, no further exploration needed. -->

From chat/src/app/components/GenerativeUI/iframe/bridge.ts:
- export function createUIResourceBridge(opts: {
    outerIframe: HTMLIFrameElement;
    tools: ModelContextTool[];
    hostInfo: HostInfo;
    initialHostContext: HostContext;
    handshakeTimeoutMs?: number;
    requestTimeoutMs?: number;
    onHandshakeComplete?: () => void;
    onHandshakeTimeout?: () => void;
    onSizeChanged?: (height: number) => void;
    onLog?: (tag: 'host'|'iframe'|'relay', msg: string, data?: unknown) => void;
  }): {
    attachLoadListener(): void;
    handleMessage(event: MessageEvent): void;
    sendHostContextChanged(params: HostContextChangedParams): void;
    destroy(): void;
  };
- export interface HostInfo { name: string; version: string; mcpVersion: string }
- export interface HostContext { theme: 'dark'|'light'; displayMode: 'inline'; dimensions: { maxWidth: number; maxHeight: number } }
- export interface HostContextChangedParams { theme: 'dark'|'light'; displayMode?: 'inline'; containerDimensions?: { width: number; maxHeight: number } }
- export const RPC_ERROR: { PARSE_ERROR: -32700; INVALID_REQUEST: -32600; METHOD_NOT_FOUND: -32601; INVALID_PARAMS: -32602; INTERNAL_ERROR: -32603; TOOL_ERROR: -32000; DENIED: -32000 }

From chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts:
- export function renderCarouselHTML(recipes: Recipe[]): string

From chat/src/app/services/genUITools.ts:
- export const GEN_UI_TOOLS: ModelContextTool[]   // contains commitRecipeToPlan
- export function registerGenUITools(): AbortController | null

From chat/src/app/context/ThemeContext.tsx:
- export function useTheme(): { theme: 'light'|'dark'; toggleTheme: () => void }

From chat/src/app/services/RecipePersistence.ts:
- export function getRecipes(): Promise<Recipe[]>

From chat/src/app/components/GenerativeUI/MealPlanColumn.tsx (Phase 4 — unchanged):
- Already subscribes to MealPlanStore via useMealPlan(). It re-renders automatically when commitRecipeToPlan handler calls addToPlan.

From chat/src/app/components/RecipeWorkbench/MissingFlagBanner.tsx:
- The page-level banner already renders in GenerativeUIPage.tsx when !navigator.modelContext. The carousel itself handles tools-unavailable inside the iframe via the bridge's tools/call error response (no host-side UI change needed for that case).
</interfaces>

<host_constants>
<!-- Locked literal values for createUIResourceBridge options. -->
hostInfo = { name: 'window-ai-generative-ui', version: '1.1.0', mcpVersion: '2026-01-26' }

initialHostContext = {
  theme: theme,                       // from useTheme()
  displayMode: 'inline',
  dimensions: { maxWidth: 672, maxHeight: 800 }   // max-w-2xl = 672px; maxHeight is a soft cap
}

handshakeTimeoutMs = 1000     // per CONTEXT.md locked decision
requestTimeoutMs = 5000        // per CONTEXT.md locked default
initial iframe height = 320px  // per CONTEXT.md locked default
</host_constants>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ChatBubbleContainer.tsx — presentational wrapper</name>
  <files>chat/src/app/components/GenerativeUI/ChatBubbleContainer.tsx</files>
  <read_first>
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-UI-SPEC.md (Component Inventory, Layout Contract — Tailwind class contract for ChatBubbleContainer)
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-RESEARCH.md (Code Examples — ChatBubbleContainer.tsx complete 12 lines)
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-CONTEXT.md (Chat-bubble container section — locked Tailwind classes)
    - chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx (Phase 4 — for stylistic consistency reference)
    - chat/tailwind.config.js (confirms animate-fade-in custom animation exists)
  </read_first>
  <action>
    Create a tiny presentational component. Pure JSX, no state, no effects, no props beyond `children`.

    Named export: `ChatBubbleContainer`. Signature: `React.FC<{ children: React.ReactNode }>`.

    Single returned element: a `<div>` with the LOCKED Tailwind class contract from UI-SPEC Layout Contract:
    `rounded-2xl max-w-2xl bg-white dark:bg-zinc-800 border border-gray-200 dark:border-zinc-700 shadow-sm p-2 mt-4 animate-fade-in`

    No prop drilling, no `className` merge, no clsx. Children render directly inside. The `animate-fade-in` class is a custom Tailwind animation defined in `chat/tailwind.config.js` (per RESEARCH Assumption A3 / UI-SPEC source traceability — already verified to exist).

    Imports: only `import React from 'react'`.

    File should be under 15 lines including imports.
  </action>
  <verify>
    <automated>npx nx typecheck chat 2>&amp;1 | tail -10 &amp;&amp; npx nx lint chat 2>&amp;1 | tail -10 &amp;&amp; grep -cE "^export const ChatBubbleContainer" chat/src/app/components/GenerativeUI/ChatBubbleContainer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at chat/src/app/components/GenerativeUI/ChatBubbleContainer.tsx.
    - `grep -cE "^export const ChatBubbleContainer" chat/src/app/components/GenerativeUI/ChatBubbleContainer.tsx` returns 1.
    - The Tailwind class contract appears verbatim in a single `className` literal: `grep -F "rounded-2xl max-w-2xl bg-white dark:bg-zinc-800 border border-gray-200 dark:border-zinc-700 shadow-sm p-2 mt-4 animate-fade-in" chat/src/app/components/GenerativeUI/ChatBubbleContainer.tsx | grep -c .` returns at least 1.
    - `wc -l chat/src/app/components/GenerativeUI/ChatBubbleContainer.tsx` returns ≤ 15.
    - `npx nx typecheck chat` exits 0.
    - `npx nx lint chat` exits 0.
  </acceptance_criteria>
  <done>
    Minimal presentational wrapper; ChatPlaceholder.tsx (Task 3) imports and renders it around UIResourceFrame.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: UIResourceFrame.tsx — React component owning iframe + bridge lifecycle</name>
  <files>chat/src/app/components/GenerativeUI/UIResourceFrame.tsx</files>
  <read_first>
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-CONTEXT.md (Sandbox + iframe rendering strategy, JSON-RPC bridge protocol, Theme propagation sections — all locked)
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-RESEARCH.md (Pattern 4 UIResourceFrame state machine, Pattern 8 Theme propagation wiring, Pattern 9 Pre-load outbound queue, Pitfall 4 ThemeContext re-renders, Pitfall 5 navigator.modelContext unavailable, Pitfall 6 Multiple frames mounted)
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-UI-SPEC.md (UIResourceFrame iframe lifecycle states, loading skeleton, error card, theme propagation, height sync sections)
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-01-SUMMARY.md (the bridge.ts exports — note exact factory option shape)
    - chat/src/app/components/GenerativeUI/iframe/bridge.ts (createUIResourceBridge factory signature — produced by Plan 05-01)
    - chat/src/app/context/ThemeContext.tsx (useTheme return shape)
    - chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts (renderCarouselHTML signature — produced by Plan 05-01)
    - chat/src/app/services/genUITools.ts (GEN_UI_TOOLS — produced by Plan 05-01)
  </read_first>
  <action>
    Create the React component that owns the outer iframe DOM element and orchestrates the bridge lifecycle.

    Named export: `UIResourceFrame: React.FC<UIResourceFrameProps>` with `interface UIResourceFrameProps { html: string; tools?: ModelContextTool[] }`. `html` is the result of `renderCarouselHTML(recipes)` from the caller. `tools` defaults to `GEN_UI_TOOLS` (per RESEARCH Open Question 3).

    Internal state and refs (per RESEARCH Pattern 4):
      - `frameState` useState typed `'mounting'|'ready'|'error'`, default `'mounting'`.
      - `iframeKey` useState number, default 0 — incremented on retry to force iframe DOM destruction.
      - `iframeHeight` useState number, default 320 (per CONTEXT.md locked default).
      - `outerIframeRef` useRef typed `HTMLIFrameElement | null`.
      - `bridgeRef` useRef typed `ReturnType<typeof createUIResourceBridge> | null`, default null.
      - `theme` destructured from `useTheme()`.

    Layout — outer wrapper div with `className="w-full overflow-hidden rounded-xl"` (per UI-SPEC Layout Contract UIResourceFrame outer div). Inside:

    Render the `<iframe>` element ALWAYS (so the ref is stable and the bridge can attach the load listener), AND overlay the loading skeleton or error card on top based on `frameState`. Use the "iframe always rendered but hidden when not ready" approach: `style={{ display: frameState === 'ready' ? 'block' : 'none', height: \`${iframeHeight}px\` }}`. Above the iframe (in render order) place the conditional overlay for mounting/error states.

    Iframe element attributes:
      - `key={iframeKey}` (retry-driven remount per Pattern 4)
      - `ref={outerIframeRef}`
      - `sandbox="allow-scripts allow-same-origin"` (OUTER iframe — per locked CONTEXT.md decision; the INNER iframe inside the srcdoc has only `allow-scripts` per Plan 05-01's iframeBridgeScript.ts emission)
      - `srcDoc={html}` (React prop name maps to srcdoc HTML attribute)
      - `title="Recipe carousel"` (a11y label)
      - `className="w-full border-0 block"`
      - `style={{ display: ..., height: \`${iframeHeight}px\` }}`

    Loading skeleton (rendered when `frameState === 'mounting'`) — VERBATIM from UI-SPEC loading state:
      `<div aria-label="Loading recipe carousel…" className="h-[200px] rounded-xl bg-gray-200 dark:bg-gray-700 animate-pulse" />`

    Error card (rendered when `frameState === 'error'`) — VERBATIM from UI-SPEC error state:
      `<div className="rounded-xl border border-red-300 dark:border-red-700 bg-red-50 dark:bg-red-900/20 p-4 flex flex-col gap-3">`
      Inside: a `<p className="text-sm font-semibold text-red-700 dark:text-red-400">` with the exact text `Couldn't load app`, a `<p className="text-sm text-red-600 dark:text-red-300">` with the exact text `The recipe carousel didn't respond in time. This can happen if WebMCP isn't enabled in your browser.`, and a `<button>` with the exact text `Try again` and className `self-start px-3 py-1.5 text-sm font-semibold rounded-md bg-red-600 hover:bg-red-700 text-white transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2`. Its onClick handler is `handleRetry`.

    `handleRetry = () => { setFrameState('mounting'); setIframeKey(k => k + 1); };` — incrementing `iframeKey` forces React to destroy and recreate the `<iframe>` DOM element, triggering a fresh `load` event and fresh bridge instantiation in the primary effect below.

    PRIMARY EFFECT — bridge instantiation + window message listener + cleanup. Deps `[iframeKey, html]` only (theme is intentionally excluded — see THEME EFFECT). Body:
      1. Read `outerIframeRef.current`; early-return if null.
      2. Call `createUIResourceBridge` with options assembled from `<host_constants>` (hostInfo `{ name: 'window-ai-generative-ui', version: '1.1.0', mcpVersion: '2026-01-26' }`, initialHostContext with current `theme`, displayMode `'inline'`, dimensions `{ maxWidth: 672, maxHeight: 800 }`, handshakeTimeoutMs 1000, requestTimeoutMs 5000), passing `tools ?? GEN_UI_TOOLS` for the tools/call proxy.
      3. Pass callbacks: `onHandshakeComplete: () => setFrameState('ready')`, `onHandshakeTimeout: () => setFrameState('error')`, `onSizeChanged: (h) => setIframeHeight(h)`, `onLog: (tag, msg, data) => console.debug(\`[mcp-apps:${tag}]\`, msg, data ?? '')`.
      4. Assign result to `bridgeRef.current`. Call `bridge.attachLoadListener()`.
      5. Add `window.addEventListener('message', onMessage)` where `onMessage = (e) => bridge.handleMessage(e)`.
      6. Cleanup: `removeEventListener('message', onMessage)`, `bridge.destroy()`, `bridgeRef.current = null`.

    THEME EFFECT — separate useEffect with deps `[theme]`. Body: read `bridgeRef.current`; if null, return. Else call `bridge.sendHostContextChanged({ theme, displayMode: 'inline' })`. Per RESEARCH Pitfall 4, the bridge ref is accessed via `bridgeRef.current` inside the effect (NOT as a dep). The bridge's pre-load outbound queue (Plan 05-01) handles the case where this effect fires before the iframe has loaded.

    Note: initial theme is sent twice (once via initialHostContext baked into the ui/initialize response inside the bridge, once via this THEME EFFECT firing on initial mount). Idempotent — the iframe's data-theme attribute re-applies the same value. No flash-of-wrong-theme because the bridge's pre-load queue ensures the host-context-changed notification arrives before the iframe paints.

    Pitfall 5 note: when navigator.modelContext is undefined, registerGenUITools() (called in GenerativeUIPage.tsx) returns null and skips registration — but the iframe's tools/call proxy goes through the bridge which holds a direct reference to GEN_UI_TOOLS (passed via the `tools` option). So tool.execute still runs even without modelContext. The "Tools unavailable" copy applies only when the bridge's tools/call lookup fails (unknown tool name returns METHOD_NOT_FOUND). For Phase 5 the stub commitRecipeToPlan tool is always present so this branch is effectively unreachable; it remains the documented fallback per the locked spec.

    Strict TS: every prop, state, and ref typed. No `: any`. No `as any` at API surfaces.

    Imports:
      - `import React, { useEffect, useRef, useState } from 'react';`
      - `import { useTheme } from '../../context/ThemeContext';`
      - `import { createUIResourceBridge } from './iframe/bridge';`
      - `import { GEN_UI_TOOLS } from '../../services/genUITools';`

    Do NOT import `renderCarouselHTML` or `RecipePersistence` here — the caller (ChatPlaceholder) generates the HTML and passes it via the `html` prop. This keeps UIResourceFrame reusable for Phase 6 where the chat tool result provides the HTML.
  </action>
  <verify>
    <automated>npx nx typecheck chat 2>&amp;1 | tail -10 &amp;&amp; npx nx lint chat 2>&amp;1 | tail -10 &amp;&amp; grep -cE "^export const UIResourceFrame" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at chat/src/app/components/GenerativeUI/UIResourceFrame.tsx.
    - `grep -cE "^export const UIResourceFrame" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx` returns 1.
    - `grep -F "createUIResourceBridge" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 1 (factory call).
    - `grep -F "useTheme" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 2 (import + call).
    - `grep -F "sandbox=\"allow-scripts allow-same-origin\"" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 1 (outer iframe sandbox attribute — both flags per locked decision).
    - `grep -F "srcDoc" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 1.
    - `grep -F "handshakeTimeoutMs: 1000" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 1.
    - `grep -F "2026-01-26" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 1.
    - `grep -F "window-ai-generative-ui" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 1.
    - `grep -F "Couldn't load app" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 1.
    - `grep -F "didn't respond in time" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 1.
    - `grep -F "Try again" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 1.
    - `grep -F "Loading recipe carousel" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 1.
    - `grep -F "animate-pulse" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 1.
    - `grep -F "iframeKey" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx | grep -c .` returns at least 3 (state + setter + key= attribute).
    - `grep -cE ": any\b" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx` returns 0.
    - `grep -cE "as any\b" chat/src/app/components/GenerativeUI/UIResourceFrame.tsx` returns 0.
    - `wc -l chat/src/app/components/GenerativeUI/UIResourceFrame.tsx` returns ≥ 80.
    - `npx nx typecheck chat` exits 0.
    - `npx nx lint chat` exits 0.
  </acceptance_criteria>
  <done>
    UIResourceFrame instantiates the bridge per Plan 05-01's createUIResourceBridge, manages mounting/ready/error states, propagates theme changes via the THEME EFFECT, and resizes the outer iframe via setIframeHeight from onSizeChanged. Retry remounts the iframe via iframeKey bump.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ChatPlaceholder.tsx — Show demo carousel trigger + UIResourceFrame slot</name>
  <files>chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx</files>
  <read_first>
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-CONTEXT.md (Debug trigger UI section — locked button copy, behavior, single-slot policy)
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-UI-SPEC.md ("Show demo carousel" button section — locked Tailwind class contract, font-semibold, disabled state, focus ring; ChatBubbleContainer interaction; ChatPlaceholder augmented layout)
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-RESEARCH.md (Code Examples — "Show demo carousel" button; Open Question 2 — searchRecipesLocal returns all 12 in Phase 5; Pitfall 6 Multiple UIResourceFrames mounted)
    - chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx (Phase 4 — existing empty-state to extend ADDITIVELY)
    - chat/src/app/services/RecipePersistence.ts (getRecipes signature)
    - chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts (renderCarouselHTML signature)
    - chat/src/app/components/GenerativeUI/ChatBubbleContainer.tsx (Task 1 output)
    - chat/src/app/components/GenerativeUI/UIResourceFrame.tsx (Task 2 output)
  </read_first>
  <action>
    EXTEND the existing ChatPlaceholder.tsx ADDITIVELY (do NOT replace the empty-state). The Phase 4 file is currently a stateless functional component returning a single card with SVG + heading + body copy. After this task it becomes stateful but every existing visible piece of content remains unchanged.

    Add state:
      - `showFrame` useState boolean, default false.
      - `carouselHTML` useState typed `string | null`, default null.

    Add async handler inside the component: `handleShowCarousel`. Steps: call `getRecipes()` (page-side helper, NOT via `navigator.modelContext` per CONTEXT.md scope boundary), pass the result to `renderCarouselHTML`, store via `setCarouselHTML`, set `setShowFrame(true)`. Wrap in try/catch with the Phase 4 error narrowing convention (`err instanceof Error ? err.message : 'Unknown error'`) and log via `console.error('[GenUI] Failed to render carousel:', message)`. Per RESEARCH Open Question 2, getRecipes returns ALL 12 recipes; no filtering in Phase 5 (Phase 6's searchRecipes tool replaces this).

    Layout changes (ADDITIVE only):
      - The existing card with `min-h-[400px] flex flex-col items-center justify-center text-center` keeps every existing element (SVG + heading + body paragraph).
      - INSIDE the existing card, AFTER the existing `<p>` body, append the "Show demo carousel" button.
      - OUTSIDE the existing card (as a sibling element), conditionally render `{showFrame && carouselHTML && (<ChatBubbleContainer><UIResourceFrame html={carouselHTML} /></ChatBubbleContainer>)}`. ChatBubbleContainer's own `mt-4` class provides the spacing.
      - Wrap the two top-level elements (the existing card + the conditional ChatBubbleContainer) in a React fragment so the component still returns a single root.

    Button — VERBATIM Tailwind class contract from UI-SPEC "Show demo carousel" button section: onClick={handleShowCarousel}, disabled={showFrame}, className="mt-6 inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-primary-600 hover:bg-primary-700 text-white text-sm font-semibold transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 dark:focus:ring-offset-gray-800 disabled:opacity-50 disabled:cursor-not-allowed", button text `Show demo carousel`.

    Single-slot policy per Pitfall 6: `disabled={showFrame}` prevents re-click while the frame is mounted. The disabled CSS opacity/cursor are part of the locked Tailwind class contract above. Phase 5 does not implement a "close" button — once shown, the carousel stays until the user navigates away or hits the UIResourceFrame error retry.

    Imports to add to the existing file (preserve the existing `import React from 'react'`):
      - `import { useState } from 'react';` (or change existing to `import React, { useState } from 'react';`)
      - `import { getRecipes } from '../../services/RecipePersistence';`
      - `import { renderCarouselHTML } from './iframe/carouselTemplate';`
      - `import { ChatBubbleContainer } from './ChatBubbleContainer';`
      - `import { UIResourceFrame } from './UIResourceFrame';`

    Preserve the existing visible copy verbatim per UI-SPEC ("Phase 4 copy is inherited unchanged"):
      - Heading: `Chat coming in a future phase`
      - Body: `Ask for a recipe here and an interactive card carousel will appear — powered by Chrome's built-in AI, all on-device.`

    Strict TS, no `: any`. The error catch uses `err instanceof Error` narrowing per the Phase 4 convention.
  </action>
  <verify>
    <automated>npx nx typecheck chat 2>&amp;1 | tail -10 &amp;&amp; npx nx lint chat 2>&amp;1 | tail -10 &amp;&amp; grep -F "Show demo carousel" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -c . &amp;&amp; grep -F "Chat coming in a future phase" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -c .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Chat coming in a future phase" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -c .` returns 1 (Phase 4 heading preserved).
    - `grep -F "Ask for a recipe here and an interactive card carousel will appear" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -c .` returns 1 (Phase 4 body copy preserved).
    - `grep -F "Show demo carousel" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -c .` returns at least 1 (button label per UI-SPEC).
    - `grep -F "bg-primary-600 hover:bg-primary-700" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -c .` returns at least 1.
    - `grep -F "font-semibold" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -c .` returns at least 1.
    - `grep -F "disabled={showFrame}" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -c .` returns at least 1 (single-slot policy).
    - `grep -F "renderCarouselHTML" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -c .` returns at least 2 (import + call).
    - `grep -F "getRecipes" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -c .` returns at least 2 (import + call).
    - `grep -F "ChatBubbleContainer" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -c .` returns at least 2 (import + JSX usage).
    - `grep -F "UIResourceFrame" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -c .` returns at least 2 (import + JSX usage).
    - `grep -F "navigator.modelContext" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx | grep -v '^[[:space:]]*//' | grep -c .` returns 0 (this component must NOT call navigator.modelContext — searchRecipesLocal is page-side per locked CONTEXT.md scope boundary).
    - `grep -cE ": any\b" chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx` returns 0.
    - `npx nx typecheck chat` exits 0.
    - `npx nx lint chat` exits 0.
  </acceptance_criteria>
  <done>
    ChatPlaceholder retains its Phase 4 empty-state, adds a primary-600 "Show demo carousel" button beneath the body copy, and on click loads getRecipes() → renderCarouselHTML → ChatBubbleContainer wrapping UIResourceFrame. Button disables while frame mounted (single-slot per Pitfall 6).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: GenerativeUIPage.tsx — mount-time registerGenUITools effect</name>
  <files>chat/src/app/components/GenerativeUIPage.tsx</files>
  <read_first>
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-CONTEXT.md (Module/file layout section — "Update chat/src/app/components/GenerativeUIPage.tsx — call registerGenUITools() on mount, abort on unmount (parallel to seedIfMissing effect)")
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-RESEARCH.md (Pattern 1 navigator.modelContext.registerTool — focus on the cleanup contract)
    - chat/src/app/components/GenerativeUIPage.tsx (current Phase 4 state — has seedIfMissing effect; this task adds a parallel effect)
    - chat/src/app/services/genUITools.ts (registerGenUITools signature — produced by Plan 05-01)
  </read_first>
  <action>
    Add ONE new useEffect to the existing GenerativeUIPage.tsx, parallel to the existing seedIfMissing effect. ADDITIVE ONLY — preserve every other line of Phase 4 work.

    Add import at the top of the file, after the existing imports: `import { registerGenUITools } from '../services/genUITools';`

    Add the new effect AFTER the existing `useEffect(() => { ... seedIfMissing ... }, []);` block (and after its closing semicolon). Body:
      - Call `registerGenUITools()`; store the result in a const `ctrl`.
      - Return a cleanup function that calls `ctrl?.abort()`.
      - Empty deps array `[]`.

    That is the entire new code path for this task. Do not refactor the existing `!navigator.modelContext && <MissingFlagBanner />` branch — the MissingFlagBanner still correctly surfaces the unavailable state, and registerGenUITools is itself defensive (returns null if navigator.modelContext is undefined per Plan 05-01 Task 3).

    Do NOT touch:
      - The seedIfMissing effect.
      - The useSEOData call.
      - The two-column flex shell.
      - The MissingFlagBanner conditional.
      - The ChatPlaceholder or MealPlanColumn JSX.

    Strict TS, no `: any`.
  </action>
  <verify>
    <automated>npx nx typecheck chat 2>&amp;1 | tail -10 &amp;&amp; npx nx lint chat 2>&amp;1 | tail -10 &amp;&amp; grep -F "registerGenUITools" chat/src/app/components/GenerativeUIPage.tsx | grep -c . &amp;&amp; grep -F "seedIfMissing" chat/src/app/components/GenerativeUIPage.tsx | grep -c .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "registerGenUITools" chat/src/app/components/GenerativeUIPage.tsx | grep -c .` returns at least 2 (import + call).
    - `grep -F "seedIfMissing" chat/src/app/components/GenerativeUIPage.tsx | grep -c .` returns at least 2 (the Phase 4 effect is preserved — import + call).
    - `grep -F "ctrl?.abort()" chat/src/app/components/GenerativeUIPage.tsx | grep -c .` returns at least 1 (cleanup function).
    - `grep -cE "useEffect\(" chat/src/app/components/GenerativeUIPage.tsx` returns at least 2 (seedIfMissing effect + new registerGenUITools effect).
    - `grep -F "<MissingFlagBanner />" chat/src/app/components/GenerativeUIPage.tsx | grep -c .` returns at least 1 (Phase 4 banner preserved).
    - `grep -F "<ChatPlaceholder />" chat/src/app/components/GenerativeUIPage.tsx | grep -c .` returns at least 1 (Phase 4 layout preserved).
    - `grep -F "<MealPlanColumn />" chat/src/app/components/GenerativeUIPage.tsx | grep -c .` returns at least 1 (Phase 4 layout preserved).
    - `grep -cE ": any\b" chat/src/app/components/GenerativeUIPage.tsx` returns 0.
    - `npx nx typecheck chat` exits 0.
    - `npx nx lint chat` exits 0.
  </acceptance_criteria>
  <done>
    GenerativeUIPage.tsx has two mount-time effects (seedIfMissing from Phase 4 + new registerGenUITools), the rest of the file is untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Browser smoke check — full Phase 5 round-trip</name>
  <files>chat/src/app/components/GenerativeUIPage.tsx, chat/src/app/components/GenerativeUI/UIResourceFrame.tsx, chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx, chat/src/app/components/GenerativeUI/ChatBubbleContainer.tsx</files>
  <action>Human-verification checkpoint. See <how-to-verify> below for the full sequence. The executor MUST start the dev server, perform every numbered step under <how-to-verify>, and only proceed past this task when the user types `approved` per <resume-signal>.</action>
  <what-built>
    - ChatBubbleContainer (Tailwind card wrapper) — Task 1
    - UIResourceFrame (React component owning iframe + bridge) — Task 2
    - ChatPlaceholder ("Show demo carousel" trigger + conditional UIResourceFrame slot) — Task 3
    - GenerativeUIPage (parallel registerGenUITools effect) — Task 4

    Outcome: on `/generative-ui`, the user can click "Show demo carousel", see a sandboxed iframe handshake within 1s, see the recipe-card carousel render, toggle dark mode and see the iframe re-theme, click "Pick" on any card, and watch the MealPlanColumn on the right update.
  </what-built>
  <how-to-verify>
    Prerequisites: Chrome 146+ Canary with `chrome://flags/#WebMCP for testing` enabled. The MissingFlagBanner SHOULD NOT appear if flag is on.

    1. Start dev server: `npx nx serve chat`. Open Chrome Canary to `http://localhost:4300/generative-ui`. Open DevTools Console; in the Console filter input type `[mcp-apps:` to scope logs.

    2. Verify page chrome (Phase 4 baseline preserved):
       - Header "Generative UI" with gradient icon renders.
       - Two-column layout (chat placeholder on left, "Your meal plan is empty" or existing entries on the right).
       - Empty-state copy in left card unchanged: heading "Chat coming in a future phase", body "Ask for a recipe here and an interactive card carousel will appear — powered by Chrome's built-in AI, all on-device."
       - The MissingFlagBanner is NOT visible (flag is on).

    3. Verify the new trigger button:
       - Beneath the body copy paragraph, a primary-600 button labeled `Show demo carousel` is visible with `font-semibold` weight.
       - Toggle dark mode (existing site-wide toggle). Button styling adapts (still primary-600).

    4. Click `Show demo carousel`:
       - Console logs `[mcp-apps:host] handshake complete` (and intermediate breadcrumbs `[mcp-apps:host] ui/initialize`, `[mcp-apps:host] host-context-changed sent`).
       - Within ~200ms (≤ 1000ms hard cap) a ChatBubbleContainer (rounded white card) appears below the chat placeholder card.
       - Inside the bubble: a horizontal-scroll carousel of recipe cards (3 visible at any time on desktop). Each card shows recipe title, totalMinutes badge, top 1–3 ingredient names, and a `Pick` button.
       - The button labeled `Show demo carousel` is now disabled (opacity 50%, cursor not-allowed).

    5. Toggle dark mode while the iframe is mounted:
       - Console logs `[mcp-apps:host] host-context-changed sent`.
       - Within one animation frame, the iframe cards switch to the dark palette (background `#1f2937` / `#374151`, text `#f9fafb`). No flash.
       - Toggle back to light; cards return to light palette.

    6. Click `Pick` on the first card:
       - Console logs `[mcp-apps:iframe] tools/call sent { name: 'commitRecipeToPlan' }` followed by `[mcp-apps:host] tools/call handled`.
       - Within ~100ms the right-column MealPlanColumn re-renders with a new entry showing the recipeId and timestamp.
       - The card's `Pick` button briefly shows `Added!` (2s) then reverts to `Pick`.

    7. Click `Pick` on a different card; verify a second entry appears in MealPlanColumn.

    8. Reload the page: the MealPlanColumn entries SHOULD persist (IDB-backed via MealPlanStore from Phase 4).

    9. Brownfield boundary check (run from repo root):
       `git diff --stat main...HEAD -- chat/src/app/components/RecipeWorkbench chat/src/app/services/recipeTools.ts chat/src/app/services/recipeStore.ts chat/src/app/services/recipeToolHandlers.ts mcp mcp-client devops`
       Output MUST be empty (no edits to v1.0 RecipeWorkbench, no edits to other workspaces). If the diff lists any files under these paths, fail this checkpoint and report.

    10. Verify the `/webmcp` page still works (regression safety):
        Navigate to `http://localhost:4300/webmcp`. Confirm the Recipe Workbench page loads with its preloaded recipe and the AgentDrawer is operational. Open DevTools and confirm no new console errors compared to the Phase 4 baseline.

    Optional (negative-path verification — skip if time-constrained):
    11. Force a handshake timeout: in DevTools, before clicking `Show demo carousel`, set a network throttle to "Offline" — N/A here because srcdoc is inline. Better: temporarily edit `iframeBridgeScript.ts` to NOT send `ui/initialize` (comment out the request), reload, click the button. Within 1s the locked error UI appears with `Couldn't load app` heading and `Try again` button. Click `Try again` and verify the iframe remounts (though it will still time out — this just exercises the retry path). REVERT the edit before continuing.
  </how-to-verify>
  <resume-signal>
    Type `approved` if all checks pass, OR describe any issues observed (handshake never completes, carousel doesn't render, Pick doesn't update meal plan, dark mode not propagating, console errors, brownfield boundary violation, /webmcp regression).
  </resume-signal>
  <verify>
    <automated>echo "checkpoint: awaiting user `approved` per resume-signal"</automated>
  </verify>
  <done>
    User has typed `approved` after stepping through every numbered item in <how-to-verify>. All browser-observable Phase 5 success criteria are confirmed: handshake within 1s, carousel renders, theme propagation works without flash, Pick triggers commitRecipeToPlan and MealPlanColumn re-renders, plan persists across reload, brownfield boundary intact, /webmcp regression-clean.
  </done>
</task>

</tasks>

<verification>
After all five tasks land:

Automated gates:
1. `npx nx typecheck chat` exits 0.
2. `npx nx lint chat` exits 0.
3. `npx nx build chat` exits 0 (the build must also succeed for the live demo to work).
4. The four files listed in `files_modified` are the ONLY files touched by this plan: `git diff --name-only main...HEAD` (relative to phase start) lists exactly those four paths.
5. No edits under `chat/src/app/components/RecipeWorkbench/`, `mcp/`, `mcp-client/`, `devops/awsweb/` — verified by grep of `git diff --name-only` output.
6. Disjoint tool registration with /webmcp: `grep -F "previousRegistrationController" chat/src/app/services/genUITools.ts` returns nothing (genUITools.ts uses `previousGenUIRegistrationController` from Plan 05-01); `grep -F "previousGenUIRegistrationController" chat/src/app/components/RecipeWorkbenchPage.tsx` returns nothing (RecipeWorkbenchPage.tsx keeps its own `previousRegistrationController`).

Browser smoke check: Task 5 above (blocking checkpoint).
</verification>

<success_criteria>
Plan 05-02 is complete when:
- All four files exist with the exports listed in `must_haves.artifacts`.
- `npx nx typecheck chat`, `npx nx lint chat`, and `npx nx build chat` all exit 0.
- The Task 5 checkpoint is approved (user types `approved`).
- Browser console shows the expected `[mcp-apps:host]` and `[mcp-apps:iframe]` breadcrumbs during the round-trip.
- Each card's `Pick` button successfully invokes `commitRecipeToPlan` and the MealPlanColumn re-renders.
- Theme propagation works: dark-mode toggle re-themes the iframe content within one animation frame, no flash on initial mount.
- The brownfield boundary holds: no edits to v1.0 RecipeWorkbench or any other workspace.
- The /webmcp page still works without regression.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-02-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Required content:
- frontmatter `provides:` list matching the four artifact paths (ChatBubbleContainer, UIResourceFrame, ChatPlaceholder updated, GenerativeUIPage updated)
- `key_files.created:` for ChatBubbleContainer, UIResourceFrame; `key_files.modified:` for ChatPlaceholder, GenerativeUIPage
- `decisions:` capturing any minor planner-discretion choices (e.g., overlay-vs-conditional render approach, exact placement of the new useEffect, error-state retry semantics)
- `notes_for_phase_6`: how to extend UIResourceFrame for chat-message-borne resources (the `html` prop already supports it — Phase 6 just routes the chat tool result through it), and how Phase 6 will add `visibility:["app"]` to commitRecipeToPlan.
- Test-flake notes if the handshake was observed to occasionally exceed 200ms (informs Phase 7 polish).
</output>
