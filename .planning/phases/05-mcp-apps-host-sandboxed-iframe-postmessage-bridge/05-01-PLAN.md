---
phase: 05-mcp-apps-host-sandboxed-iframe-postmessage-bridge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - chat/src/app/components/GenerativeUI/iframe/bridge.ts
  - chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts
  - chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts
  - chat/src/app/services/genUITools.ts
autonomous: true
requirements:
  - GENUI-06
  - GENUI-07
  - GENUI-08
tags: [mcp-apps, iframe, sandbox, json-rpc, postmessage, sep-1865, webmcp, brownfield]
must_haves:
  truths:
    - "Module bridge.ts exports JSON-RPC 2.0 TS types (JsonRpcRequest, JsonRpcResponse, JsonRpcNotification, JsonRpcError, UIInitializeParams, UIInitializeResult, HostInfo, HostContext, HostContextChangedParams, SizeChangedParams, ToolsCallParams, ToolsCallResult) and the RPC_ERROR code constant"
    - "Module bridge.ts exports createUIResourceBridge() factory that returns a host-side bridge with: pre-load outbound queue, FIFO flush on iframe load, 1000ms handshake timeout, ResizeObserver-driven height sync handler, sendHostContextChanged notification, tools/call proxy that resolves a passed-in ModelContextTool[] by name, strict event.source === outerIframe.contentWindow origin guard, and destroy() cleanup"
    - "Module iframeBridgeScript.ts exports two strings: outerShellHTML (or outerShellRelayScript) and innerIframeBridgeScript — the iframe-side JS that handles ui/initialize, ui/notifications/initialized, ui/notifications/host-context-changed, tools/call requests with pending-request map, ResizeObserver(document.documentElement) debounced 50ms, and delegated click on .pick-btn[data-recipe-id]"
    - "Module carouselTemplate.ts exports renderCarouselHTML(recipes: Recipe[]): string that returns a complete <!DOCTYPE html> outer-shell document containing the inner iframe (sandbox='allow-scripts') whose srcdoc carries the locked inline CSS subset, the inner bridge script, and HTML-entity-escaped recipe cards (title, totalMinutes badge, top ingredient names, Pick button with data-recipe-id)"
    - "Module genUITools.ts exports GEN_UI_TOOLS: ModelContextTool[] containing the stub commitRecipeToPlan tool whose execute handler calls MealPlanStore.addToPlan and returns { content: [{ type: 'text', text: 'Added to plan' }] }"
    - "Module genUITools.ts exports registerGenUITools(): AbortController that mirrors the RecipeWorkbenchPage.tsx:164-228 pattern with its OWN module-scope previousGenUIRegistrationController guard, wraps GEN_UI_TOOLS with wrapToolsWithEvents, registers each via navigator.modelContext.registerTool with the controller's signal, swallows DUPLICATE_NAME_PATTERN errors, and returns the controller for the caller to abort on unmount"
    - "All four new files compile under `npx nx typecheck chat` with no errors and no `any` casts at the API boundary (per CLAUDE.md TS strict requirement)"
    - "Lint passes on all four new files via `npx nx lint chat`"
  artifacts:
    - path: "chat/src/app/components/GenerativeUI/iframe/bridge.ts"
      provides: "JSON-RPC types + createUIResourceBridge host-side factory"
      exports: ["JsonRpcRequest", "JsonRpcResponse", "JsonRpcNotification", "JsonRpcError", "UIInitializeParams", "UIInitializeResult", "HostInfo", "HostContext", "HostContextChangedParams", "SizeChangedParams", "ToolsCallParams", "ToolsCallResult", "RPC_ERROR", "createUIResourceBridge"]
    - path: "chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts"
      provides: "outer shell relay JS + inner iframe bridge JS as exported string templates"
      exports: ["outerShellHTML", "innerIframeBridgeScript"]
    - path: "chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts"
      provides: "renderCarouselHTML(recipes) helper that emits the complete double-iframe HTML document with inline CSS subset and HTML-escaped recipe data"
      exports: ["renderCarouselHTML"]
    - path: "chat/src/app/services/genUITools.ts"
      provides: "GEN_UI_TOOLS stub array + registerGenUITools() lifecycle helper"
      exports: ["GEN_UI_TOOLS", "registerGenUITools"]
  key_links:
    - from: "chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts"
      to: "chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts"
      via: "string import + interpolation into srcdoc"
      pattern: "import.*from.*iframeBridgeScript"
    - from: "chat/src/app/services/genUITools.ts"
      to: "chat/src/app/services/MealPlanStore.ts"
      via: "addToPlan call inside execute handler"
      pattern: "MealPlanStore\\.(addToPlan|.+)"
    - from: "chat/src/app/services/genUITools.ts"
      to: "chat/src/app/services/toolAdapter.ts"
      via: "wrapToolsWithEvents import + usage inside registerGenUITools"
      pattern: "wrapToolsWithEvents"
---

<objective>
Land the foundational MCP Apps host modules in isolation — JSON-RPC types + host-side bridge factory, iframe-side bridge JS string templates, the carousel HTML generator, and the stub `commitRecipeToPlan` tool with its lifecycle registration helper. After this plan, every primitive the Phase 5 host stack needs exists as a typed, lint-clean, compiling module. No React component yet, no page wiring yet — those land in Plan 05-02 which depends on these exports.

Purpose: Front-load all non-visual concerns (JSON-RPC protocol, sandbox HTML generation, tool registration) so Plan 05-02 can focus on the React lifecycle wiring without re-litigating bridge semantics.

Output: Four new files under `chat/src/app/components/GenerativeUI/iframe/` and `chat/src/app/services/` — all compiling and lint-clean, ready to be imported by `UIResourceFrame.tsx` and `GenerativeUIPage.tsx` in Plan 05-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-CONTEXT.md
@.planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-RESEARCH.md
@.planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-UI-SPEC.md
@.planning/phases/04-v1-1-foundation-page-shell-store-seed/04-01-SUMMARY.md
@chat/src/app/types/webmcp.d.ts
@chat/src/app/services/toolAdapter.ts
@chat/src/app/services/RecipePersistence.ts
@chat/src/app/services/MealPlanStore.ts
@chat/src/app/services/recipeSeed.ts
@chat/src/app/components/RecipeWorkbenchPage.tsx

<interfaces>
<!-- Key contracts extracted from the codebase. Use directly — no further exploration needed. -->

From chat/src/app/types/webmcp.d.ts (ambient, no import needed):
- ModelContextTool: { name: string; description: string; inputSchema?: object; execute: (input: any, client?: ModelContextClient) => Promise<unknown> | unknown; title?: string; annotations?: { readOnlyHint?: boolean; untrustedContentHint?: boolean } }
- navigator.modelContext?: ModelContext with registerTool(tool, options?: { signal?: AbortSignal }): void

From chat/src/app/services/toolAdapter.ts:
- export function wrapToolsWithEvents(tools: ModelContextTool[], onEvent: (e: ToolCallEvent) => void): ModelContextTool[]
- export type ToolCallEvent = { kind: 'pending'; toolName: string; args: Record<string, unknown> } | { kind: 'done'; toolName: string } | { kind: 'error'; toolName: string; message: string }

From chat/src/app/services/RecipePersistence.ts:
- export interface Recipe { id: string; title: string; servings: number; ingredients: Ingredient[]; steps: string[]; totalMinutes?: number; searchableIngredients?: string[] }
- export interface Ingredient { name: string; quantity: number; unit: string }
- export interface PlanEntry { id: string; recipeId: string; addedAt: number; servings?: number }
- export function getRecipes(): Promise<Recipe[]>

From chat/src/app/services/MealPlanStore.ts:
- export async function addToPlan(entry: PlanEntry): Promise<void>  // also notifies pub-sub

From chat/src/app/components/RecipeWorkbenchPage.tsx (lines 164-228, the registration template):
- Module-scope `let previousRegistrationController: AbortController | null = null;`
- Module-scope `const DUPLICATE_NAME_PATTERN = /duplicate tool name|already registered/i;`
- Pattern: defensive prior-controller abort -> new AbortController -> wrapped tools loop -> per-tool try/catch swallowing DUPLICATE_NAME_PATTERN -> return cleanup that aborts controller.

SEP-1865 wire format (locked in CONTEXT.md + RESEARCH.md Pattern 3):
- protocolVersion string: '2026-01-26'
- HostInfo: { name: 'window-ai-generative-ui', version: '1.1.0', mcpVersion: '2026-01-26' }
- HostContext: { theme: 'light'|'dark'; displayMode: 'inline'; dimensions: { maxWidth: number; maxHeight: number } }

RPC error codes (locked in RESEARCH.md Pattern 3):
- PARSE_ERROR=-32700, INVALID_REQUEST=-32600, METHOD_NOT_FOUND=-32601, INVALID_PARAMS=-32602, INTERNAL_ERROR=-32603, TOOL_ERROR=-32000, DENIED=-32000
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: bridge.ts — JSON-RPC types + createUIResourceBridge host factory</name>
  <files>chat/src/app/components/GenerativeUI/iframe/bridge.ts</files>
  <read_first>
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-CONTEXT.md (Sandbox + iframe rendering strategy, JSON-RPC bridge protocol sections — locked)
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-RESEARCH.md (Pattern 3 JSON-RPC types, Pattern 9 pre-load outbound queue, Implementation Guide bridge.ts Host-Side Class, "tools/call proxy handler" code example, Pitfall 5 navigator.modelContext fallback)
    - chat/src/app/types/webmcp.d.ts (ambient ModelContextTool shape)
    - chat/src/app/services/MealPlanStore.ts (addToPlan signature — bridge does NOT touch this directly but the tools/call proxy invokes tool.execute which lands here)
    - chat/src/app/components/RecipeWorkbenchPage.tsx lines 164-228 (controller pattern — not used here but informs the destroy() shape)
  </read_first>
  <action>
    Create the JSON-RPC layer for the host side. Two concerns in one module per the locked file layout (bridge.ts owns BOTH the types AND the host-side factory):

    (1) Export the JSON-RPC 2.0 core type set verbatim from RESEARCH.md Pattern 3:
        JsonRpcRequest, JsonRpcResponse, JsonRpcNotification, JsonRpcError.

    (2) Export the SEP-1865 message shape interfaces verbatim from RESEARCH.md Pattern 3:
        UIInitializeParams, UIInitializeResult, HostInfo, HostContext, HostContextChangedParams,
        SizeChangedParams, ToolsCallParams, ToolsCallResult.

    (3) Export `RPC_ERROR` as `const` with codes PARSE_ERROR=-32700, INVALID_REQUEST=-32600,
        METHOD_NOT_FOUND=-32601, INVALID_PARAMS=-32602, INTERNAL_ERROR=-32603, TOOL_ERROR=-32000,
        DENIED=-32000 (the table in RESEARCH.md Pattern 3 is authoritative — match it exactly).

    (4) Export `createUIResourceBridge(opts)` factory (use a factory function returning a stateful
        object rather than a class — easier to consume from React without `this` binding). Options
        shape:
          {
            outerIframe: HTMLIFrameElement;             // owner-supplied DOM element
            tools: ModelContextTool[];                   // resolved tool registry (per RESEARCH Open Q3)
            hostInfo: HostInfo;                          // { name, version, mcpVersion }
            initialHostContext: HostContext;             // { theme, displayMode, dimensions }
            handshakeTimeoutMs?: number;                 // default 1000
            requestTimeoutMs?: number;                   // default 5000
            onHandshakeComplete?: () => void;            // fires on ui/notifications/initialized
            onHandshakeTimeout?: () => void;             // fires after handshakeTimeoutMs
            onSizeChanged?: (height: number) => void;    // host-side ResizeObserver receiver
            onLog?: (tag: 'host'|'iframe'|'relay', msg: string, data?: unknown) => void;
          }

    The factory must return an object with these methods (NO React imports — pure TS, framework-free):
      - attachLoadListener(): void   // wires outerIframe.addEventListener('load', ...) — must be called by the component AFTER assigning srcdoc
      - handleMessage(event: MessageEvent): void   // wired to window's 'message' listener by the component
      - sendHostContextChanged(params: HostContextChangedParams): void   // queues or sends immediately
      - destroy(): void   // removes listeners (those it owns), clears timers, rejects in-flight pending requests, empties queue

    Internal state (closures, not exported):
      - pendingOutbound: unknown[] = []
      - iframeLoaded: boolean = false
      - pendingRequests: Map<string|number, { resolve, reject, timer }>
      - handshakeTimer: ReturnType<typeof setTimeout> | null
      - destroyed: boolean

    Behavior contracts (mirror RESEARCH.md Pattern 9 + "tools/call proxy handler" code example):
      - On `attachLoadListener()`: register a one-shot 'load' handler on outerIframe; on fire set
        iframeLoaded=true, flush pendingOutbound FIFO via outerIframe.contentWindow.postMessage(msg, '*'),
        start handshakeTimer (handshakeTimeoutMs); if timer fires before ui/notifications/initialized
        seen → call onHandshakeTimeout.
      - `handleMessage`: GUARD `event.source === outerIframe.contentWindow` first (RESEARCH Anti-Patterns).
        Parse `event.data` as a JsonRpcRequest | JsonRpcResponse | JsonRpcNotification. Switch on method:
          - 'ui/initialize' (request): respond via outerIframe.contentWindow.postMessage with
            { jsonrpc: '2.0', id, result: { protocolVersion: '2026-01-26', hostInfo, hostContext: initialHostContext } }.
          - 'ui/notifications/initialized' (notification): clear handshakeTimer, call onHandshakeComplete.
          - 'ui/notifications/size-changed' (notification): call onSizeChanged(params.height) — the host
            component is responsible for writing outerIframe.style.height (this preserves React control).
          - 'tools/call' (request): find tool = tools.find(t => t.name === params.name); if undefined →
            post error response with code METHOD_NOT_FOUND. Else: await tool.execute(params.arguments ?? {})
            wrapped in try/catch. Success → post { jsonrpc:'2.0', id, result }. Failure → post
            { jsonrpc:'2.0', id, error: { code: TOOL_ERROR, message } }.
          - Response messages (id present, result/error present, method absent): match by id from
            pendingRequests Map (host doesn't currently SEND requests in Phase 5, but the plumbing must
            exist so Plan 05-02's theme propagation can extend safely).
          - Unknown method: post error response with code METHOD_NOT_FOUND.
      - `sendHostContextChanged(params)`: if !iframeLoaded → push to pendingOutbound; else postMessage
        immediately. The notification shape is { jsonrpc: '2.0', method: 'ui/notifications/host-context-changed', params }.
      - `destroy()`: set destroyed=true, clear handshakeTimer, reject all pendingRequests with
        Error('bridge destroyed'), clear pendingOutbound. Idempotent.

    Logging: every inbound and outbound message goes through `onLog?.('host', '<method>', data)` or
    `'iframe'` for inbound, `'host'` for outbound. The console.debug breadcrumbs Plan 05-02 needs land
    via this callback (caller passes (tag, msg, data) => console.debug(`[mcp-apps:${tag}]`, msg, data)).

    Strict TS: no `any` at the API surface. The factory option types and method signatures are typed.
    Internal switches may narrow via type guards (e.g. checking `'id' in data` to distinguish request
    vs notification).

    Do not import React. Do not import from MealPlanStore. The tools/call proxy is data-driven through
    the `tools` option; the caller (Plan 05-02's UIResourceFrame) passes GEN_UI_TOOLS in.

    Do not register window message listeners inside the factory — the React component owns lifecycle
    and calls `handleMessage` from its own listener (this avoids leaking listeners on StrictMode
    double-mount). The factory ONLY owns the 'load' listener on outerIframe (which the component can
    destroy by replacing the iframe DOM element via key bump).
  </action>
  <verify>
    <automated>npx nx typecheck chat 2>&amp;1 | tail -30 &amp;&amp; npx nx lint chat --files chat/src/app/components/GenerativeUI/iframe/bridge.ts 2>&amp;1 | tail -20 &amp;&amp; grep -cE "^export (interface|type|const|function)" chat/src/app/components/GenerativeUI/iframe/bridge.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at chat/src/app/components/GenerativeUI/iframe/bridge.ts.
    - `grep -cE "^export (interface|type|const|function)" chat/src/app/components/GenerativeUI/iframe/bridge.ts` returns ≥ 14 (12 interface/type exports per `must_haves.artifacts[0].exports` + RPC_ERROR const + createUIResourceBridge function).
    - `grep -F "createUIResourceBridge" chat/src/app/components/GenerativeUI/iframe/bridge.ts | grep -c "export"` ≥ 1.
    - `grep -F "RPC_ERROR" chat/src/app/components/GenerativeUI/iframe/bridge.ts | grep -c "export"` ≥ 1.
    - `grep -F "'2026-01-26'" chat/src/app/components/GenerativeUI/iframe/bridge.ts | grep -c .` ≥ 1 (protocolVersion appears as a string literal default somewhere, either in a type default or in the ui/initialize response builder).
    - `grep -cE ": any" chat/src/app/components/GenerativeUI/iframe/bridge.ts` returns 0 (no `: any` annotations anywhere — strict TS).
    - `grep -cE "as any" chat/src/app/components/GenerativeUI/iframe/bridge.ts` returns 0 (no `as any` casts).
    - `grep -F "event.source === " chat/src/app/components/GenerativeUI/iframe/bridge.ts | grep -c .` ≥ 1 (origin guard present per RESEARCH Anti-Patterns).
    - `grep -F "pendingOutbound" chat/src/app/components/GenerativeUI/iframe/bridge.ts | grep -c .` ≥ 1 (pre-load queue present per Pattern 9).
    - `grep -F "handshakeTimer" chat/src/app/components/GenerativeUI/iframe/bridge.ts | grep -c .` ≥ 1 (1000ms handshake timeout machinery).
    - `npx nx typecheck chat` exits 0.
    - `npx nx lint chat` exits 0 (no errors on this file).
    - Comments in the file reference 05-CONTEXT.md or 05-RESEARCH.md at least once (preserve provenance per Phase 4 convention).
  </acceptance_criteria>
  <done>
    bridge.ts compiles, lints, and exports the full JSON-RPC type surface + createUIResourceBridge factory with origin guard, pre-load queue, 1000ms handshake timer, tools/call proxy via injected `tools` option, and a framework-free destroy() — no React, no DOM mutation outside outerIframe.contentWindow.postMessage, no `any` casts.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: iframeBridgeScript.ts + carouselTemplate.ts — sandbox HTML emitters</name>
  <files>chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts, chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts</files>
  <read_first>
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-CONTEXT.md (Sandbox + iframe rendering strategy, Theme propagation sections — locked)
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-RESEARCH.md (Pattern 2 Double-iframe HTML structure, Pattern 5 ResizeObserver script, Pattern 6 Handshake from inner iframe, Security Domain — HTML entity escaping required, Pitfall 3 Tailwind classes unavailable)
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-UI-SPEC.md (Inline CSS Subset, Color, Typography, Iframe Inner Layout, "Pick" button interaction, Copywriting Contract for iframe — Pick / Tools unavailable / Added!)
    - chat/src/app/services/RecipePersistence.ts (Recipe + Ingredient types — for renderCarouselHTML input)
  </read_first>
  <action>
    Create two co-dependent files. carouselTemplate.ts imports from iframeBridgeScript.ts.

    ── iframeBridgeScript.ts ──────────────────────────────────────────────────────

    Export two string constants — these are JS source code as strings, NOT compiled
    TypeScript. Use template literals (no transpilation needed). The inner script
    runs in a null-origin srcdoc context (no ES modules, no TS — plain ES2017+ JS
    using `var`, function declarations, IIFEs).

    Export 1: `outerShellHTML` — a complete `<!DOCTYPE html>` document for the
    OUTER iframe's srcdoc. Per RESEARCH Pattern 2, this shell:
      - Contains a single inner `<iframe id="inner" sandbox="allow-scripts">` whose
        `srcdoc` value is the placeholder string `__INNER_SRCDOC__` (carouselTemplate.ts
        will perform a literal `replace('__INNER_SRCDOC__', escapeForAttribute(innerHTML))`).
      - Has a `<style>` block resetting html/body margin/padding to 0 and stretching the
        iframe to 100% width with `border: 0; display: block;`.
      - Has a `<script>` block (NOT modules) with two `window.addEventListener('message', ...)`
        listeners — the relay pair per RESEARCH Pattern 2:
          (a) inner→host: if event.source === inner.contentWindow → window.parent.postMessage(event.data, '*')
          (b) host→inner: if event.source === window.parent → inner.contentWindow.postMessage(event.data, '*')
        These are mutually exclusive by event.source so no echo loop (RESEARCH "Relay echo problem").
      - Includes a `console.debug('[mcp-apps:relay]', direction, data)` log in each branch.

    Export 2: `innerIframeBridgeScript` — the inline JS string for the INNER iframe.
    Source per RESEARCH Pattern 5 (ResizeObserver) + Pattern 6 (handshake/request correlator):
      - IIFE that sets up:
          - var pendingIframeRequests = {}; var nextId = 1;
          - function sendRequest(method, params) returning a Promise with 5s timeout, posting
            { jsonrpc: '2.0', id, method, params } to window.parent. Adds to pendingIframeRequests.
          - function sendNotification(method, params) posting { jsonrpc: '2.0', method, params }
            to window.parent (no id).
          - window 'message' listener:
              * if data.jsonrpc !== '2.0' → ignore.
              * if data.id !== undefined && pendingIframeRequests[data.id] → resolve/reject pending.
              * if data.method === 'ui/notifications/host-context-changed' →
                document.documentElement.setAttribute('data-theme', data.params.theme || 'light').
      - Send `ui/initialize` request as soon as the script runs (srcdoc is inline; DOMContentLoaded
        is not strictly required, but wrap in `if (document.readyState === 'loading')
        document.addEventListener('DOMContentLoaded', start); else start();` for safety).
        On its response (which contains hostContext.theme) → set initial data-theme attribute, then
        send `ui/notifications/initialized` notification.
      - After handshake: attach ResizeObserver(document.documentElement) with 50ms debounce; on fire,
        sendNotification('ui/notifications/size-changed', { height: Math.ceil(entry.contentRect.height) }).
      - After handshake: attach delegated click listener on document — if event.target.matches('.pick-btn[data-recipe-id]'):
          * read recipeId = button.dataset.recipeId
          * button.disabled = true; button.style.opacity = '0.6';
          * sendRequest('tools/call', { name: 'commitRecipeToPlan', arguments: { recipeId } })
            then() → button.textContent = 'Added!'; setTimeout(() => { button.textContent = 'Pick'; button.disabled = false; button.style.opacity = ''; }, 2000)
            catch() → button.textContent = 'Tools unavailable'; button.style.background = '#6b7280'; (no revert).
      - Every inbound + outbound message gets `console.debug('[mcp-apps:iframe]', method, data)`.

    Both scripts use only ES2017+ syntax (var, function, arrow OK, template literals OK, async/await
    OK in IIFE wrappers). No imports. No `let const class` is required but `let const` are fine.

    ── carouselTemplate.ts ────────────────────────────────────────────────────────

    Export `renderCarouselHTML(recipes: Recipe[]): string`. Behavior:

    (1) Build the INNER document HTML as a single string:
        `<!DOCTYPE html><html><head>
           <meta charset="UTF-8">
           <meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; script-src 'unsafe-inline'; img-src data:">
           <style>` + CSS_SUBSET + `</style>
         </head>
         <body>
           <div class="carousel">` + recipes.map(renderCard).join('') + `</div>
           <script>` + innerIframeBridgeScript + `</script>
         </body>
         </html>`

        Where CSS_SUBSET is the COMPLETE inline CSS string locked in 05-UI-SPEC.md
        ("Inline CSS Subset" + "Body reset" + "CSS custom properties on :root and [data-theme='dark']"
        + media query block). Copy these verbatim — they are byte-locked by checker sign-off.

    (2) `renderCard(recipe)` emits ONLY classes listed in the UI-SPEC table:
        <div class="card">
          <h3 class="card-title">{escapeHtml(recipe.title)}</h3>
          {recipe.totalMinutes !== undefined && <span class="badge">{recipe.totalMinutes} min</span>}
          <ul class="ingredients">
            {recipe.ingredients.slice(0, 3).map(i => `<li>${escapeHtml(i.name)}</li>`).join('')}
          </ul>
          <button class="pick-btn" data-recipe-id="${escapeAttr(recipe.id)}" type="button">Pick</button>
        </div>

    (3) Provide local helpers `escapeHtml(s)` and `escapeAttr(s)` per RESEARCH Security Domain
        ("HTML entity escaping is required"). escapeHtml replaces & < > " ' with entities;
        escapeAttr does the same (single-pass acceptable since attribute values are double-quoted).

    (4) Wrap the inner HTML inside the OUTER shell:
        return outerShellHTML.replace('__INNER_SRCDOC__', escapeAttr(innerHTML));

        Note: srcdoc attribute values must have their `"` characters escaped to `&quot;`.
        escapeAttr already does this.

    Constraints:
      - Do NOT use any Tailwind class names inside the carousel HTML (Pitfall 3). Only the
        classes in the UI-SPEC Inline CSS Subset table: .carousel, .card, .card-title, .badge,
        .ingredients, .pick-btn (+ pseudo-class variants).
      - Do NOT inline recipe data via JSON.parse(...) inside the inner script — bake it
        into static DOM elements as shown above (per CONTEXT.md "the recipe data baked into
        the HTML as static <div class=\"recipe-card\" ...>"). One subtlety: the CONTEXT.md
        shows `class="recipe-card"` but the UI-SPEC inline CSS subset uses `.card`. The
        UI-SPEC is the locked authority — use `.card`.
      - Default placeholder badge: if recipe.totalMinutes is undefined, omit the badge entirely.
      - If recipe.ingredients is empty, render an empty <ul> (still valid HTML).
      - No external resources (no <img src=>, no <link>, no <script src=>). All CSS and JS inline.

    Strict TS: function signature is `(recipes: Recipe[]): string`. Internal helpers
    are typed `(s: string) => string`.

    Both files compile under TS strict, no `any`.
  </action>
  <verify>
    <automated>npx nx typecheck chat 2>&amp;1 | tail -20 &amp;&amp; npx nx lint chat 2>&amp;1 | tail -20 &amp;&amp; node -e "const {renderCarouselHTML}=require('./chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts'); console.log('compiles')" 2>&amp;1 | head -5; grep -F "innerIframeBridgeScript" chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts | grep -c .; grep -F "escapeHtml" chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts | grep -c .; grep -F "ResizeObserver" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts | grep -c .; grep -F "sandbox=\\\"allow-scripts\\\"" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts | grep -c .; grep -F "default-src 'none'" chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts | grep -c .</automated>
  </verify>
  <acceptance_criteria>
    - chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts exists.
    - chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts exists.
    - `grep -cE "^export const outerShellHTML" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts` returns 1.
    - `grep -cE "^export const innerIframeBridgeScript" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts` returns 1.
    - `grep -F "sandbox=\"allow-scripts\"" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts | grep -cv "allow-same-origin"` returns ≥ 1 (inner iframe sandbox attribute is `allow-scripts` only, NO allow-same-origin per Locked decision).
    - `grep -F "__INNER_SRCDOC__" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts | grep -c .` returns ≥ 1 (placeholder for carouselTemplate.ts to substitute).
    - `grep -F "ResizeObserver" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts | grep -c .` returns ≥ 1.
    - `grep -F "ui/initialize" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts | grep -c .` returns ≥ 2 (request send + response handling).
    - `grep -F "ui/notifications/size-changed" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts | grep -c .` returns ≥ 1.
    - `grep -F "ui/notifications/host-context-changed" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts | grep -c .` returns ≥ 1.
    - `grep -F "tools/call" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts | grep -c .` returns ≥ 1.
    - `grep -F "data-theme" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts | grep -c .` returns ≥ 1.
    - `grep -cE "^export function renderCarouselHTML" chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts` returns 1.
    - `grep -F "default-src 'none'" chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts | grep -c .` returns ≥ 1 (CSP baseline present).
    - `grep -F "innerIframeBridgeScript" chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts | grep -c .` returns ≥ 1 (imports the inner script).
    - `grep -F "outerShellHTML" chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts | grep -c .` returns ≥ 1 (imports the outer shell).
    - `grep -F "escapeHtml" chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts | grep -c .` returns ≥ 2 (definition + at least one call site).
    - `grep -F "Tailwind" chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts` returns nothing OR only comments (no Tailwind utility classes interpolated — per Pitfall 3).
    - `grep -cE "(^|[^a-zA-Z])class=\"(carousel|card|card-title|badge|ingredients|pick-btn)[\" ]" chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts` returns ≥ 6 (all six UI-SPEC class names appear in the template).
    - `grep -cE ": any" chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts` returns 0.
    - `npx nx typecheck chat` exits 0.
    - `npx nx lint chat` exits 0.
  </acceptance_criteria>
  <done>
    Both files compile and lint. carouselTemplate.ts emits a complete double-iframe HTML document with the locked CSP baseline, the UI-SPEC inline CSS subset, HTML-escaped recipe cards, and the inline bridge JS. iframeBridgeScript.ts exports the outer relay shell + inner iframe bridge script (handshake + ResizeObserver + tools/call sender + theme listener + Pick button delegation) as strings ready to inject into srcdoc.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: genUITools.ts — stub commitRecipeToPlan + registerGenUITools lifecycle helper</name>
  <files>chat/src/app/services/genUITools.ts</files>
  <read_first>
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-CONTEXT.md (Tool registration for round-trip section — locked stub shape)
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-RESEARCH.md (Pattern 1 navigator.modelContext.registerTool, Pattern 7 wrapToolsWithEvents contract, Pattern 10 commitRecipeToPlan tool definition, Pitfall 1 StrictMode double-mount)
    - chat/src/app/components/RecipeWorkbenchPage.tsx lines 1-30 (module-scope guard imports), lines 35-50 (DUPLICATE_NAME_PATTERN + previousRegistrationController), lines 164-228 (the registration effect body to mirror)
    - chat/src/app/services/toolAdapter.ts (wrapToolsWithEvents + ToolCallEvent — full file is short)
    - chat/src/app/services/MealPlanStore.ts (addToPlan signature)
    - chat/src/app/services/RecipePersistence.ts (PlanEntry interface re-exported from MealPlanStore)
  </read_first>
  <action>
    Create the page-side WebMCP tool registration module. Mirrors RecipeWorkbenchPage.tsx:164-228
    but as an exported helper function (not embedded in a component) so GenerativeUIPage.tsx in
    Plan 05-02 can call it from a tiny useEffect.

    Module-scope state (per Pitfall 1 — module-scope refs persist across React StrictMode remounts):
      let previousGenUIRegistrationController: AbortController | null = null;
      const DUPLICATE_NAME_PATTERN = /duplicate tool name|already registered/i;

    Use a NAME DIFFERENT from RecipeWorkbenchPage.tsx's `previousRegistrationController` —
    `previousGenUIRegistrationController` (per RESEARCH Pattern 1 key facts: "each module has
    its own"). This prevents cross-contamination between `/webmcp` and `/generative-ui` tool
    registrations if both pages are open simultaneously (STATE.md "Duplicate-tool-name across
    /webmcp ↔ /generative-ui route transitions" risk).

    Export `GEN_UI_TOOLS: ModelContextTool[]` containing ONE entry (the stub):
      {
        name: 'commitRecipeToPlan',
        description: 'Add a recipe to the current meal plan',
        inputSchema: {
          type: 'object',
          properties: {
            recipeId: { type: 'string', description: 'The ID of the recipe to add' },
            servings: { type: 'number', description: 'Number of servings (optional, defaults to recipe default)' },
          },
          required: ['recipeId'],
        },
        execute: async (input) => {
          const { recipeId, servings } = input as { recipeId: string; servings?: number };
          await MealPlanStore.addToPlan({
            id: crypto.randomUUID(),
            recipeId,
            addedAt: Date.now(),
            servings,
          });
          return { content: [{ type: 'text', text: 'Added to plan' }] };
        },
      }

    No `visibility:["app"]` annotation per CONTEXT.md scope boundary (Phase 6 GENUI-05 adds that).

    Export `registerGenUITools(): AbortController`:
      - Returns null OR the new AbortController (TS: `AbortController | null`). Caller pattern:
        `const ctrl = registerGenUITools(); return () => ctrl?.abort();`
      - If `typeof navigator === 'undefined' || !navigator.modelContext` → console.warn
        '[GenUI] navigator.modelContext unavailable; tool registration skipped' and return null.
      - Otherwise, mirror the RecipeWorkbenchPage.tsx pattern:
        1. If previousGenUIRegistrationController exists and is not aborted → abort it.
        2. Create new controller; assign to module-scope previousGenUIRegistrationController.
        3. Build wrapped tools via wrapToolsWithEvents(GEN_UI_TOOLS, onToolEvent) where onToolEvent
           is `(e) => { console.debug('[mcp-apps:genUI-tool]', e.kind, e.toolName); }`.
        4. for (const tool of wrapped): try navigator.modelContext.registerTool(tool, { signal:
           controller.signal }); push name on success; catch DUPLICATE_NAME_PATTERN → treat as
           success per Pitfall 1; any other error → console.error and break.
        5. Return the controller.

    Do NOT mount a useEffect inside this file (it is a service module, not a React component).
    The caller in Plan 05-02 wraps the call in useEffect with `[]` deps and aborts on cleanup.

    Strict TS: no `: any` in the public API. The execute callback signature uses
    `(input: unknown) => Promise<unknown>` and casts internally; the inputSchema constrains
    the model side, the cast inside execute is local.

    Imports:
      import * as MealPlanStore from './MealPlanStore';
      import { wrapToolsWithEvents, type ToolCallEvent } from './toolAdapter';
  </action>
  <verify>
    <automated>npx nx typecheck chat 2>&amp;1 | tail -20 &amp;&amp; npx nx lint chat 2>&amp;1 | tail -20 &amp;&amp; grep -cE "^export (const|function) (GEN_UI_TOOLS|registerGenUITools)" chat/src/app/services/genUITools.ts; grep -F "previousGenUIRegistrationController" chat/src/app/services/genUITools.ts | grep -c .; grep -F "previousRegistrationController" chat/src/app/services/genUITools.ts; grep -F "DUPLICATE_NAME_PATTERN" chat/src/app/services/genUITools.ts | grep -c .; grep -F "wrapToolsWithEvents" chat/src/app/services/genUITools.ts | grep -c .; grep -F "MealPlanStore.addToPlan" chat/src/app/services/genUITools.ts | grep -c .; grep -F "commitRecipeToPlan" chat/src/app/services/genUITools.ts | grep -c .</automated>
  </verify>
  <acceptance_criteria>
    - File exists at chat/src/app/services/genUITools.ts.
    - `grep -cE "^export const GEN_UI_TOOLS" chat/src/app/services/genUITools.ts` returns 1.
    - `grep -cE "^export function registerGenUITools" chat/src/app/services/genUITools.ts` returns 1.
    - `grep -F "previousGenUIRegistrationController" chat/src/app/services/genUITools.ts | grep -c .` returns ≥ 3 (declaration, defensive abort, assignment).
    - `grep -F "previousRegistrationController" chat/src/app/services/genUITools.ts` returns nothing OR only inside a comment (the name must be DIFFERENT from RecipeWorkbenchPage.tsx's guard — disjoint module-scope refs).
    - `grep -F "DUPLICATE_NAME_PATTERN" chat/src/app/services/genUITools.ts | grep -c .` returns ≥ 2 (declaration + test() in catch).
    - `grep -F "wrapToolsWithEvents" chat/src/app/services/genUITools.ts | grep -c .` returns ≥ 1.
    - `grep -F "MealPlanStore.addToPlan" chat/src/app/services/genUITools.ts | grep -c .` returns ≥ 1 (handler invokes the Phase 4 API).
    - `grep -F "commitRecipeToPlan" chat/src/app/services/genUITools.ts | grep -c .` returns ≥ 1 (tool name string literal).
    - `grep -F "navigator.modelContext.registerTool" chat/src/app/services/genUITools.ts | grep -c .` returns ≥ 1.
    - `grep -F "visibility" chat/src/app/services/genUITools.ts` returns nothing (visibility:["app"] is Phase 6 — must NOT appear here).
    - `grep -F "searchRecipes" chat/src/app/services/genUITools.ts` returns nothing (Phase 6 only).
    - `grep -cE ": any" chat/src/app/services/genUITools.ts` returns 0 at the public API surface (inputs typed as `unknown` then narrowed).
    - `grep -cE "as any" chat/src/app/services/genUITools.ts` returns 0.
    - `npx nx typecheck chat` exits 0.
    - `npx nx lint chat` exits 0.
  </acceptance_criteria>
  <done>
    GEN_UI_TOOLS contains exactly one entry (commitRecipeToPlan stub). registerGenUITools mirrors the RecipeWorkbenchPage.tsx:164-228 pattern with its OWN module-scope guard (`previousGenUIRegistrationController`), wraps via wrapToolsWithEvents, handles unavailable-modelContext gracefully, and returns an AbortController the caller can abort on unmount. No `visibility` annotation. No `searchRecipes`. No `any` at API surface.
  </done>
</task>

</tasks>

<verification>
After all three tasks land:
1. `npx nx typecheck chat` exits 0.
2. `npx nx lint chat` exits 0.
3. `ls chat/src/app/components/GenerativeUI/iframe/` shows `bridge.ts`, `iframeBridgeScript.ts`, `carouselTemplate.ts`.
4. `ls chat/src/app/services/genUITools.ts` exists.
5. No edits to files outside the four listed in `files_modified`. Specifically: `grep -L "GEN_UI_TOOLS\|createUIResourceBridge\|renderCarouselHTML" chat/src/app/components/RecipeWorkbenchPage.tsx chat/src/app/components/GenerativeUIPage.tsx chat/src/app/components/GenerativeUI/ChatPlaceholder.tsx` returns ALL THREE filenames (i.e., none of them import the new identifiers yet — wiring is Plan 05-02).
6. Brownfield boundary check: `git diff --name-only main...HEAD` (or the current branch's diff against pre-phase) lists ONLY files under `chat/src/app/components/GenerativeUI/iframe/` and `chat/src/app/services/genUITools.ts`. Nothing under `mcp/`, `mcp-client/`, `devops/awsweb/`, `chat/src/app/components/RecipeWorkbench/`, or `/webmcp` paths.

Browser-side smoke check: NOT applicable to this plan — these are pure modules with no UI surface. Plan 05-02 owns the end-to-end browser smoke test (open `/generative-ui` → click "Show demo carousel" → handshake → click "Pick" → MealPlanColumn updates).
</verification>

<success_criteria>
Plan 05-01 is complete when:
- All four files exist with the exports listed in `must_haves.artifacts`.
- `npx nx typecheck chat` and `npx nx lint chat` both exit 0.
- No `: any` or `as any` at API boundaries in any new file.
- bridge.ts has origin guard (`event.source === outerIframe.contentWindow`), pre-load outbound queue (`pendingOutbound`), 1000ms handshake timer (`handshakeTimer`), and tools/call proxy via injected `tools` option.
- iframeBridgeScript.ts has inner sandbox `allow-scripts` only (NO `allow-same-origin`), ResizeObserver(document.documentElement), ui/initialize request, ui/notifications/host-context-changed handler, tools/call sender, and Pick button delegation.
- carouselTemplate.ts uses ONLY the six UI-SPEC class names (.carousel/.card/.card-title/.badge/.ingredients/.pick-btn), emits the CSP `default-src 'none'` baseline, and HTML-escapes recipe data.
- genUITools.ts has GEN_UI_TOOLS with exactly one entry (commitRecipeToPlan), no `visibility`, no `searchRecipes`, mirrors the RecipeWorkbenchPage.tsx registration pattern, uses its OWN `previousGenUIRegistrationController` module-scope guard.
- Each commit message references both the plan ID and the locked CONTEXT.md/RESEARCH.md sections (per Phase 4 commit hygiene).
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-01-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Required content:
- frontmatter `provides:` list matching the four artifact exports
- `key_files.created:` listing all four files
- `decisions:` noting any minor planner-discretion choices (e.g., factory vs class for bridge, exact onLog signature)
- `notes_for_plan_05_02`: which exports Plan 05-02 imports and from where
</output>
