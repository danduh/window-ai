---
phase: 08-proofreader-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - chat/src/app/components/Proofreader/ProofreaderPage.tsx
  - chat/src/app/components/Proofreader/ProofreaderHeader.tsx
  - chat/src/app/components/Proofreader/ProofreaderForm.tsx
  - chat/src/app/components/Proofreader/ProofreaderOutputModeToggle.tsx
  - chat/src/app/components/Proofreader/ProofreaderResultPanel.tsx
  - chat/src/app/AppRouter.tsx
  - chat/src/app/hooks/useSEOData.ts
  - chat/scripts/prerender-react.js
autonomous: true
requirements: [PROOF-01, PROOF-03, PROOF-05, PROOF-06]
tags: [proofreader, chrome-ai, page-shell, routing, seo, tabs, output-mode-toggle, brownfield]

must_haves:
  truths:
    - "Navigating to `/proofreader` mounts the Proofreader page with site Tailwind theme + dark-mode support and the nav link is visible in BOTH desktop and mobile nav (positioned after the existing Generative UI link)"
    - "When `window.Proofreader` is undefined, the `/proofreader` page renders a `MissingFlagBanner` whose title is `\"Proofreader API isn't enabled in this browser.\"`, lists BOTH required Chrome flags as copy-pasteable code chips, and the form below the banner is disabled — the workbench layout still renders but inputs are not interactable"
    - "When `ProofreaderService.getAvailability()` resolves to `'downloadable'` or `'downloading'`, the page shows an inline progress bar with a byte-counter sourced from the `monitor` callback supplied to `ProofreaderService.createWithProgress(language, onProgress)`"
    - "When `getAvailability()` resolves to `'available'` (state = 'ready'), the textarea is pre-filled with the locked demo string `\"i think there going to a meetting tommorow at the office. there progress have been great on the project!\"`, the language `<select>` defaults to the value in localStorage key `window-ai.proofreader.language` (falling back to `'en'`), and the Proofread button is enabled"
    - "Submitting the form calls `ProofreaderService.proofread(text, { language })` and, on resolve, the `ProofreaderResultPanel` renders `result.correctedInput` as a paragraph plus a `<ul>` listing each correction in the form `changed \"X\" → \"Y\" because [explanation]` (where X = original substring from `text.slice(c.startIndex, c.endIndex)` and Y = `c.correction`); missing `explanation` falls back to the suggestion type chip (e.g. `[spelling]`)"
    - "If `proofread()` rejects, the result panel renders an inline error card containing the thrown `error.message` and a Retry button that re-invokes the last submission; the error does NOT alert(), does NOT console-only, and does NOT crash the page (PROOF-06)"
    - "The `ProofreaderOutputModeToggle` segmented control shows all three options ('Plain + list', 'Side-by-side', 'Inline strikethrough'); only 'plain' is enabled; the other two have `disabled` + `cursor-not-allowed` styling AND a `title` tooltip reading `\"Coming in Phase 9\"`"
    - "Selecting a non-default language and reloading the page restores that language as the pre-selected `<select>` value (localStorage persistence — PROOF-05)"
    - "Tabs component renders with Docs FIRST (path `/docs`, content = placeholder card 'Documentation coming in Phase 12') and Workbench SECOND (path empty); URL `/proofreader/docs` selects the Docs tab; URL `/proofreader` selects the Workbench tab (via `Tabs` matching `currentPath.includes('/docs')` BEFORE the empty-path workbench match)"
    - "`seoConfigs.proofreader` and `seoConfigs.proofreaderDocs` entries exist in BOTH `chat/src/app/hooks/useSEOData.ts` AND `chat/scripts/prerender-react.js` with byte-identical title and description strings (Phase 12 audits with `grep -F`; this plan ships both sides at the same time to prevent drift)"
    - "The `/proofreader/docs` route is registered in `AppRouter.tsx` and points to `ProofreaderPage` — visiting it does NOT 404; the page renders the same outer shell with the Docs tab active"
  artifacts:
    - path: "chat/src/app/components/Proofreader/ProofreaderPage.tsx"
      provides: "Top-level Proofreader route handler with path-aware SEO, availability detection, page state machine, and Tabs"
      exports: ["ProofreaderPage"]
    - path: "chat/src/app/components/Proofreader/ProofreaderHeader.tsx"
      provides: "Title + tagline + gradient icon block mirroring GenerativeUIHeader visual shape"
      exports: ["ProofreaderHeader"]
    - path: "chat/src/app/components/Proofreader/ProofreaderForm.tsx"
      provides: "Textarea with locked default demo text + language <select> (en/es/ja/de/fr) + Proofread button; exposes onProofread(text, language) callback"
      exports: ["ProofreaderForm"]
    - path: "chat/src/app/components/Proofreader/ProofreaderOutputModeToggle.tsx"
      provides: "Three-option segmented control; 'plain' enabled, other two disabled with 'Coming in Phase 9' tooltip"
      exports: ["ProofreaderOutputModeToggle", "OutputMode"]
    - path: "chat/src/app/components/Proofreader/ProofreaderResultPanel.tsx"
      provides: "Renders ProofreadResult in selected mode; Phase 8 implements 'plain' branch only, other branches return placeholder card"
      exports: ["ProofreaderResultPanel"]
    - path: "chat/src/app/AppRouter.tsx"
      provides: "Registered /proofreader + /proofreader/docs routes and desktop + mobile nav links positioned after /generative-ui"
      contains: "path=\"/proofreader\""
    - path: "chat/src/app/hooks/useSEOData.ts"
      provides: "seoConfigs.proofreader + seoConfigs.proofreaderDocs entries with locked title/description strings"
      contains: "proofreader:"
    - path: "chat/scripts/prerender-react.js"
      provides: "/proofreader + /proofreader/docs entries in the routes array AND the seoConfigs map with byte-identical strings to useSEOData.ts"
      contains: "/proofreader"
  key_links:
    - from: "chat/src/app/components/Proofreader/ProofreaderPage.tsx"
      to: "chat/src/app/services/ProofreaderService.ts"
      via: "named imports getAvailability + proofread + createWithProgress + LOCAL_STORAGE_KEY + LANGUAGE_OPTIONS + type ProofreaderLanguageCode"
      pattern: "from\\s+['\"]\\.\\./services/ProofreaderService['\"]"
    - from: "chat/src/app/components/Proofreader/ProofreaderPage.tsx"
      to: "chat/src/app/components/MissingFlagBanner.tsx"
      via: "named import (the Plan 08-01 moved path)"
      pattern: "from\\s+['\"]\\.\\./MissingFlagBanner['\"]"
    - from: "chat/src/app/AppRouter.tsx"
      to: "chat/src/app/components/Proofreader/ProofreaderPage.tsx"
      via: "named import + Route element"
      pattern: "path=\"/proofreader\""
    - from: "chat/src/app/components/Proofreader/ProofreaderPage.tsx"
      to: "chat/src/app/hooks/useSEOData.ts"
      via: "useSEOData(seoConfigs.proofreader | seoConfigs.proofreaderDocs, ...)"
      pattern: "seoConfigs\\.(proofreader|proofreaderDocs)"
    - from: "chat/scripts/prerender-react.js"
      to: "chat/src/app/hooks/useSEOData.ts"
      via: "duplicated seoConfigs string literals (byte-identical mirror — Phase 12 grep-F audits)"
      pattern: "Proofreader — Chrome on-device grammar"
---

<objective>
Ship the `/proofreader` and `/proofreader/docs` route surface on top of the Plan 08-01 foundation: page component subtree (Page, Header, Form, OutputModeToggle, ResultPanel) under `chat/src/app/components/Proofreader/`, AppRouter route + desktop + mobile nav link registration, and dual-write SEO entries in `useSEOData.ts` + `prerender-react.js`.

Purpose: PROOF-01 (route + nav + banner), PROOF-03 (textarea + button + result render), PROOF-05 (language selector + localStorage persistence), and PROOF-06 (graceful error handling) — the four remaining Phase 8 requirements after 08-01 delivered the typed service surface. Mirrors the v1.1 GenerativeUIPage shape (path-aware SEO, max-w-6xl wrapper, Tabs with Docs first) and the WriteRewritePage form+result layout pattern, but as a single-column workbench instead of two-column.

Output: 5 new files under `chat/src/app/components/Proofreader/`, 3 modified files (AppRouter.tsx, useSEOData.ts, prerender-react.js). Phase 9 layers the two disabled output modes; Phase 12 fills the Docs placeholder and runs the byte-identical SEO audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-proofreader-foundation/08-CONTEXT.md
@.planning/phases/08-proofreader-foundation/08-01-PLAN.md
@.planning/research/v1.2-multimodal-proofreader-api.md

@chat/src/app/components/GenerativeUIPage.tsx
@chat/src/app/components/GenerativeUI/GenerativeUIHeader.tsx
@chat/src/app/components/WriteRewritePage.tsx
@chat/src/app/components/Tabs.tsx
@chat/src/app/AppRouter.tsx
@chat/src/app/hooks/useSEOData.ts
@chat/scripts/prerender-react.js

<interfaces>
<!-- Contracts that executor must consume directly. No codebase exploration needed. -->

From Plan 08-01 (already landed — wave 1 outputs):

- `chat/src/app/services/ProofreaderService.ts` exports:
  - `proofread(text: string, opts?: { language?: ProofreaderLanguageCode; signal?: AbortSignal }): Promise<ProofreadResult>`
  - `getAvailability(language?: ProofreaderLanguageCode): Promise<'available' | 'downloadable' | 'downloading' | 'unavailable'>`
  - `createWithProgress(language: ProofreaderLanguageCode, onProgress: (pct: number) => void): Promise<Proofreader>`
  - `destroyAllSessions(): void`
  - `LANGUAGE_OPTIONS: ReadonlyArray<{ code: ProofreaderLanguageCode; label: string }>` — order is English, Spanish, Japanese, German, French
  - `LOCAL_STORAGE_KEY = 'window-ai.proofreader.language'`
  - `type ProofreaderLanguageCode = 'en' | 'es' | 'ja' | 'de' | 'fr'`

- `chat/src/app/components/MissingFlagBanner.tsx` named export:
  - `MissingFlagBanner: React.FC<{ title?: string; body?: string; flags?: Array<{ name: string; url: string; note?: string }>; browserRequirement?: string }>` — defaults preserve v1.0 WebMCP banner copy. For `/proofreader`, pass all four props explicitly (locked values per CONTEXT.md §"/proofreader banner specifics"):
    - title: `"Proofreader API isn't enabled in this browser."`
    - body: `"Enable the flags below in Chrome 146+ Canary, then reload."`
    - flags: two entries — `chrome://flags/#optimization-guide-on-device-model` (note `set to "Enabled BypassPerfRequirement"`) and `chrome://flags/#proofreader-api-for-gemini-nano` (note `set to "Enabled"`)
    - browserRequirement: `"Chrome 146+ Canary"`

- Ambient types from `chat/src/app/types/dom-chromium-ai.d.ts`:
  - `ProofreadResult = { correctedInput: string; corrections: ProofreaderCorrection[] }`
  - `ProofreaderCorrection = { startIndex: number; endIndex: number; correction: string; types?: ProofreaderCorrectionType[]; explanation?: string }`
  - `window.Proofreader: ProofreaderConstructor`

From `chat/src/app/components/Tabs.tsx` (existing, unchanged):
- `interface Tab { id: string; label: string; content: React.ReactNode; path?: string }`
- `interface TabsProps { tabs: Tab[]; defaultTab?: string; basePath?: string }`
- Matching logic: `tabs.find(t => t.path && currentPath.includes(t.path))` — Docs path `/docs` must come FIRST in the array so it wins over workbench's empty path. Mirror GenerativeUIPage.tsx lines 68-93 ordering exactly.

From `chat/src/app/components/GenerativeUIPage.tsx` (path-aware SEO pattern to mirror, lines 14-25 and 95-107):
- `const location = useLocation()`
- `const isDocs = location.pathname.startsWith('/proofreader/docs')` — startsWith, NOT includes (RESEARCH Pitfall 6)
- `useSEOData(isDocs ? seoConfigs.proofreaderDocs : seoConfigs.proofreader, isDocs ? '/proofreader/docs' : '/proofreader')`
- Outer wrapper: `<div className="min-h-screen bg-white dark:bg-gray-800 transition-colors duration-200"><div className="max-w-6xl mx-auto p-4"> ... </div></div>`
- Tabs invocation: `<Tabs basePath="/proofreader" defaultTab="workbench" tabs={tabs} />`

From `chat/src/app/AppRouter.tsx` (insertion points):
- Line 11: `import {GenerativeUIPage}` — add `import {ProofreaderPage} from './components/Proofreader/ProofreaderPage';` AFTER this line.
- Line 74-76 (desktop nav): the existing `<Link to="/generative-ui" ...>Generative UI</Link>` block — insert a near-identical `<Link to="/proofreader" ...>Proofreader</Link>` AFTER it with `trackUserInteraction('navigation_click', 'proofreader_link')`. Preserve `font-medium`, `text-sm`, transition classes verbatim.
- Line 167-169 (mobile nav): same insertion AFTER the mobile `<Link to="/generative-ui" ...>` block with `trackUserInteraction('navigation_click', 'proofreader_link_mobile')`. Use `text-base font-medium` to match the surrounding mobile-nav class string.
- Line 243-244 (Routes): the two `<Route path="/generative-ui" ... />` and `<Route path="/generative-ui/docs" ... />` entries — insert directly after them:
  - `<Route path="/proofreader" element={<ProofreaderPage/>}/>`
  - `<Route path="/proofreader/docs" element={<ProofreaderPage/>}/>`

From `chat/src/app/hooks/useSEOData.ts` (insertion site — the `seoConfigs` const ends at line 93 with the `generativeUIDocs` entry; insert the two new keys before the closing `}` on line 93):
- Locked title for `seoConfigs.proofreader`: `"Proofreader — Chrome on-device grammar + spelling correction | Chrome AI APIs"`
- Locked description: `"Gemini Nano proofreads your text on-device with grammar, spelling, capitalization, and punctuation corrections. Three output styles, five languages, zero network."`
- Locked title for `seoConfigs.proofreaderDocs`: `"Proofreader API Docs — surface, corrections shape, language support | Chrome AI APIs"`
- Locked description for proofreaderDocs: SAME as the proofreader page description (per planning_context — Phase 12 may revise, this plan ships them identical to minimize drift surface).
- Keywords for proofreader (planner's discretion, mirror tone of `generativeUI` entry): `"Proofreader API, Gemini Nano, on-device grammar, spelling correction, Chrome AI, ProofreaderCorrection, correctionExplanationLanguage, LoRA adapter"`
- Keywords for proofreaderDocs: same keywords or near-identical (planner's discretion).

From `chat/scripts/prerender-react.js` (insertion sites):
- Lines 60-63: the `// Generative UI routes` block in the `routes` array — add directly after it:
  - `// Proofreader routes` comment
  - `{ path: '/proofreader', filename: 'proofreader.html' },`
  - `{ path: '/proofreader/docs', filename: 'proofreader-docs.html' },`
- Lines 401-411: the `'/generative-ui/docs'` entry inside `getSEODataForRoute`'s `seoConfigs` — add two new entries `'/proofreader'` and `'/proofreader/docs'` AFTER it, BEFORE the closing `};` on line 412. Title + description strings MUST be byte-identical to the `useSEOData.ts` entries (Phase 12 audits with `grep -F`). `structuredData` field: mirror the `'/generative-ui'` block's `'@type': 'WebPage'` for the page route, and `'@type': 'TechArticle'` for the docs route.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the Proofreader component subtree (5 files under chat/src/app/components/Proofreader/)</name>
  <files>chat/src/app/components/Proofreader/ProofreaderPage.tsx, chat/src/app/components/Proofreader/ProofreaderHeader.tsx, chat/src/app/components/Proofreader/ProofreaderForm.tsx, chat/src/app/components/Proofreader/ProofreaderOutputModeToggle.tsx, chat/src/app/components/Proofreader/ProofreaderResultPanel.tsx</files>
  <read_first>
    - chat/src/app/components/GenerativeUIPage.tsx (full file — mirror useLocation + path-aware SEO + Tabs ordering + max-w-6xl wrapper)
    - chat/src/app/components/GenerativeUI/GenerativeUIHeader.tsx (full file — mirror gradient icon block + title + tagline shape for ProofreaderHeader)
    - chat/src/app/components/WriteRewritePage.tsx (form + result layout — mirror textarea + button styling and the writtenContent error rendering)
    - chat/src/app/components/Tabs.tsx (full file — confirm path matching order; Docs must be FIRST in array)
    - chat/src/app/services/ProofreaderService.ts (after Plan 08-01 — the public surface this page consumes)
    - chat/src/app/components/MissingFlagBanner.tsx (after Plan 08-01 — the prop shape this page invokes)
    - .planning/phases/08-proofreader-foundation/08-CONTEXT.md (decisions §"Page shell + routing + tabs", §"Error UX", §"/proofreader banner specifics" — locked copy + page-state machine)
    - .planning/research/v1.2-multimodal-proofreader-api.md (§1 "Canonical usage pattern", §1 "Model download behavior" — informs progress-bar UX)
  </read_first>
  <action>
    Create `chat/src/app/components/Proofreader/` and add five new files. (a) `ProofreaderHeader.tsx` — a zero-prop React.FC that mirrors `GenerativeUIHeader.tsx` exactly in shape: outer `<header className="mb-8">` with a flex row containing a gradient icon block (`bg-gradient-to-br from-primary-500 to-purple-600`, `w-12 h-12 rounded-xl`, white SVG inside — use the heroicons "document-check" or "pencil-square" path), then a div with `<h1 className="text-3xl font-bold text-gray-900 dark:text-white leading-tight">Proofreader</h1>` and `<p className="mt-2 text-base text-gray-600 dark:text-gray-400">On-device grammar + spelling correction with Gemini Nano — five languages, three output styles, zero network.</p>`. (b) `ProofreaderForm.tsx` — React.FC accepting props `{ defaultText: string; defaultLanguage: ProofreaderLanguageCode; disabled: boolean; isSubmitting: boolean; onProofread: (text: string, language: ProofreaderLanguageCode) => void; onLanguageChange: (lang: ProofreaderLanguageCode) => void; }`. Internal state: `text` controlled via `useState(defaultText)`, `language` controlled via `useState(defaultLanguage)`. On `language` change, call `onLanguageChange(lang)` so the parent persists to localStorage. Render a `<form onSubmit={e => { e.preventDefault(); onProofread(text, language); }}>` with: a `<textarea rows={6}>` styled with the same Tailwind class string used by `WriteRewritePage.tsx`'s `writingPrompt` textarea (border, focus ring, dark-mode bg, resize-none), placeholder `"Paste or type some text to proofread…"`, plus a flex row containing `<select>` (id `proofreaderLanguage`, label "Language") populated from `LANGUAGE_OPTIONS` (option value=code, label=label) and a Proofread button (`bg-primary-600 hover:bg-primary-700 disabled:bg-gray-400 text-white px-4 py-3 rounded-lg font-medium disabled:cursor-not-allowed`). Button label switches between `"Proofread"` / `"Proofreading..."` based on `isSubmitting`; button is `disabled={disabled || isSubmitting || !text.trim()}`. (c) `ProofreaderOutputModeToggle.tsx` — export `type OutputMode = 'plain' | 'side-by-side' | 'inline-strikethrough'` and a React.FC `{ mode: OutputMode; onChange: (m: OutputMode) => void }`. Render a segmented control: outer `<div role="tablist" className="inline-flex rounded-lg bg-gray-200 dark:bg-gray-700 p-1">` containing three buttons. The 'plain' button is always enabled and uses the active-pill styling from WriteRewritePage's writer/rewriter toggle (`bg-white dark:bg-gray-600 text-gray-900 dark:text-white shadow` when active). The 'side-by-side' and 'inline-strikethrough' buttons set `disabled` AND `title="Coming in Phase 9"` AND `className` adds `opacity-50 cursor-not-allowed`. Phase 8 hard-codes that only 'plain' can be active even if a future caller passes another value (defensively coerce to 'plain'). (d) `ProofreaderResultPanel.tsx` — React.FC `{ result: ProofreadResult | null; mode: OutputMode; originalText: string; error: string | null; onRetry: () => void; }`. If `error` is non-null: render an inline error card (`bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 rounded-lg p-4 mt-6`) with the message and a Retry button (`bg-red-600 hover:bg-red-700 text-white px-3 py-2 rounded-md text-sm`); the Retry button calls `onRetry()`. If `result` is null and no error: render a muted placeholder card "Results will appear here." If `mode` !== 'plain': render a placeholder `<div className="mt-6 p-4 rounded-lg border border-dashed border-gray-300 dark:border-gray-600 text-gray-500 dark:text-gray-400">Mode coming in Phase 9 — switch to 'Plain + list' to view this result.</div>`. If `mode === 'plain'` and `result` is present: render a card with the heading "Corrected text" then `<p className="whitespace-pre-wrap">{result.correctedInput}</p>`, then heading "Suggestions" then a `<ul className="list-disc list-inside space-y-1 text-sm">`. For each `c` in `result.corrections`, compute `const original = originalText.slice(c.startIndex, c.endIndex);` and render `<li>changed "{original}" → "{c.correction}" {c.explanation ? `because ${c.explanation}` : c.types?.length ? `[${c.types.join(', ')}]` : ''}</li>`. (e) `ProofreaderPage.tsx` — top-level React.FC mirroring GenerativeUIPage. Imports: `React, { useEffect, useMemo, useState, useCallback }`, `useLocation` from react-router-dom, `useSEOData, seoConfigs` from `../../hooks/useSEOData`, `MissingFlagBanner` from `../MissingFlagBanner`, `Tabs` from `../Tabs`, `ProofreaderHeader`, `ProofreaderForm`, `ProofreaderOutputModeToggle, type OutputMode`, `ProofreaderResultPanel`, and from `../../services/ProofreaderService`: `getAvailability, proofread, createWithProgress, LANGUAGE_OPTIONS, LOCAL_STORAGE_KEY, type ProofreaderLanguageCode`. Page state: `type PageState = 'idle' | 'unavailable' | 'downloading' | 'ready' | 'proofreading' | 'error'` (locked per CONTEXT specifics §3); `const [pageState, setPageState] = useState<PageState>('idle')`. Result state: `const [result, setResult] = useState<ProofreadResult | null>(null)`, `const [error, setError] = useState<string | null>(null)`, `const [originalText, setOriginalText] = useState('')`, `const [downloadPct, setDownloadPct] = useState(0)`, `const [language, setLanguage] = useState<ProofreaderLanguageCode>('en')`, `const [mode, setMode] = useState<OutputMode>('plain')`. Path-aware SEO: `const location = useLocation(); const isDocs = location.pathname.startsWith('/proofreader/docs'); useSEOData(isDocs ? seoConfigs.proofreaderDocs : seoConfigs.proofreader, isDocs ? '/proofreader/docs' : '/proofreader');`. Mount effect 1 (read localStorage): `useEffect(() => { try { const stored = localStorage.getItem(LOCAL_STORAGE_KEY); if (stored && LANGUAGE_OPTIONS.some(o => o.code === stored)) setLanguage(stored as ProofreaderLanguageCode); } catch { /* private mode / SSR no-op */ } }, []);`. Mount effect 2 (availability check, StrictMode-safe with `cancelled` flag mirroring GenerativeUIPage.tsx lines 29-45): call `getAvailability(language)`; if `'unavailable'` → setPageState('unavailable'); if `'available'` → setPageState('ready'); if `'downloadable' | 'downloading'` → setPageState('downloading') and call `createWithProgress(language, pct => setDownloadPct(pct))`, then setPageState('ready') on resolve, setPageState('error') + setError on rejection. The locked default text constant (declare once at module scope or inside the page): `const DEFAULT_DEMO_TEXT = "i think there going to a meetting tommorow at the office. there progress have been great on the project!";`. `onLanguageChange` callback (memoized via useCallback): updates `language` state AND writes to `localStorage.setItem(LOCAL_STORAGE_KEY, lang)` inside a try/catch. `onProofread` callback (memoized): set pageState='proofreading', setError(null), setOriginalText(text), then `try { const r = await proofread(text, { language: lang }); setResult(r); setPageState('ready'); } catch (err) { setError(err instanceof Error ? err.message : 'Unknown error'); setPageState('error'); }`. `onRetry`: re-invoke `onProofread(originalText, language)` when originalText is truthy. Workbench content JSX: `<><ProofreaderForm defaultText={DEFAULT_DEMO_TEXT} defaultLanguage={language} disabled={pageState === 'unavailable'} isSubmitting={pageState === 'proofreading'} onProofread={onProofread} onLanguageChange={onLanguageChange} />{pageState === 'downloading' && <div className="mt-4"><div className="h-2 bg-gray-200 dark:bg-gray-700 rounded overflow-hidden"><div className="h-full bg-primary-500" style={{ width: `${downloadPct}%` }} /></div><p className="mt-1 text-xs text-gray-500 dark:text-gray-400">Downloading Proofreader model — {downloadPct.toFixed(0)}%</p></div>}<div className="mt-6"><ProofreaderOutputModeToggle mode={mode} onChange={setMode} /></div><ProofreaderResultPanel result={result} mode={mode} originalText={originalText} error={error} onRetry={onRetry} /></>`. Docs tab content: `<div className="max-w-none p-4 text-gray-500 dark:text-gray-400">Documentation coming in Phase 12.</div>`. Tabs array (memoized, Docs FIRST per v1.1 lesson): `[{ id: 'docs', label: 'Docs', path: '/docs', content: docsContent }, { id: 'workbench', label: 'Workbench', path: '', content: workbenchContent }]`. Outer render: `<div className="min-h-screen bg-white dark:bg-gray-800 transition-colors duration-200"><div className="max-w-6xl mx-auto p-4">{pageState === 'unavailable' && <MissingFlagBanner title="Proofreader API isn't enabled in this browser." body="Enable the flags below in Chrome 146+ Canary, then reload." flags={[{ name: 'Optimization Guide On Device', url: 'chrome://flags/#optimization-guide-on-device-model', note: 'set to "Enabled BypassPerfRequirement"' }, { name: 'Proofreader API', url: 'chrome://flags/#proofreader-api-for-gemini-nano', note: 'set to "Enabled"' }]} browserRequirement="Chrome 146+ Canary" />}<ProofreaderHeader /><Tabs basePath="/proofreader" defaultTab="workbench" tabs={tabs} /><p className="mt-4 text-center text-xs text-gray-500 dark:text-gray-400">🔒 Zero network during demo — open DevTools → Network tab</p></div></div>`. No `any`, no `as any`. No fenced code blocks inside JSX comments — keep any inline comments to one or two lines. Mirror StrictMode-safe `cancelled` flag idiom from GenerativeUIPage.tsx for the availability mount effect.
  </action>
  <acceptance_criteria>
    - All 5 files exist under `chat/src/app/components/Proofreader/`
    - `grep -c "export const ProofreaderPage" chat/src/app/components/Proofreader/ProofreaderPage.tsx` returns at least 1
    - `grep -c "export const ProofreaderHeader" chat/src/app/components/Proofreader/ProofreaderHeader.tsx` returns at least 1
    - `grep -c "export const ProofreaderForm" chat/src/app/components/Proofreader/ProofreaderForm.tsx` returns at least 1
    - `grep -c "export const ProofreaderOutputModeToggle" chat/src/app/components/Proofreader/ProofreaderOutputModeToggle.tsx` returns at least 1
    - `grep -c "export type OutputMode" chat/src/app/components/Proofreader/ProofreaderOutputModeToggle.tsx` returns at least 1
    - `grep -c "export const ProofreaderResultPanel" chat/src/app/components/Proofreader/ProofreaderResultPanel.tsx` returns at least 1
    - `grep -c "i think there going to a meetting tommorow" chat/src/app/components/Proofreader/ProofreaderPage.tsx` returns at least 1 (locked DEFAULT_DEMO_TEXT verbatim)
    - `grep -c "window-ai.proofreader.language" chat/src/app/components/Proofreader/ProofreaderPage.tsx` returns 0 — the page must import `LOCAL_STORAGE_KEY` from ProofreaderService, NOT duplicate the literal
    - `grep -c "LOCAL_STORAGE_KEY" chat/src/app/components/Proofreader/ProofreaderPage.tsx` returns at least 2 (import + at least one usage)
    - `grep -c "useLocation" chat/src/app/components/Proofreader/ProofreaderPage.tsx` returns at least 1
    - `grep -c "startsWith('/proofreader/docs')" chat/src/app/components/Proofreader/ProofreaderPage.tsx` returns 1 (NOT includes per RESEARCH Pitfall 6)
    - `grep -c "max-w-6xl mx-auto p-4" chat/src/app/components/Proofreader/ProofreaderPage.tsx` returns at least 1
    - `grep -c "Coming in Phase 9" chat/src/app/components/Proofreader/ProofreaderOutputModeToggle.tsx` returns at least 2 (tooltip on both disabled buttons)
    - `grep -c "Documentation coming in Phase 12" chat/src/app/components/Proofreader/ProofreaderPage.tsx` returns at least 1
    - `grep -c "Proofreader API isn't enabled in this browser." chat/src/app/components/Proofreader/ProofreaderPage.tsx` returns at least 1 (locked banner title passed as prop)
    - `grep -c "chrome://flags/#optimization-guide-on-device-model" chat/src/app/components/Proofreader/ProofreaderPage.tsx` returns at least 1
    - `grep -c "chrome://flags/#proofreader-api-for-gemini-nano" chat/src/app/components/Proofreader/ProofreaderPage.tsx` returns at least 1
    - `grep -RcE ': any|as any' chat/src/app/components/Proofreader/ | grep -v ':0$' | wc -l | xargs -I{} test {} -eq 0` (no `: any` or `as any` in any Proofreader component)
    - `npx nx build chat` exits 0
    - `npx nx lint chat` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f chat/src/app/components/Proofreader/ProofreaderPage.tsx && test -f chat/src/app/components/Proofreader/ProofreaderHeader.tsx && test -f chat/src/app/components/Proofreader/ProofreaderForm.tsx && test -f chat/src/app/components/Proofreader/ProofreaderOutputModeToggle.tsx && test -f chat/src/app/components/Proofreader/ProofreaderResultPanel.tsx && grep -RcE ': any|as any' chat/src/app/components/Proofreader/ | awk -F: '{s+=$2} END {exit (s>0)}' && npx nx build chat 2>&1 | tail -20 | grep -iE 'error|TS[0-9]+' | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>
    Five Proofreader components compile cleanly; ProofreaderPage owns the page-state machine, path-aware SEO, availability detection, and download progress; Tabs renders Docs FIRST with the locked Phase 12 placeholder copy; banner copy is passed as explicit props with both locked Chrome flag URLs; default demo text is the locked sentence; localStorage key is imported (not duplicated). No new `any` introduced. Build + lint pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register /proofreader + /proofreader/docs routes and add desktop + mobile nav links in AppRouter.tsx</name>
  <files>chat/src/app/AppRouter.tsx</files>
  <read_first>
    - chat/src/app/AppRouter.tsx (lines 1-15 imports; lines 70-80 desktop nav block; lines 160-175 mobile nav block; lines 240-250 Routes block)
    - chat/src/app/components/Proofreader/ProofreaderPage.tsx (after Task 1 — the exported component this file imports)
    - .planning/phases/08-proofreader-foundation/08-CONTEXT.md (decisions §"Page shell + routing + tabs" — locked nav placement AFTER Generative UI)
  </read_first>
  <action>
    Make four additive edits to `chat/src/app/AppRouter.tsx`. (a) Add `import {ProofreaderPage} from './components/Proofreader/ProofreaderPage';` directly AFTER the existing `import {GenerativeUIPage}` line (currently line 11). Match the curly-brace and quote style of the surrounding imports exactly. (b) In the desktop nav block, directly AFTER the existing `<Link to="/generative-ui" ...>Generative UI</Link>` block (currently lines 74-76), insert a near-identical block with `to="/proofreader"`, the same Tailwind class string verbatim, `onClick={() => trackUserInteraction('navigation_click', 'proofreader_link')}` and label `Proofreader`. (c) In the mobile nav block, directly AFTER the existing mobile `<Link to="/generative-ui" ...>Generative UI</Link>` block (currently lines 167-169), insert a near-identical block with `to="/proofreader"`, the same mobile-nav Tailwind class string verbatim (`text-base font-medium`, `block px-3 py-2`), `onClick={() => trackUserInteraction('navigation_click', 'proofreader_link_mobile')}` and label `Proofreader`. (d) In the Routes block, directly AFTER the existing `<Route path="/generative-ui/docs" element={<GenerativeUIPage/>}/>` line (currently line 244), insert two new routes: `<Route path="/proofreader" element={<ProofreaderPage/>}/>` and `<Route path="/proofreader/docs" element={<ProofreaderPage/>}/>`. Wrap the two new routes with a comment line `{/* Proofreader routes */}` matching the surrounding comment style. Do NOT modify the order or content of any other nav link, route, or import. Do NOT touch the writer/rewriter, recipe-workbench, or generative-ui blocks.
  </action>
  <acceptance_criteria>
    - `grep -c "import {ProofreaderPage}" chat/src/app/AppRouter.tsx` returns 1
    - `grep -c 'to="/proofreader"' chat/src/app/AppRouter.tsx` returns at least 2 (desktop + mobile nav link)
    - `grep -c 'path="/proofreader"' chat/src/app/AppRouter.tsx` returns 1
    - `grep -c 'path="/proofreader/docs"' chat/src/app/AppRouter.tsx` returns 1
    - `grep -c "proofreader_link" chat/src/app/AppRouter.tsx` returns at least 1 (desktop tracking)
    - `grep -c "proofreader_link_mobile" chat/src/app/AppRouter.tsx` returns at least 1 (mobile tracking)
    - `grep -c "ProofreaderPage" chat/src/app/AppRouter.tsx` returns at least 3 (import + 2 Route element refs)
    - Sanity check that existing routes were NOT removed: `grep -c 'path="/generative-ui"' chat/src/app/AppRouter.tsx` returns 1; `grep -c 'path="/webmcp"' chat/src/app/AppRouter.tsx` returns 1
    - `npx nx build chat` exits 0
    - `npx nx lint chat` exits 0
    - Manual smoke (recorded by executor in the summary): `npx nx serve chat`, visit `http://localhost:4300/proofreader` — page mounts (banner shown without Chrome flags); `http://localhost:4300/proofreader/docs` — page mounts with Docs tab active and the placeholder text; desktop nav shows "Proofreader" between "Generative UI" and "Writer/Rewriter"; mobile menu (hamburger) shows the same.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "import {ProofreaderPage}" chat/src/app/AppRouter.tsx | xargs -I{} test {} -eq 1 && grep -c 'path="/proofreader"' chat/src/app/AppRouter.tsx | xargs -I{} test {} -eq 1 && grep -c 'path="/proofreader/docs"' chat/src/app/AppRouter.tsx | xargs -I{} test {} -eq 1 && grep -c 'to="/proofreader"' chat/src/app/AppRouter.tsx | xargs -I{} test {} -ge 2 && grep -c 'path="/generative-ui"' chat/src/app/AppRouter.tsx | xargs -I{} test {} -eq 1 && npx nx build chat 2>&1 | tail -20 | grep -iE 'error|TS[0-9]+' | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>
    AppRouter.tsx registers `/proofreader` + `/proofreader/docs` as Routes, imports ProofreaderPage, and exposes desktop + mobile nav links positioned directly AFTER the Generative UI links with correct tracking-event names. No existing v1.0 / v1.1 nav/route entries were removed or reordered. Build + lint pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add seoConfigs.proofreader + seoConfigs.proofreaderDocs to useSEOData.ts AND prerender-react.js (byte-identical mirror)</name>
  <files>chat/src/app/hooks/useSEOData.ts, chat/scripts/prerender-react.js</files>
  <read_first>
    - chat/src/app/hooks/useSEOData.ts (lines 30-94 — the full `seoConfigs` object; mirror the `generativeUI` + `generativeUIDocs` entry shape)
    - chat/scripts/prerender-react.js (lines 25-65 routes array; lines 120-415 `getSEODataForRoute` function — mirror the `/generative-ui` + `/generative-ui/docs` entries)
    - .planning/phases/08-proofreader-foundation/08-CONTEXT.md (decisions §"SEO + page metadata" — locked title + description strings)
  </read_first>
  <action>
    Two parallel edits. (a) In `chat/src/app/hooks/useSEOData.ts`, inside the `seoConfigs` const (currently ends with `generativeUIDocs` at line 92 closing brace then `}` at line 93), insert two new entries directly after `generativeUIDocs`, before the closing `}`. Add a comment line above each entry matching the existing pattern: `// Must match prerender-react.js seoConfigs['/proofreader'] verbatim — Phase 12 grep -F audits.` and similarly for the docs entry. Field values (locked):
    - `proofreader: { title: 'Proofreader — Chrome on-device grammar + spelling correction | Chrome AI APIs', description: 'Gemini Nano proofreads your text on-device with grammar, spelling, capitalization, and punctuation corrections. Three output styles, five languages, zero network.', keywords: 'Proofreader API, Gemini Nano, on-device grammar, spelling correction, Chrome AI, ProofreaderCorrection, correctionExplanationLanguage, LoRA adapter' }`
    - `proofreaderDocs: { title: 'Proofreader API Docs — surface, corrections shape, language support | Chrome AI APIs', description: 'Gemini Nano proofreads your text on-device with grammar, spelling, capitalization, and punctuation corrections. Three output styles, five languages, zero network.', keywords: 'Proofreader API docs, ProofreaderCorrection, ProofreadResult, correctionExplanationLanguage, expectedInputLanguages, Chrome 146 Canary, on-device AI' }`
    Match the existing single-quote convention used in the file. Do NOT change any pre-existing `seoConfigs` entry. (b) In `chat/scripts/prerender-react.js`, perform two parallel inserts:
    1) Inside the `routes` array (currently lines 25-63), directly AFTER the `// Generative UI routes` block (the two `{path: '/generative-ui', ...}` and `{path: '/generative-ui/docs', ...}` lines around lines 60-62), insert:
       - `// Proofreader routes`
       - `{ path: '/proofreader', filename: 'proofreader.html' },`
       - `{ path: '/proofreader/docs', filename: 'proofreader-docs.html' },`
    2) Inside `getSEODataForRoute`'s inline `seoConfigs` map (currently ends at line 411 with the `'/generative-ui/docs'` entry then closing `};` on line 412), insert two new entries directly AFTER the `'/generative-ui/docs'` entry, before the closing `};`:
       - `'/proofreader': { title: 'Proofreader — Chrome on-device grammar + spelling correction | Chrome AI APIs', description: 'Gemini Nano proofreads your text on-device with grammar, spelling, capitalization, and punctuation corrections. Three output styles, five languages, zero network.', keywords: 'Proofreader API, Gemini Nano, on-device grammar, spelling correction, Chrome AI, ProofreaderCorrection, correctionExplanationLanguage, LoRA adapter', structuredData: { '@context': 'https://schema.org', '@type': 'WebPage', name: 'Proofreader Demo', description: 'On-device grammar and spelling correction with Gemini Nano' } }`
       - `'/proofreader/docs': { title: 'Proofreader API Docs — surface, corrections shape, language support | Chrome AI APIs', description: 'Gemini Nano proofreads your text on-device with grammar, spelling, capitalization, and punctuation corrections. Three output styles, five languages, zero network.', keywords: 'Proofreader API docs, ProofreaderCorrection, ProofreadResult, correctionExplanationLanguage, expectedInputLanguages, Chrome 146 Canary, on-device AI', structuredData: { '@context': 'https://schema.org', '@type': 'TechArticle', name: 'Proofreader API Documentation', description: 'Technical documentation for the Proofreader API surface, corrections shape, and language support' } }`
    Match the existing single-quote + trailing-comma style. The title and description strings in (a) and (b) MUST be byte-identical for both routes — copy them once into a scratch buffer and paste into both files to avoid drift. Do NOT modify any existing route entry, any existing SEO entry, the `createSitemap`, `createRobotsTxt`, or `createEnhancedHTML` functions. The em-dash (—) is U+2014; copy from the existing `generativeUI` entry's title to ensure the same code point lands in both files.
  </action>
  <acceptance_criteria>
    - `grep -c "proofreader:" chat/src/app/hooks/useSEOData.ts` returns at least 1
    - `grep -c "proofreaderDocs:" chat/src/app/hooks/useSEOData.ts` returns at least 1
    - `grep -c "'/proofreader'" chat/scripts/prerender-react.js` returns at least 2 (routes array entry + seoConfigs key)
    - `grep -c "'/proofreader/docs'" chat/scripts/prerender-react.js` returns at least 2 (routes array entry + seoConfigs key)
    - `grep -c "proofreader.html" chat/scripts/prerender-react.js` returns at least 1
    - `grep -c "proofreader-docs.html" chat/scripts/prerender-react.js` returns at least 1
    - **Byte-identical title check:** `grep -F "Proofreader — Chrome on-device grammar + spelling correction | Chrome AI APIs" chat/src/app/hooks/useSEOData.ts` returns at least 1 hit AND `grep -F "Proofreader — Chrome on-device grammar + spelling correction | Chrome AI APIs" chat/scripts/prerender-react.js` returns at least 1 hit
    - **Byte-identical docs title check:** `grep -F "Proofreader API Docs — surface, corrections shape, language support | Chrome AI APIs" chat/src/app/hooks/useSEOData.ts` returns at least 1 AND same grep against `chat/scripts/prerender-react.js` returns at least 1
    - **Byte-identical description check:** `grep -F "Gemini Nano proofreads your text on-device with grammar, spelling, capitalization, and punctuation corrections. Three output styles, five languages, zero network." chat/src/app/hooks/useSEOData.ts` returns at least 2 (one for each entry) AND same grep against `chat/scripts/prerender-react.js` returns at least 2
    - Sanity check that existing entries were NOT removed: `grep -c "generativeUI:" chat/src/app/hooks/useSEOData.ts` returns 1; `grep -c "'/generative-ui'" chat/scripts/prerender-react.js` returns at least 2
    - `npx nx build chat` exits 0 (TypeScript validates the new seoConfigs entries match the existing inferred const shape)
    - `npx nx lint chat` exits 0
    - Manual smoke (recorded by executor in summary): after build, `node chat/scripts/prerender-react.js` runs without throwing AND `chat/dist/chat/proofreader.html` exists AND contains the locked title string.
  </acceptance_criteria>
  <verify>
    <automated>grep -F "Proofreader — Chrome on-device grammar + spelling correction | Chrome AI APIs" chat/src/app/hooks/useSEOData.ts | wc -l | xargs -I{} test {} -ge 1 && grep -F "Proofreader — Chrome on-device grammar + spelling correction | Chrome AI APIs" chat/scripts/prerender-react.js | wc -l | xargs -I{} test {} -ge 1 && grep -F "Proofreader API Docs — surface, corrections shape, language support | Chrome AI APIs" chat/src/app/hooks/useSEOData.ts | wc -l | xargs -I{} test {} -ge 1 && grep -F "Proofreader API Docs — surface, corrections shape, language support | Chrome AI APIs" chat/scripts/prerender-react.js | wc -l | xargs -I{} test {} -ge 1 && grep -c "'/proofreader'" chat/scripts/prerender-react.js | xargs -I{} test {} -ge 2 && grep -c "'/proofreader/docs'" chat/scripts/prerender-react.js | xargs -I{} test {} -ge 2 && npx nx build chat 2>&1 | tail -20 | grep -iE 'error|TS[0-9]+' | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>
    `seoConfigs.proofreader` and `seoConfigs.proofreaderDocs` are present in both `useSEOData.ts` and `prerender-react.js` with byte-identical title + description strings; the routes array in `prerender-react.js` includes both proofreader entries with their HTML filenames; em-dash code point matches between files. Phase 12's `grep -F` audit is set up to pass. Build + lint pass.
  </done>
</task>

</tasks>

<verification>
- TypeScript: `npx nx build chat` exits 0 (catches missing exports, prop mismatches, and `any` regressions).
- Lint: `npx nx lint chat` exits 0.
- Byte-identical SEO mirror (set up for Phase 12 audit):
  - `grep -F "Proofreader — Chrome on-device grammar + spelling correction | Chrome AI APIs" chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js` returns at least 1 hit per file.
  - `grep -F "Proofreader API Docs — surface, corrections shape, language support | Chrome AI APIs" chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js` returns at least 1 hit per file.
  - `grep -F "Gemini Nano proofreads your text on-device with grammar, spelling, capitalization, and punctuation corrections. Three output styles, five languages, zero network." chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js` returns at least 2 hits per file (one per route).
- Page smoke without flags (Canary, Proofreader undefined): `/proofreader` loads, `MissingFlagBanner` visible with both flag URLs in `<code>` chips, form is disabled, page does NOT crash.
- Page smoke with flags (Canary, Proofreader available): `/proofreader` loads with default textarea content visible, language selector defaults to 'en' (or last-stored language from localStorage), clicking Proofread surfaces a `correctedInput` paragraph + suggestions list within the result panel.
- Page smoke (download path): if `getAvailability()` returns 'downloadable', a progress bar appears and ticks; pageState transitions to 'ready' once the download resolves.
- Tabs: visiting `/proofreader` selects the Workbench tab; visiting `/proofreader/docs` selects the Docs tab with the "Documentation coming in Phase 12" placeholder. Clicking the Docs tab from `/proofreader` navigates to `/proofreader/docs` (Tabs internal navigate).
- Nav links: visible in desktop nav between "Generative UI" and "Writer/Rewriter"; visible in mobile menu after "Generative UI"; trackUserInteraction calls fire on click.
- localStorage persistence: change language → reload → previously-selected language is pre-selected in the `<select>`.
- Error path: temporarily throw inside `ProofreaderService.proofread` (or pass invalid input that triggers a DOMException) → the result panel renders the inline error card with a Retry button; clicking Retry re-invokes the proofread call.
- Brownfield boundary: `git diff --stat` shows changes only to the 8 files listed in `files_modified` — no edits to `/webmcp`, `/generative-ui`, `RecipeWorkbench/*`, `mcp/`, `mcp-client/`, or `devops/awsweb/`.
</verification>

<success_criteria>
- All three task acceptance criteria pass.
- Phase 8 success criteria satisfied for PROOF-01 (route + nav + banner — criteria 1, 2), PROOF-03 (textarea + button + plain result — criterion 4), PROOF-05 (language selector + localStorage — criterion 5), and PROOF-06 (graceful error — criterion 6). Phase 8 success criterion 3 (download progress UX) is wired but its formal verification requires a real model download — recorded as executor's smoke note.
- ProofreaderOutputModeToggle ships with all three options visible; only 'plain' is enabled. Phase 9 enables the other two without touching the toggle component contract.
- SEO byte-identical mirror is shipped (set up for Phase 12 grep-F audit).
- No v1.0 (`/webmcp`) or v1.1 (`/generative-ui`) functional behavior changes. `RecipeWorkbench/*`, `mcp/`, `mcp-client/`, `devops/awsweb/` untouched.
- Phase 9 can build the alternate output modes by extending `ProofreaderResultPanel` (the 'side-by-side' and 'inline-strikethrough' branches) and enabling the two disabled buttons in `ProofreaderOutputModeToggle` — no service or page-shell changes required.
</success_criteria>

<output>
After completion, create `.planning/phases/08-proofreader-foundation/08-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Include: a `git diff --stat` of the 8 modified files, screenshots (or text-based descriptions) of `/proofreader` with and without the Chrome flags enabled, the `npx nx build chat` + `npx nx lint chat` exit codes, the result of running `node chat/scripts/prerender-react.js` (proofreader.html + proofreader-docs.html written), and a note confirming the desktop + mobile nav links are positioned directly after Generative UI.
</output>
