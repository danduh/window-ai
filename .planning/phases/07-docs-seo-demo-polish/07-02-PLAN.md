---
phase: 07-docs-seo-demo-polish
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts
  - chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts
  - chat/src/app/components/GenerativeUI/iframe/bridge.ts
  - chat/src/app/components/GenerativeUI/MealPlanColumn.tsx
  - chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx
  - chat/src/app/components/GenerativeUIPage.tsx
  - .planning/phases/07-docs-seo-demo-polish/REHEARSAL.md
autonomous: true
requirements: [GENUI-14, GENUI-15]
must_haves:
  truths:
    - "The Pick button at the bottom of each recipe card is fully visible inside the iframe (no clipping) on a fresh Chrome 146 Canary run"
    - "The MealPlanColumn displays each entry's recipe title (resolved via RecipePersistence.getRecipe), with the recipeId shown as a smaller meta line"
    - "When `plan.length > 0`, a 'Clear plan' button is visible in MealPlanColumn; clicking it calls MealPlanStore.clearPlan() and the list goes empty"
    - "While the chat agent awaits a LanguageModel response, a 'Thinking…' system-styled Message appears in the transcript and is removed when the reply arrives"
    - "All console.debug calls in the GenerativeUI bridge/iframe/chat code paths are gated behind `import.meta.env.DEV` so a production build is quiet"
    - "A subtle 'Zero network during demo' marker is rendered on /generative-ui pointing the audience at DevTools → Network"
    - "A REHEARSAL.md log template with 5 cold-run sections exists at .planning/phases/07-docs-seo-demo-polish/"
  artifacts:
    - path: "chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts"
      provides: "ResizeObserver now observes document.body and reads Math.max(scrollHeight, offsetHeight); console.debug calls dev-gated"
      contains: "document.body"
    - path: "chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts"
      provides: "Inner-body min-height safety net for the carousel"
      contains: "min-height: 200px"
    - path: "chat/src/app/components/GenerativeUI/iframe/bridge.ts"
      provides: "Host-side debug logs gated behind import.meta.env.DEV"
      contains: "import.meta.env.DEV"
    - path: "chat/src/app/components/GenerativeUI/MealPlanColumn.tsx"
      provides: "Title resolution + Clear plan button"
      contains: "Clear plan"
    - path: "chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx"
      provides: "Thinking… transient Message + dev-gated console.debug"
      contains: "Thinking"
    - path: "chat/src/app/components/GenerativeUIPage.tsx"
      provides: "Zero-network kicker marker text"
      contains: "Zero network"
    - path: ".planning/phases/07-docs-seo-demo-polish/REHEARSAL.md"
      provides: "Cold-run rehearsal log template (5 empty sections)"
      contains: "Cold run 5"
  key_links:
    - from: "chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts"
      to: "ResizeObserver"
      via: "ro.observe(document.body) inside the start() handshake-completion callback"
      pattern: "ro\\.observe\\(document\\.body\\)"
    - from: "chat/src/app/components/GenerativeUI/MealPlanColumn.tsx"
      to: "chat/src/app/services/RecipePersistence.ts"
      via: "getRecipe(entry.recipeId) on each plan row"
      pattern: "getRecipe"
    - from: "chat/src/app/components/GenerativeUI/MealPlanColumn.tsx"
      to: "chat/src/app/services/MealPlanStore.ts"
      via: "clearPlan() on button click"
      pattern: "clearPlan"
    - from: "chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx"
      to: "transcript"
      via: "addMessage('Thinking…', 'System') before session.prompt, then setMessages(prev => prev.filter(m => m.text !== 'Thinking…')) on first reply"
      pattern: "Thinking"
---

<objective>
Close the v1.1 demo by (a) fixing the Phase 5 Pick-button clipping carried into Phase 6, (b) shipping the four polish items locked in CONTEXT.md (titles in MealPlanColumn, Clear plan button, Thinking… loader, dev-gated debug logs), (c) placing the zero-network kicker marker on the page, and (d) creating REHEARSAL.md as the speaker's cold-run log template.

Purpose: Closes GENUI-14 (demo runs cleanly across 5 cold runs — REHEARSAL.md is where the speaker proves it) and GENUI-15 (zero outbound network during demo — visible marker on the page makes the kicker discoverable). Also removes the only blocker carried from Phase 5: clicking the Pick button must work for a real user, not just for `postMessage` injection.

Output: 6 modified files under `chat/src/app/components/GenerativeUI/` + GenerativeUIPage + 1 new file under `.planning/phases/07-docs-seo-demo-polish/`. No edits outside the GenerativeUI subtree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-docs-seo-demo-polish/07-CONTEXT.md
@.planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-VERIFICATION.md
@.planning/phases/06-in-page-chat-tool-wiring/06-VERIFICATION.md
@chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts
@chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts
@chat/src/app/components/GenerativeUI/iframe/bridge.ts
@chat/src/app/components/GenerativeUI/MealPlanColumn.tsx
@chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx
@chat/src/app/components/GenerativeUIPage.tsx
@chat/src/app/services/MealPlanStore.ts
@chat/src/app/services/RecipePersistence.ts
@chat/src/app/services/genUITools.ts
@chat/src/app/hooks/useMealPlan.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From chat/src/app/services/RecipePersistence.ts:
  export const getRecipe = async (id: string): Promise<Recipe | undefined>
  export interface Recipe { id: string; title: string; servings: number; ingredients: Ingredient[]; steps: string[]; totalMinutes?: number; searchableIngredients?: string[] }

From chat/src/app/services/MealPlanStore.ts:
  export const clearPlan = async (): Promise<void>   // calls notifyMealPlanStore() — useMealPlan subscribers auto-refresh

From chat/src/app/hooks/useMealPlan.ts:
  useMealPlan() returns { plan: PlanEntry[], loading: boolean }   // already subscribed to MealPlanStore pub-sub

From chat/src/app/components/ChatBox.tsx (existing):
  Message = { id: number; text: string; sender: string; uiResourceUri?: string }
  GenUIChatPanel.tsx already uses sender values 'User', 'Bot', 'System' — keep 'System' for Thinking… for visual consistency with the commit-listener "Added <recipe> ✓" line.

Vite env flag:
  `import.meta.env.DEV` — boolean, true in `vite dev` and tests, false in production `vite build`. This is the standard guard per CONTEXT.md Established Patterns.

For iframeBridgeScript.ts: this is a STRING literal (the inner-iframe inline `<script>` body). `import.meta.env.DEV` is NOT available inside the iframe runtime (no Vite). For the iframe-side debug helper, gate behind `false` in the string template OR ship a static `var DEV = false` at top of the IIFE so production has cheap no-op debug calls. (Vite will tree-shake the host-side bridge.ts but the iframe string is delivered verbatim.)

For bridge.ts (HOST side, real TS module): use `import.meta.env.DEV` directly in `onLog` consumers and the inline `console.debug` callers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Pick-button clipping (ResizeObserver target + min-height safety net)</name>
  <read_first>
    - chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts (lines 78-188 — the inner iframe inline script with start()/ResizeObserver setup)
    - chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts (lines 43-149 — CSS_SUBSET; the `html, body { margin: 0; padding: 0; ... }` block at line 68 is where the body reset lives)
    - .planning/phases/05-mcp-apps-host-sandboxed-iframe-postmessage-bridge/05-VERIFICATION.md (Success Criterion 4 minor-visual-observation — the documented root cause: ResizeObserver returned 150px clipping Pick)
    - 07-CONTEXT.md (Pick button clipping fix section — exact fix instructions)
  </read_first>
  <files>chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts, chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts</files>
  <action>
    Two coordinated edits to make `ui/notifications/size-changed` report a height that includes overflow content (the Pick button row).

    (A) chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts — change the ResizeObserver target + height measurement inside the `innerIframeBridgeScript` string template's `start()` function.
      1. The current code observes `document.documentElement` and reads `entry.contentRect.height`. Replace with:
         - Observe `document.body` instead (per CONTEXT.md root-cause analysis: document.documentElement does not expand for overflow children of an overflow-x carousel; document.body does).
         - Inside the debounced callback, ignore `entry.contentRect.height` and instead compute `var height = Math.max(document.body.scrollHeight, document.body.offsetHeight);`. Keep the existing `Math.ceil` for integer transport.
      2. Keep the 50ms debounce, keep `sendNotification('ui/notifications/size-changed', { height: height })`, keep the rest of the start() body (handshake, click handler) untouched.
      3. Also introduce a tiny DEV gate at the top of the IIFE (right after `var pendingIframeRequests = {};`):
         var DEV = false; function debug() { if (DEV) console.debug.apply(console, arguments); }
         Then replace every `console.debug(...)` inside this string template with `debug(...)`. There are five existing `console.debug` calls in this file — convert all five to `debug(...)` so production builds are quiet by default. The flag stays `false` because this script ships pre-stringified inside `<script>` tags; toggling DEV here is a manual step the developer can do during iframe debugging.

    (B) chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts — add a body min-height safety net to the locked CSS_SUBSET.
      Find the `html, body { ... }` rule at line 68. Add a separate `body { min-height: 200px; }` rule AFTER the existing `html, body { margin: 0; ... }` block (do NOT modify the existing combined rule, since other 05-UI-SPEC.md-locked properties live there). The new rule must appear inside CSS_SUBSET, before the `.carousel` rule. Rationale (CONTEXT.md): Pick button sits at card bottom; cards need ≥180px to render fully; 200px gives breathing room and matches the locked-decision number.

    Brownfield boundary: do NOT modify the outer shell HTML, the relay listeners, the message handling, the carousel render functions, or the renderCarouselHTML signature. Only the ResizeObserver setup + the CSS_SUBSET min-height rule.
  </action>
  <acceptance_criteria>
    - `grep -c 'ro\.observe(document\.body)' chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts` returns 1
    - `grep -c 'ro\.observe(document\.documentElement)' chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts` returns 0
    - `grep -c 'Math.max(document.body.scrollHeight' chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts` returns 1
    - `grep -c 'var DEV = false' chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts` returns 1
    - `grep -c 'console\.debug' chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts` returns 0 (all migrated to `debug(...)`)
    - `grep -c 'debug(' chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts` returns at least 5
    - `grep -cE '\bmin-height:\s*200px' chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts` returns 1
    - The min-height rule sits inside the CSS_SUBSET template string (between the backticks `` ` `` opening and closing of CSS_SUBSET, NOT outside)
    - npx nx typecheck chat passes (no TS errors from changed files)
  </acceptance_criteria>
  <verify>
    <automated>set -e; cd /Users/danielos/dev/window-ai; \
      test "$(grep -c 'ro.observe(document.body)' chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts)" = "1" || { echo FAIL observe target; exit 1; }; \
      test "$(grep -c 'ro.observe(document.documentElement)' chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts)" = "0" || { echo FAIL old observe still present; exit 1; }; \
      grep -nF 'Math.max(document.body.scrollHeight' chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts &gt; /dev/null || { echo FAIL height calc; exit 1; }; \
      grep -nF 'var DEV = false' chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts &gt; /dev/null || { echo FAIL DEV gate; exit 1; }; \
      test "$(grep -c 'console.debug' chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts)" = "0" || { echo FAIL console.debug migration; exit 1; }; \
      grep -nE 'min-height:\s*200px' chat/src/app/components/GenerativeUI/iframe/carouselTemplate.ts &gt; /dev/null || { echo FAIL min-height; exit 1; }; \
      npx nx typecheck chat 2&gt;&amp;1 | tail -5; \
      echo OK</automated>
  </verify>
  <done>ResizeObserver targets document.body, height includes overflow children via Math.max(scrollHeight, offsetHeight), CSS body has min-height: 200px safety net, all iframe-side console.debug calls dev-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Polish MealPlanColumn (title resolution + Clear plan button) and dev-gate host-side debug logs</name>
  <read_first>
    - chat/src/app/components/GenerativeUI/MealPlanColumn.tsx (current — entry.recipeId is rendered directly as text)
    - chat/src/app/services/MealPlanStore.ts (clearPlan() already exists, notifies subscribers — useMealPlan auto-refreshes)
    - chat/src/app/services/RecipePersistence.ts (getRecipe returns Promise<Recipe | undefined>)
    - chat/src/app/hooks/useMealPlan.ts (already subscribes to MealPlanStore; clearPlan() will trigger a re-fetch)
    - chat/src/app/components/GenerativeUI/iframe/bridge.ts (the host-side bridge factory — onLog callback is the central choke point; the inline `console.debug` calls inside outerShellHTML are STRING-template only and were handled in Task 1)
    - chat/src/app/components/GenerativeUIPage.tsx (current — has the kicker text already? confirm via grep, add if missing; the file was refactored in 07-01 to use Tabs but the page header + banner still render outside Tabs)
  </read_first>
  <files>chat/src/app/components/GenerativeUI/MealPlanColumn.tsx, chat/src/app/components/GenerativeUI/iframe/bridge.ts, chat/src/app/components/GenerativeUIPage.tsx</files>
  <action>
    Three sub-edits.

    (A) chat/src/app/components/GenerativeUI/MealPlanColumn.tsx — title resolution + Clear plan button.
      1. Add imports: `useEffect`, `useState`, `useCallback` from 'react'; `getRecipe` from '../../services/RecipePersistence'; `clearPlan` from '../../services/MealPlanStore'.
      2. Inside the component, add a `titles` state: `const [titles, setTitles] = useState<Record<string, string>>({});`. After `useMealPlan()`, add a `useEffect` keyed on `plan` that:
         - Builds a set of unique recipeIds present in plan
         - For each id not already in `titles`, calls `await getRecipe(id)` and merges `{ [id]: recipe?.title ?? id }` into the titles map
         - Use a `cancelled` flag pattern (mirror the seedIfMissing effect in GenerativeUIPage.tsx) so React StrictMode double-mount does not double-fetch
      3. In the populated `<ul>` branch, render each entry as:
           - Primary line: the resolved title (`titles[entry.recipeId] ?? entry.recipeId`), with `font-medium`
           - Secondary line (smaller, muted): the recipeId, in a `text-xs text-gray-400 dark:text-gray-500` span — call it a "debug/uniqueness" meta line per CONTEXT.md
           - Timestamp on the same row as the title or below — preserve the existing addedAt formatting
      4. Add a "Clear plan" action above or beside the `<ul>` rendering, visible ONLY when `plan.length > 0`. Use:
           <button onClick={handleClear} className="text-xs text-red-600 dark:text-red-400 hover:underline" type="button">Clear plan</button>
         Wire `handleClear = useCallback(async () => { await clearPlan(); }, [])`. clearPlan triggers `notifyMealPlanStore()` → useMealPlan's subscription fires → component re-renders with `plan.length === 0` → empty state appears. No local optimistic state needed.
      5. Keep the loading and empty-state branches unchanged (no Clear button in either branch).

    (B) chat/src/app/components/GenerativeUI/iframe/bridge.ts — gate debug logs behind `import.meta.env.DEV`.
      1. The current code uses an `onLog` callback that the consumer passes in. Find every internal `onLog?.('host', ...)` call site (there are ~10 throughout `attachLoadListener`, `handleMessage`, `handleInboundRequest`, `handleInboundNotification`, `sendHostContextChanged`). Wrap each `onLog?.(...)` call with `if (import.meta.env.DEV) onLog?.(...)`. Do NOT change the function signatures, do NOT remove `onLog` from `UIResourceBridgeOptions`, do NOT change behavior — only short-circuit the logging in production.
      2. Add a one-line comment above the first wrapped call: `// onLog is dev-only — production builds skip logging entirely (07-CONTEXT.md Polish items).`

    (C) chat/src/app/components/GenerativeUIPage.tsx — append the zero-network kicker marker.
      1. Check whether the marker text is already present (grep). If not, append a small footer-style line at the very bottom of the rendered root `<div>`, INSIDE the existing top-level div but OUTSIDE the Tabs strip, so it shows on BOTH the Docs and Workbench tabs.
      2. Use this exact JSX (subtle styling per CONTEXT.md "low key" recommendation):
           <p className="mt-4 text-center text-xs text-gray-500 dark:text-gray-400">
             🔒 Zero network during demo — open DevTools → Network tab
           </p>
      3. The marker is always-visible per Claude's-Discretion ruling in CONTEXT.md (recommendation: subtle, always-on, not DEV-only — so the speaker can point to it on stage even in production builds).

    Brownfield: do NOT modify any other GenerativeUI subcomponent, do NOT touch RecipeWorkbench, do NOT touch any service file besides reading its exports.
  </action>
  <acceptance_criteria>
    - `grep -c "from '../../services/RecipePersistence'" chat/src/app/components/GenerativeUI/MealPlanColumn.tsx` returns 1
    - `grep -c "from '../../services/MealPlanStore'" chat/src/app/components/GenerativeUI/MealPlanColumn.tsx` returns 1
    - `grep -c 'Clear plan' chat/src/app/components/GenerativeUI/MealPlanColumn.tsx` returns 1
    - `grep -c 'getRecipe' chat/src/app/components/GenerativeUI/MealPlanColumn.tsx` returns ≥2 (import + usage)
    - `grep -c 'clearPlan' chat/src/app/components/GenerativeUI/MealPlanColumn.tsx` returns ≥2 (import + usage)
    - In MealPlanColumn.tsx the populated branch references `titles[` (the title lookup) at least once
    - The Clear plan button is gated by `plan.length > 0` (grep for `plan.length` proximity to the button JSX)
    - In bridge.ts, `grep -c 'import.meta.env.DEV' chat/src/app/components/GenerativeUI/iframe/bridge.ts` returns ≥1
    - In bridge.ts every remaining `onLog?.(` call site is preceded by an `if (import.meta.env.DEV)` guard. Verification: `grep -c 'onLog?.(' chat/src/app/components/GenerativeUI/iframe/bridge.ts` equals the count after the change; before each call there must be the env-gate
    - `grep -c 'Zero network during demo' chat/src/app/components/GenerativeUIPage.tsx` returns 1
    - `npx nx typecheck chat` passes; `npx nx lint chat` passes
  </acceptance_criteria>
  <verify>
    <automated>set -e; cd /Users/danielos/dev/window-ai; \
      grep -nF "from '../../services/RecipePersistence'" chat/src/app/components/GenerativeUI/MealPlanColumn.tsx &gt; /dev/null || { echo FAIL persistence import; exit 1; }; \
      grep -nF "from '../../services/MealPlanStore'" chat/src/app/components/GenerativeUI/MealPlanColumn.tsx &gt; /dev/null || { echo FAIL store import; exit 1; }; \
      grep -nF 'Clear plan' chat/src/app/components/GenerativeUI/MealPlanColumn.tsx &gt; /dev/null || { echo FAIL clear button; exit 1; }; \
      grep -nF 'titles[' chat/src/app/components/GenerativeUI/MealPlanColumn.tsx &gt; /dev/null || { echo FAIL title map lookup; exit 1; }; \
      grep -nF 'plan.length' chat/src/app/components/GenerativeUI/MealPlanColumn.tsx &gt; /dev/null || { echo FAIL plan.length gate; exit 1; }; \
      grep -nF 'import.meta.env.DEV' chat/src/app/components/GenerativeUI/iframe/bridge.ts &gt; /dev/null || { echo FAIL DEV gate in bridge; exit 1; }; \
      grep -nF 'Zero network during demo' chat/src/app/components/GenerativeUIPage.tsx &gt; /dev/null || { echo FAIL kicker marker; exit 1; }; \
      npx nx typecheck chat 2&gt;&amp;1 | tail -5; \
      npx nx lint chat 2&gt;&amp;1 | tail -5; \
      echo OK</automated>
  </verify>
  <done>MealPlanColumn shows titles + Clear plan button works via MealPlanStore.clearPlan(), host-side bridge.ts onLog calls are dev-gated, zero-network kicker text rendered on /generative-ui.</done>
</task>

<task type="auto">
  <name>Task 3: Add Thinking… loader to GenUIChatPanel, dev-gate its debug logs, and create REHEARSAL.md template</name>
  <read_first>
    - chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx (lines 158-399 — addMessage usage, isLoading state, the agent loop at handleUserMessage)
    - 07-CONTEXT.md > Chat loading state (the locked Thinking… implementation: transient Message with sender 'System', italicized via existing system styling, added before session.prompt and removed on response)
    - 07-CONTEXT.md > Demo rehearsal (REHEARSAL.md content template: 5 cold-run sections + structure)
  </read_first>
  <files>chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx, .planning/phases/07-docs-seo-demo-polish/REHEARSAL.md</files>
  <action>
    Two sub-edits.

    (A) chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx — Thinking… loader + dev-gate debug logs.
      1. Add a `THINKING_TEXT = 'Thinking…'` module-level constant near the other module constants (above the component, after INTENT_SCHEMA). Using a constant keeps the add/remove logic match-by-text consistent.
      2. In `handleUserMessage`, BEFORE the `try { ... }` block but AFTER `addMessage(text, 'User')` (line 260), call `addMessage(THINKING_TEXT, 'System')`. This appends a transient transcript line the user sees while the model is still thinking.
      3. INSIDE the `try { ... }` block, immediately AFTER `const rawResponse = await session.prompt(promptText);` for the FIRST iteration (callCount === 0), remove the Thinking message. The cleanest implementation: after `addMessage(THINKING_TEXT, 'System')`, set a local flag `let thinkingRemoved = false;` BEFORE the loop. Inside the loop, after the first `session.prompt` resolves, if `!thinkingRemoved`, run `setMessages(prev => prev.filter(m => m.text !== THINKING_TEXT))` and set `thinkingRemoved = true`. This ensures the Thinking line is removed on FIRST prompt resolution (whether that resolution leads to a tool call or to "done").
      4. In the `catch` and `finally` blocks of `handleUserMessage`, also clean up: if the Thinking message is still present (i.e., the prompt threw before resolving), strip it. Easiest: add `setMessages(prev => prev.filter(m => m.text !== THINKING_TEXT))` at the very top of the `finally` block (idempotent — no-op if already removed). Move this to be the FIRST statement inside finally so it runs regardless of try-path outcome. (Per Claude's-Discretion in CONTEXT.md, no 10s safety timeout is needed — the LanguageModel call either resolves, throws, or stays open with the existing isLoading UI affordance.)
      5. Dev-gate the host-side debug calls: find every `console.debug(...)` and `console.assert(...)` and `console.warn(...)` and `console.error(...)` in this file. Wrap the `console.debug(...)` calls (if any exist — current file has none, but if a debug helper is added during execution it must be guarded). The existing `console.error(...)` calls are intentional and remain unguarded (errors should surface in production). The runtime `console.assert(!promptText.includes('ui://'), ...)` MUST be kept exactly as-is for the GENUI-10 invariant — do NOT dev-gate the assert, since it is load-bearing per 06-VERIFICATION.md Success Criterion 4.
      6. Do NOT alter the dispatch loop semantics, the visibility filter, the _meta interceptor, or the commit-listener wiring.

    (B) .planning/phases/07-docs-seo-demo-polish/REHEARSAL.md — create the cold-run log template.
      Create a new markdown file with this structure (verbatim, with empty sections for the speaker to fill in during the actual rehearsal):

      # Phase 7 Demo Rehearsal — Cold-Run Log

      **Goal:** 5 consecutive clean runs of the 90-second demo flow on the speaker's laptop (Chrome 146+ Canary, WebMCP flag enabled). Zero outbound network requests. No failed inferences.

      **Cold-run definition:** fresh Chrome profile OR DevTools → Application → Storage → "Clear site data" → reload. Each run starts from empty IDB + cleared LanguageModel session.

      **Demo script:**
      1. Open `/generative-ui` in Chrome 146 Canary.
      2. Open DevTools → Network tab; clear; start recording.
      3. Type a recipe query into chat (e.g. "Find me a 30-minute chicken dinner").
      4. Confirm: carousel renders inside the chat bubble; Pick buttons visible and unclipped.
      5. Click "Pick" on a card.
      6. Confirm: MealPlanColumn updates within one animation frame with the recipe title (not the ID).
      7. Confirm: Network tab shows zero outbound requests after initial page paint.
      8. Stop recording, archive Network trace if any cold run fails.

      ## Cold run 1

      - Date / time:
      - Browser version:
      - Result (PASS / FAIL):
      - Failures observed:
      - Network requests after paint:
      - Notes / fixes applied:

      ## Cold run 2

      - Date / time:
      - Browser version:
      - Result (PASS / FAIL):
      - Failures observed:
      - Network requests after paint:
      - Notes / fixes applied:

      ## Cold run 3

      - Date / time:
      - Browser version:
      - Result (PASS / FAIL):
      - Failures observed:
      - Network requests after paint:
      - Notes / fixes applied:

      ## Cold run 4

      - Date / time:
      - Browser version:
      - Result (PASS / FAIL):
      - Failures observed:
      - Network requests after paint:
      - Notes / fixes applied:

      ## Cold run 5

      - Date / time:
      - Browser version:
      - Result (PASS / FAIL):
      - Failures observed:
      - Network requests after paint:
      - Notes / fixes applied:

      ## Summary

      - Consecutive clean runs:
      - GENUI-14 status:
      - GENUI-15 status:
      - Sign-off:

  </action>
  <acceptance_criteria>
    - `grep -c 'THINKING_TEXT' chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx` returns ≥3 (declaration + at-least-2 usages)
    - `grep -nF "addMessage(THINKING_TEXT" chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx` returns ≥1 (the pre-prompt insertion)
    - `grep -cF 'setMessages(prev =>' chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx` returns ≥2 (the filter-out paths: one inside try after first prompt, one in finally)
    - `grep -c "console.assert(!promptText.includes('ui://')" chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx` returns 1 (assert preserved verbatim — GENUI-10 invariant must NOT be dev-gated)
    - `.planning/phases/07-docs-seo-demo-polish/REHEARSAL.md` exists
    - The strings `Cold run 1`, `Cold run 2`, `Cold run 3`, `Cold run 4`, `Cold run 5` each appear exactly once in REHEARSAL.md
    - `GENUI-14` and `GENUI-15` each appear in REHEARSAL.md (Summary section)
    - `npx nx typecheck chat` passes; `npx nx lint chat` passes
  </acceptance_criteria>
  <verify>
    <automated>set -e; cd /Users/danielos/dev/window-ai; \
      test "$(grep -c 'THINKING_TEXT' chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx)" -ge "3" || { echo FAIL THINKING_TEXT count; exit 1; }; \
      grep -nF 'addMessage(THINKING_TEXT' chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx &gt; /dev/null || { echo FAIL pre-prompt insert; exit 1; }; \
      test "$(grep -cF 'setMessages(prev =&gt;' chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx)" -ge "2" || { echo FAIL filter paths; exit 1; }; \
      grep -nF "console.assert(!promptText.includes('ui://')" chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx &gt; /dev/null || { echo FAIL GENUI-10 assert removed; exit 1; }; \
      test -f .planning/phases/07-docs-seo-demo-polish/REHEARSAL.md || { echo FAIL REHEARSAL missing; exit 1; }; \
      for n in 1 2 3 4 5; do test "$(grep -c \"Cold run $n\" .planning/phases/07-docs-seo-demo-polish/REHEARSAL.md)" = "1" || { echo "FAIL Cold run $n"; exit 1; }; done; \
      grep -qF GENUI-14 .planning/phases/07-docs-seo-demo-polish/REHEARSAL.md || { echo FAIL GENUI-14 ref; exit 1; }; \
      grep -qF GENUI-15 .planning/phases/07-docs-seo-demo-polish/REHEARSAL.md || { echo FAIL GENUI-15 ref; exit 1; }; \
      npx nx typecheck chat 2&gt;&amp;1 | tail -5; \
      npx nx lint chat 2&gt;&amp;1 | tail -5; \
      echo OK</automated>
  </verify>
  <done>Thinking… loader appears before each session.prompt and is removed on first response (or in finally on error); GENUI-10 console.assert preserved verbatim; REHEARSAL.md template exists with 5 cold-run sections + GENUI-14/GENUI-15 references.</done>
</task>

</tasks>

<verification>
- All three task `<automated>` gates pass
- `npx nx build chat` produces a clean production build (and the prerendered /generative-ui-docs.html exists in dist/chat/)
- Browser smoke check (manual, recorded in execute-phase SUMMARY):
  - Navigate to /generative-ui, click "Show demo carousel" debug trigger OR the live chat path → carousel renders → Pick button is FULLY VISIBLE at card bottom (no clipping)
  - Click Pick → MealPlanColumn updates with the recipe TITLE (not the ID); the recipeId appears below in smaller muted text
  - Click "Clear plan" → list clears, empty-state copy reappears
  - Send a chat message → "Thinking…" appears briefly in the transcript, then is replaced by the bot's reply (or by the carousel bubble for searchRecipes)
  - Scroll to the bottom of /generative-ui → see "🔒 Zero network during demo — open DevTools → Network tab" subtle line
  - DevTools console in production build (npx nx build chat + serve dist) → no `[mcp-apps:*]` debug noise (only errors / warns if any)
</verification>

<success_criteria>
GENUI-14 ready for rehearsal: every blocking issue from Phase 5/6 verification (Pick clipping, missing loader, recipeId-as-text) is closed; REHEARSAL.md exists so the speaker can log 5 cold runs and prove repeatability.
GENUI-15 closed (visible side): zero-network kicker marker rendered on /generative-ui, pointing the audience at DevTools. The actual zero-network behavior is structurally guaranteed (no fetch / XHR / WebSocket in the codebase touched in this milestone) and confirmed by Network capture during rehearsal.
Brownfield boundary respected: only the 6 source files inside `chat/src/app/components/GenerativeUI/` + `GenerativeUIPage.tsx` + 1 planning artifact are touched. Nothing under `/webmcp/`, `mcp/`, `mcp-client/`, `devops/awsweb/`, `chat/src/app/components/RecipeWorkbench/*`, or services besides imports.
</success_criteria>

<output>
After completion, create `.planning/phases/07-docs-seo-demo-polish/07-02-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Include: the typecheck/lint/build commands and their tail output, a screenshot path for the un-clipped Pick button (if the executor captures one via Chrome DevTools MCP), and a one-line note confirming the GENUI-10 console.assert was preserved verbatim (not dev-gated).
</output>
