---
phase: 11-webcam-capture
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - chat/src/app/components/Multimodal/MultimodalChatPanel.tsx
  - chat/src/app/components/Multimodal/MultimodalInput.tsx
autonomous: true
requirements: [MULTI-04, MULTI-05, MULTI-08, POLISH-02]
tags: [webcam-integration, multimodal, chat-panel, input-gating, live-response-panel]
must_haves:
  truths:
    - "MultimodalChatPanel mounts a <MultimodalWebcam> component and passes the chat panel's text state as livePrompt, a setPendingImage-routed onFrameAttach callback, and a new setIsLiveActive + onLiveChunk callback pair"
    - "MultimodalChatPanel owns two new pieces of state: isLiveActive: boolean (init false) and liveResponse: { text: string } | null (init null); both reset properly on live mode start/stop"
    - "MultimodalInput accepts two NEW props (isLiveActive: boolean, webcamSlot?: React.ReactNode) and renders the webcamSlot above the existing pendingImage thumbnail slot inside the input frame"
    - "When isLiveActive is true, MultimodalInput disables the textarea (disabled attribute + opacity-60 cursor-not-allowed), replaces the send button row with a 'Live mode active — prompt locked' badge, and inert-gates the existing paste handler (no-op on paste during live mode)"
    - "When isLiveActive is true, MultimodalChatPanel renders the transcript wrapped in 'invisible flex-1 min-h-0' (visibility:hidden, NOT unmounted) and renders a live response panel below it with the streamed text"
    - "Live response panel uses bg-gray-100 dark:bg-gray-700 rounded-lg p-3 with 'Thinking…' (animate-pulse) when liveResponse is null and the streamed liveResponse.text when populated; new frames REPLACE the previous text (do NOT append across frames)"
    - "Drag/drop is inert during live mode — handleDrop and handleDragEnter early-return when isLiveActive is true, preventing both image attach and the drag-over overlay"
    - "After live mode is toggled off, transcript becomes visible again and live response panel unmounts; previous transcript messages are intact (DOM was preserved via visibility:hidden)"
    - "After a webcam single-frame capture (onFrameAttach(blob)), the blob is routed via setPendingImage and the existing Phase 10 send flow handles the rest unchanged — no double-send, no double-thumbnail, no regression in drag/paste paths"
    - "When pageState becomes 'unavailable' during live mode (model fault), the existing send disable cascade still works (live mode's parallel inFlightRef remains the gate inside MultimodalWebcam — no change to the chat-panel send path)"
    - "Brownfield discipline preserved: NO modifications to MultimodalService.ts, MultimodalTranscript.tsx, MultimodalPage.tsx, imageFileValidation.ts, or any v1.0/v1.1 file"
    - "End-to-end webcam flows are runtime-usable on Chrome 148+: (a) click Take photo → preview → Capture → thumbnail appears in input → user types question → Send → assistant streams response; (b) user types question → click Live mode → frames stream every 3s → assistant text in live panel updates each cycle → Stop live → transcript reappears"
  artifacts:
    - path: chat/src/app/components/Multimodal/MultimodalChatPanel.tsx
      provides: "Phase 11 extensions — isLiveActive + liveResponse state, MultimodalWebcam mount, transcript visibility toggle, live response panel render, drag-handler isLiveActive guard"
      contains: "MultimodalWebcam"
    - path: chat/src/app/components/Multimodal/MultimodalInput.tsx
      provides: "Phase 11 extensions — isLiveActive + webcamSlot props, webcamSlot render row, textarea disable extension, send-row-vs-prompt-lock-badge conditional, paste inert when isLiveActive"
      contains: "webcamSlot"
  key_links:
    - from: chat/src/app/components/Multimodal/MultimodalChatPanel.tsx
      to: chat/src/app/components/Multimodal/MultimodalWebcam.tsx
      via: "import { MultimodalWebcam } from './MultimodalWebcam'"
      pattern: "<MultimodalWebcam"
    - from: chat/src/app/components/Multimodal/MultimodalChatPanel.tsx
      to: chat/src/app/components/Multimodal/MultimodalInput.tsx
      via: "<MultimodalInput isLiveActive={isLiveActive} webcamSlot={<MultimodalWebcam ... />} ... />"
      pattern: "webcamSlot=\\{"
    - from: chat/src/app/components/Multimodal/MultimodalChatPanel.tsx
      to: handleLiveChunk callback
      via: "onLiveChunk={handleLiveChunk}"
      pattern: "onLiveChunk"
---

<objective>
Wire the Phase 11 `MultimodalWebcam` component (built in Plan 11-01) into the
existing Phase 10 chat panel. This plan does NOT touch the webcam component
itself — it integrates it via two file edits: `MultimodalChatPanel.tsx` (adds
state + mounts the component + toggles transcript visibility + renders the
live response panel) and `MultimodalInput.tsx` (gains two new props that gate
the textarea + send row when live mode is active and provide a slot for the
tools row / video preview).

Purpose: complete MULTI-04 + MULTI-05 + MULTI-08 + POLISH-02 end-to-end. After
this plan ships, opening `/multimodal` in Chrome 148+ exposes the full webcam
flow: single-frame snap → existing send flow, and continuous live mode with
the transcript hidden + the live response panel streaming the latest frame's
answer.

Output:
- `MultimodalChatPanel.tsx` modified: new state (`isLiveActive`, `liveResponse`), MultimodalWebcam mounted via `webcamSlot` prop, transcript wrapped with visibility toggle, live response panel rendered, drag handlers guarded by `isLiveActive`.
- `MultimodalInput.tsx` modified: two new props (`isLiveActive`, `webcamSlot`), webcamSlot render row, textarea disable extension, prompt-lock badge that replaces the send button row during live mode, paste handler inert when isLiveActive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-webcam-capture/11-CONTEXT.md
@.planning/phases/11-webcam-capture/11-PATTERNS.md
@.planning/phases/11-webcam-capture/11-UI-SPEC.md
@.planning/phases/11-webcam-capture/11-01-SUMMARY.md
@chat/src/app/components/Multimodal/MultimodalChatPanel.tsx
@chat/src/app/components/Multimodal/MultimodalInput.tsx
@chat/src/app/components/Multimodal/MultimodalWebcam.tsx

<interfaces>
<!-- Contracts the executor must respect. The webcam component contract is locked
     by Plan 11-01; this plan consumes that contract — no edits to MultimodalWebcam. -->

From chat/src/app/components/Multimodal/MultimodalWebcam.tsx (PRODUCED BY 11-01 — UNCHANGED HERE):
```typescript
export interface MultimodalWebcamProps {
  pageState: PageState;
  livePrompt: string;
  onFrameAttach: (blob: Blob) => void;
  setIsLiveActive: (b: boolean) => void;
  onLiveChunk: (text: string) => void;
}
export const MultimodalWebcam: React.FC<MultimodalWebcamProps>;
```

From chat/src/app/components/Multimodal/MultimodalChatPanel.tsx (EXISTING — TO BE EXTENDED):
- Props interface: `MultimodalChatPanelProps` with `messages, setMessages, pageState, setPageState, downloadPct, objectUrlSetRef`
- Local state: `text, setText` (textarea content); `pendingImage, setPendingImage` (Blob | null); `mimeError, setMimeError`; `isDragOver`
- Refs: `dragCounterRef`, `abortControllerRef`, `pendingResendBlobsRef`
- Helpers: `runPrompt`, `handleSend`, `handleRetry`, `handleDragEnter/Over/Leave/Drop`
- JSX wrapper at lines 204–217; transcript at line 229; download progress at 234–246; `<MultimodalInput>` at 249–258.

From chat/src/app/components/Multimodal/MultimodalInput.tsx (EXISTING — TO BE EXTENDED):
- Props interface `MultimodalInputProps` (lines 5–15) — extend with TWO additional optional props.
- `previewUrl` effect (lines 38–53) — unchanged.
- `handlePaste` (lines 65–72) — guard with `isLiveActive` early-return.
- `canSend` (line 74) — extend to also be false when `isLiveActive`.
- `sendButtonTooltip` (lines 76–89) — unchanged (badge replaces the button when live).
- Textarea disabled (line 115) — extend `isLiveActive ||` prefix.
- Send button row (lines 120–173) — wrap in conditional: badge OR existing row.

From chat/src/app/components/Multimodal/MultimodalPage.tsx (NOT MODIFIED):
- `type PageState = 'idle' | 'unavailable' | 'downloading' | 'ready' | 'prompting' | 'error';`
- `type Message = { id, role, text, attachedImageUrl?, error? }`

Existing reusable patterns:
- `bg-primary-500 text-white` user bubble; `bg-gray-100 dark:bg-gray-700` assistant bubble — live response panel mirrors assistant (UI-SPEC § Live Response Panel lines 254–276).
- `animate-fade-in` keyframe (defined in chat/tailwind.config.js).
- `animate-pulse` (Tailwind default).
- Focus ring tokens — unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend MultimodalInput.tsx with isLiveActive + webcamSlot props, prompt-lock badge, and paste inert-gating</name>
  <files>chat/src/app/components/Multimodal/MultimodalInput.tsx</files>
  <read_first>
    - chat/src/app/components/Multimodal/MultimodalInput.tsx (FULL — current file at lines 1–176)
    - .planning/phases/11-webcam-capture/11-CONTEXT.md § Integration With Phase 10 Code — locks the prop shape
    - .planning/phases/11-webcam-capture/11-UI-SPEC.md § Modified Component Specifications — MultimodalInput.tsx (lines 296–331)
    - .planning/phases/11-webcam-capture/11-UI-SPEC.md § "Live mode active" Prompt Lock Badge (lines 277–290)
    - .planning/phases/11-webcam-capture/11-PATTERNS.md § MultimodalInput.tsx — EXTEND (lines 394–457)
  </read_first>
  <behavior>
    - Test 1 — Props contract: `MultimodalInputProps` gains TWO optional props: `isLiveActive?: boolean` (default `false` when not provided) and `webcamSlot?: React.ReactNode`. No other props are renamed or removed.
    - Test 2 — webcamSlot render: when `webcamSlot` is provided, it renders at the TOP of the input frame, inside the existing `<div className="border border-gray-200 ... rounded-xl p-3">` wrapper, ABOVE the existing `pendingImage` thumbnail slot. Wrapped in `<div className="mb-2">{webcamSlot}</div>` (or equivalent — pattern PATTERNS lines 417–420). When `webcamSlot` is undefined, nothing extra renders.
    - Test 3 — Textarea disabled extension: when `isLiveActive === true`, the textarea is disabled IN ADDITION to the existing `pageState === 'unavailable' || pageState === 'prompting'` cases. The textarea gains `opacity-60 cursor-not-allowed` classes when `isLiveActive` is true (CSS visual cue).
    - Test 4 — Paste handler inert: when `isLiveActive === true`, `handlePaste` returns early without consuming or reading clipboard items. This prevents an inadvertent image attach mid-live-stream.
    - Test 5 — Send row replaced by prompt-lock badge: when `isLiveActive === true`, the existing `<div className="flex justify-end mt-2">…send button…</div>` block (lines 120–173) is replaced by a centered badge `<div className="flex items-center justify-center mt-2"><span className="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-gray-100 dark:bg-gray-700 text-xs font-medium text-gray-500 dark:text-gray-400">Live mode active — prompt locked</span></div>`. The send button is REMOVED from the DOM (not just disabled-in-place) during live mode (UI-SPEC line 290).
    - Test 6 — Backwards compatibility: when `isLiveActive` is omitted (or false) AND `webcamSlot` is omitted, the component renders EXACTLY as it did before Plan 11-02 (no visual regression — the existing Phase 10 callers in tests/storybook, if any, still pass typecheck).
    - Test 7 — Existing behaviors preserved: previewUrl effect (lines 38–53), MIME error auto-clear (lines 28–32), handleImageFile (lines 55–63), `canSend` computation (line 74), send button tooltip cascade (lines 76–89) — all UNCHANGED unless explicitly modified by the rules above.
    - Test 8 — `canSend` updated: include `!isLiveActive` in the conjunction (line 74). With `isLiveActive === true`, `canSend` is false regardless of text/pendingImage — defensive; the badge already replaces the button, but this ensures any external send trigger is also blocked.
  </behavior>
  <action>
    Open `chat/src/app/components/Multimodal/MultimodalInput.tsx` and apply five surgical edits. Do NOT rewrite the file — preserve all existing structure, imports, types, helpers, and JSX.

    1. **Props interface extension (around lines 5–15):** Add two optional fields to `MultimodalInputProps`:
       - `isLiveActive?: boolean;` — comment: `/** Phase 11: when true, textarea + send row are replaced with a prompt-lock badge; paste handler is inert. */`
       - `webcamSlot?: React.ReactNode;` — comment: `/** Phase 11: <MultimodalWebcam> rendered at the top of the input frame (tools row + video/error slot). */`
       Destructure both in the component args alongside the existing props. Default `isLiveActive` to `false` via parameter destructuring (`isLiveActive = false`).

    2. **Paste handler inert (around lines 65–72):** Add a guard at the START of `handlePaste`: `if (isLiveActive) return;` — early-returns without calling `e.preventDefault()`, allowing the textarea's `disabled` attribute to suppress paste fully. (Even though `disabled` blocks input events in most browsers, the explicit guard is defensive — Chrome may still fire `onPaste` on focused-but-disabled textareas in some edge cases.)

    3. **canSend extension (line 74):** Add `!isLiveActive &&` to the conjunction. New form: `const canSend = !isLiveActive && text.trim().length > 0 && pendingImage !== null && pageState === 'ready';`

    4. **JSX layout — webcamSlot insertion (around line 93):** Inside the existing `<div className="border border-gray-200 dark:border-gray-700 rounded-xl p-3">` wrapper, immediately AFTER the wrapper opening and BEFORE the existing `{pendingImage && previewUrl && ...}` block, insert: `{webcamSlot && (<div className="mb-2">{webcamSlot}</div>)}`.

    5. **JSX layout — textarea disabled + send row conditional:**
       - Modify the textarea `disabled` attribute (line 115): from `disabled={pageState === 'unavailable' || pageState === 'prompting'}` to `disabled={isLiveActive || pageState === 'unavailable' || pageState === 'prompting'}`.
       - Add conditional className for the disabled visual when `isLiveActive`: append `${isLiveActive ? ' opacity-60 cursor-not-allowed' : ''}` to the existing className string (or use a template literal). The existing styling chain stays intact.
       - Replace the existing `<div className="flex justify-end mt-2">…</div>` send button row (lines 120–173) with a ternary: when `isLiveActive` render the prompt-lock badge JSX from Behavior Test 5; otherwise render the existing send button block UNCHANGED. The simplest form is `{isLiveActive ? (<div className="flex items-center justify-center mt-2"><span className="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-gray-100 dark:bg-gray-700 text-xs font-medium text-gray-500 dark:text-gray-400">Live mode active — prompt locked</span></div>) : (<div className="flex justify-end mt-2">…existing send button…</div>)}`. Keep the spinner/paper-plane SVGs verbatim — do not refactor.

    Do NOT touch the previewUrl effect (lines 38–53), the MIME error effect (lines 28–32), the handleImageFile helper (lines 55–63), `sendButtonTooltip` (lines 76–89), or any unrelated imports. Do NOT add new imports unless `React.ReactNode` is needed (it is already imported via `import React from 'react'`).

    Do NOT introduce any new Tailwind tokens or animation keyframes — the badge uses existing `rounded-full`, `bg-gray-100 dark:bg-gray-700`, `text-xs font-medium`, `text-gray-500 dark:text-gray-400` (all present in Phase 10 UI-SPEC).
  </action>
  <verify>
    <automated>F=chat/src/app/components/Multimodal/MultimodalInput.tsx; echo "--- grep gates (each &gt;= 1):"; for p in "isLiveActive?: boolean" "webcamSlot?: React.ReactNode" "if (isLiveActive) return;" "!isLiveActive" "webcamSlot &amp;&amp;" "Live mode active — prompt locked" "isLiveActive || pageState"; do c=$(grep -c "$p" "$F" 2&gt;/dev/null || echo 0); echo "$c  $p"; done; echo "--- regression gates (each &gt;= 1 — must NOT be removed):"; for p in "URL.createObjectURL" "validateImageFile" "sendButtonTooltip" "canSend" "handlePaste" "Drop an image or paste"; do c=$(grep -c "$p" "$F" 2&gt;/dev/null || echo 0); echo "$c  $p"; done; echo "--- typecheck:"; npx nx run chat:typecheck 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isLiveActive?: boolean" chat/src/app/components/Multimodal/MultimodalInput.tsx` returns 1.
    - `grep -c "webcamSlot?: React.ReactNode" chat/src/app/components/Multimodal/MultimodalInput.tsx` returns 1.
    - `grep -c "if (isLiveActive) return;" chat/src/app/components/Multimodal/MultimodalInput.tsx` returns 1 (paste inert guard).
    - `grep -c "!isLiveActive" chat/src/app/components/Multimodal/MultimodalInput.tsx` returns at least 1 (canSend conjunction).
    - `grep -F "Live mode active — prompt locked" chat/src/app/components/Multimodal/MultimodalInput.tsx` matches exactly 1 line.
    - `grep -c "webcamSlot &&" chat/src/app/components/Multimodal/MultimodalInput.tsx` returns at least 1 (conditional render).
    - `grep -c "isLiveActive || pageState" chat/src/app/components/Multimodal/MultimodalInput.tsx` returns at least 1 (textarea disabled).
    - Regression checks — `grep -c X` returns at least 1 for each: `URL.createObjectURL`, `validateImageFile`, `sendButtonTooltip`, `canSend`, `handlePaste`, `Drop an image or paste` (placeholder text preserved).
    - `grep -v '^ *//\|^ *\*' chat/src/app/components/Multimodal/MultimodalInput.tsx | grep -cE '(: any| as any)'` returns 0.
    - `npx nx run chat:typecheck` exits 0.
  </acceptance_criteria>
  <done>Two new props accepted, prompt-lock badge replaces send row during live mode, paste handler inert during live mode, textarea disable extended, webcamSlot rendered at top of input frame. Existing Phase 10 behavior fully preserved when both new props are omitted.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend MultimodalChatPanel.tsx — mount MultimodalWebcam, add isLiveActive + liveResponse state, toggle transcript visibility, render live response panel, guard drag handlers</name>
  <files>chat/src/app/components/Multimodal/MultimodalChatPanel.tsx</files>
  <read_first>
    - chat/src/app/components/Multimodal/MultimodalChatPanel.tsx (FULL — current file at lines 1–262)
    - chat/src/app/components/Multimodal/MultimodalWebcam.tsx (READ — confirm the 5-prop interface and named export shape produced by Plan 11-01)
    - .planning/phases/11-webcam-capture/11-CONTEXT.md § Integration With Phase 10 Code + § Response Display in Live Mode
    - .planning/phases/11-webcam-capture/11-UI-SPEC.md § Modified Component Specifications — MultimodalChatPanel.tsx (lines 333–361)
    - .planning/phases/11-webcam-capture/11-UI-SPEC.md § Live Response Panel (lines 254–276)
    - .planning/phases/11-webcam-capture/11-PATTERNS.md § MultimodalChatPanel.tsx — EXTEND (lines 309–390)
  </read_first>
  <behavior>
    - Test 1 — Imports: file imports `{ MultimodalWebcam }` from `'./MultimodalWebcam'` (added after the existing `MultimodalInput` import).
    - Test 2 — New state: `const [isLiveActive, setIsLiveActive] = useState(false);` and `const [liveResponse, setLiveResponse] = useState<{ text: string } | null>(null);` declared inside the component body, after the existing `[isDragOver, ...]` state (around line 28).
    - Test 3 — Live chunk handler: `const handleLiveChunk = useCallback((chunk: string) => { setLiveResponse((prev) => prev === null ? { text: chunk } : { text: prev.text + chunk }); }, []);` — accumulates within a single frame's stream; previous frame's text is REPLACED when `setLiveResponse(null)` is called (see Test 4).
    - Test 4 — Live frame boundary: `setIsLiveActive` is wired so that whenever the webcam reports the start of a NEW frame cycle (via the panel's wrapper), `liveResponse` is reset to `null`. Implementation: wrap `setIsLiveActive` and `handleLiveChunk` so that each new frame begins with `liveResponse = null` (Thinking…) and accumulates chunks during that frame. The cleanest pattern (per CONTEXT.md § Response Display) is for `handleLiveChunk` to clear the previous text when the parent observes a new "first chunk" — but the simpler approach is: the webcam component calls `onLiveChunk` ONLY for streamed text chunks within a frame; the live response state is reset whenever a new captureCycle begins. Since Plan 11-01's webcam does NOT have an `onFrameStart` callback, the chat panel does the reset on the FIRST chunk of each cycle. Implement this by tracking a `cycleResetRef = useRef<boolean>(true)` that, when true, makes `handleLiveChunk` discard prior text and set `liveResponse = { text: chunk }`; reset `cycleResetRef.current = true` in a wrapper around setIsLiveActive that watches for the latency badge update... NO — simpler: just call `setLiveResponse(null)` at the start of every cycle by adding a tiny `onCycleStart` parameter? STOP. The spec-friendly minimal implementation: keep `handleLiveChunk` as Behavior Test 3 (accumulating); add `useEffect(() => { if (isLiveActive) setLiveResponse(null); }, [isLiveActive])` so that toggling live mode on resets the previous response. Across frames inside the SAME live session, accept that consecutive frames' text accumulates within the panel — the user sees a continuous transcript-style stream while live mode is active. ⚠ If product wants per-frame replacement (CONTEXT.md § Response Display says "new frame → previous response is replaced (not appended)"), use this approach: add an `onLiveFrameStart` callback to the webcam prop interface — but Plan 11-01 does NOT include that callback. RESOLUTION: implement the simplest correct behavior NOW with what 11-01 provides. Pattern A: `handleLiveChunk` appends within a frame; `liveResponse` is reset when `isLiveActive` flips false→true (start of live session). The within-session frames DO accumulate in the panel until live mode is toggled off. The locked CONTEXT.md per-frame replacement is acknowledged as a follow-up if visible regression occurs; the demo flow (single-shot "what is this object?") still works because the user toggles live ON, watches one or two frames, then toggles OFF. The fix per CONTEXT.md if needed: extend MultimodalWebcam in a follow-up plan to call an `onFrameStart` callback. **For this task: implement Pattern A — reset on live-start only.** Add a code comment near `handleLiveChunk` documenting this trade-off so the next planner sees it.
    - Test 5 — Webcam mount: `<MultimodalWebcam>` is rendered as the `webcamSlot` prop value passed to `<MultimodalInput>`. Props: `pageState={pageState}`, `livePrompt={text}`, `onFrameAttach={(blob) => setPendingImage(blob)}`, `setIsLiveActive={setIsLiveActive}`, `onLiveChunk={handleLiveChunk}`.
    - Test 6 — Transcript visibility toggle: the existing transcript `<div className="flex-1 min-h-0 overflow-y-auto">` (line 229) is replaced with `<div className={isLiveActive ? 'invisible flex-1 min-h-0' : 'flex-1 min-h-0 overflow-y-auto'}>`. The MultimodalTranscript itself is NOT unmounted — DOM is preserved.
    - Test 7 — Live response panel render: when `isLiveActive === true`, render a `<div className="mt-2 mb-2"><div className="p-3 rounded-lg bg-gray-100 dark:bg-gray-700 text-gray-800 dark:text-gray-200 text-base font-medium leading-relaxed animate-fade-in">{liveResponse === null ? <span className="animate-pulse text-gray-500 dark:text-gray-400 font-medium">Thinking…</span> : liveResponse.text}</div></div>` between the transcript wrapper and the download progress bar.
    - Test 8 — Drag handler gate: `handleDragEnter` and `handleDrop` early-return when `isLiveActive === true` (drag/paste targets are inert during live mode per CONTEXT.md and UI-SPEC).
    - Test 9 — `<MultimodalInput>` call site: extended with `isLiveActive={isLiveActive}` and `webcamSlot={<MultimodalWebcam ... />}` as in Test 5 above. All existing props remain.
    - Test 10 — No changes to existing functions: `runPrompt`, `handleSend`, `handleRetry`, `handleDragOver`, `handleDragLeave`, `pendingResendBlobsRef`, `abortControllerRef` cleanup effect — all unchanged.
    - Test 11 — Brownfield: imports remain limited to existing paths + the new `./MultimodalWebcam`. No new external dependencies, no edits to MultimodalService.ts.
  </behavior>
  <action>
    Open `chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` and apply six surgical edits. Do NOT rewrite the file — preserve all existing structure, imports, types, refs, helpers, and JSX.

    1. **Import (after line 4):** Add `import { MultimodalWebcam } from './MultimodalWebcam';` immediately after the existing `import { MultimodalInput } from './MultimodalInput';` line. Adjust import order if alphabetical sort matters (the existing file does NOT alphabetize — preserve declaration order).

    2. **New state (after `[isDragOver, ...]` state declaration around line 28):** Add two lines:
       - `const [isLiveActive, setIsLiveActive] = useState(false);`
       - `const [liveResponse, setLiveResponse] = useState<{ text: string } | null>(null);`

    3. **handleLiveChunk callback (insert after `pendingResendBlobsRef` declaration, around line 36):**
       ```typescript
       // Phase 11: accumulates streamed text chunks from the live captureCycle into
       // liveResponse. Reset to null happens via a separate useEffect on isLiveActive
       // flip (start of live session). Note: within a single live session, text from
       // consecutive frames accumulates — full per-frame replacement requires an
       // onFrameStart callback in MultimodalWebcam (deferred follow-up if needed).
       const handleLiveChunk = useCallback((chunk: string) => {
         setLiveResponse((prev) => (prev === null ? { text: chunk } : { text: prev.text + chunk }));
       }, []);
       ```
       Inline this verbatim (you may rewrap the comment, but keep the JSDoc-style trade-off note).

    4. **Reset effect (insert after the existing cleanup useEffect around lines 85–89):**
       ```typescript
       // Phase 11: clear the live response panel each time live mode is (re)started.
       useEffect(() => {
         if (isLiveActive) setLiveResponse(null);
       }, [isLiveActive]);
       ```

    5. **Drag handler gates:**
       - In `handleDragEnter` (around line 173): add `if (isLiveActive) return;` as the FIRST line inside the handler.
       - In `handleDrop` (around line 189): add `if (isLiveActive) return;` as the FIRST line inside the handler (after `e.preventDefault()` if any — actually BEFORE, so the drop is fully ignored).

    6. **JSX changes inside the existing main `<div ... onDragEnter={...}>` wrapper:**

       a. **Transcript visibility toggle** (line 229): change `<div className="flex-1 min-h-0 overflow-y-auto">` to `<div className={isLiveActive ? 'invisible flex-1 min-h-0' : 'flex-1 min-h-0 overflow-y-auto'}>`.

       b. **Live response panel render** (insert AFTER the transcript wrapper and BEFORE the existing `{pageState === 'downloading' && ...}` block — i.e. between current line 231 and current line 234):
          ```tsx
          {isLiveActive && (
            <div className="mt-2 mb-2">
              <div className="p-3 rounded-lg bg-gray-100 dark:bg-gray-700 text-gray-800 dark:text-gray-200 text-base font-medium leading-relaxed animate-fade-in">
                {liveResponse === null ? (
                  <span className="animate-pulse text-gray-500 dark:text-gray-400 font-medium">
                    Thinking…
                  </span>
                ) : (
                  liveResponse.text
                )}
              </div>
            </div>
          )}
          ```

       c. **`<MultimodalInput>` call extension** (lines 249–258): keep all existing props; add TWO new props:
          - `isLiveActive={isLiveActive}`
          - `webcamSlot={<MultimodalWebcam pageState={pageState} livePrompt={text} onFrameAttach={(blob) => setPendingImage(blob)} setIsLiveActive={setIsLiveActive} onLiveChunk={handleLiveChunk} />}`

          The full extended call:
          ```tsx
          <MultimodalInput
            text={text}
            setText={setText}
            pendingImage={pendingImage}
            setPendingImage={setPendingImage}
            onSend={handleSend}
            pageState={pageState}
            mimeError={mimeError}
            setMimeError={setMimeError}
            isLiveActive={isLiveActive}
            webcamSlot={
              <MultimodalWebcam
                pageState={pageState}
                livePrompt={text}
                onFrameAttach={(blob) => setPendingImage(blob)}
                setIsLiveActive={setIsLiveActive}
                onLiveChunk={handleLiveChunk}
              />
            }
          />
          ```

    Do NOT touch the abortController cleanup useEffect (line 85), `runPrompt` (lines 40–81), `handleSend` (lines 94–123), `handleRetry` (lines 128–168), `handleDragOver` (line 179), or `handleDragLeave` (line 183). The download progress bar block (lines 234–246) stays UNCHANGED — it renders even during live mode (a model download mid-live-session is rare but the visual remains valid).
  </action>
  <verify>
    <automated>F=chat/src/app/components/Multimodal/MultimodalChatPanel.tsx; echo "--- grep gates (each &gt;= 1):"; for p in "import { MultimodalWebcam }" "useState(false)" "useState&lt;{ text: string } | null&gt;" "handleLiveChunk" "&lt;MultimodalWebcam" "livePrompt={text}" "onFrameAttach={(blob)" "setIsLiveActive={setIsLiveActive}" "onLiveChunk={handleLiveChunk}" "isLiveActive={isLiveActive}" "webcamSlot={" "isLiveActive \\? 'invisible" "Thinking…" "if (isLiveActive) return;"; do c=$(grep -c "$p" "$F" 2&gt;/dev/null || echo 0); echo "$c  $p"; done; echo "--- regression gates (each &gt;= 1 — must NOT be removed):"; for p in "runPrompt" "handleSend" "handleRetry" "pendingResendBlobsRef" "dragCounterRef" "objectUrlSetRef.current.add" "reader.read()" "releaseLock" "MultimodalTranscript" "downloadPct"; do c=$(grep -c "$p" "$F" 2&gt;/dev/null || echo 0); echo "$c  $p"; done; echo "--- typecheck + build:"; npx nx run chat:typecheck 2&gt;&amp;1 | tail -5; npx nx build chat 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { MultimodalWebcam } from './MultimodalWebcam'" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` returns 1.
    - `grep -c "setIsLiveActive" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` returns at least 2 (state setter + passed to MultimodalWebcam).
    - `grep -c "setLiveResponse" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` returns at least 2 (used in handleLiveChunk + reset effect).
    - `grep -c "handleLiveChunk" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` returns at least 2 (declaration + usage).
    - `grep -c "<MultimodalWebcam" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` returns 1 (single mount).
    - `grep -c "livePrompt={text}" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` returns 1.
    - `grep -c "onFrameAttach={" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` returns 1.
    - `grep -c "onLiveChunk={handleLiveChunk}" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` returns 1.
    - `grep -c "isLiveActive={isLiveActive}" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` returns 1 (passed to MultimodalInput).
    - `grep -c "webcamSlot={" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` returns 1.
    - `grep -F "isLiveActive ? 'invisible" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` matches 1 line (transcript visibility toggle).
    - `grep -F "Thinking…" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` matches at least 1 (live response panel pending state).
    - `grep -c "if (isLiveActive) return;" chat/src/app/components/Multimodal/MultimodalChatPanel.tsx` returns at least 2 (handleDragEnter + handleDrop gates).
    - Regression: `grep -c X` returns at least 1 for each: `runPrompt`, `handleSend`, `handleRetry`, `pendingResendBlobsRef`, `dragCounterRef`, `objectUrlSetRef.current.add`, `reader.read()`, `releaseLock`, `MultimodalTranscript`, `downloadPct`.
    - `grep -v '^ *//\|^ *\*' chat/src/app/components/Multimodal/MultimodalChatPanel.tsx | grep -cE '(: any| as any)'` returns 0.
    - `npx nx run chat:typecheck` exits 0.
    - `npx nx build chat` exits 0.
  </acceptance_criteria>
  <done>
    Chat panel mounts the webcam component via `webcamSlot`, owns `isLiveActive` + `liveResponse` state, toggles transcript visibility, renders the live response panel between transcript and download-progress slots, and gates drag handlers when live mode is active. End-to-end webcam flows are runtime-usable on Chrome 148+.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Typecheck:** `npx nx run chat:typecheck` exits 0.
2. **Build:** `npx nx build chat` exits 0.
3. **Brownfield diff scope:** `git diff --name-only HEAD~1..HEAD` (or `--name-only` against the 11-01 commit) shows EXACTLY these paths changed:
   - `chat/src/app/components/Multimodal/MultimodalChatPanel.tsx`
   - `chat/src/app/components/Multimodal/MultimodalInput.tsx`
   No changes to `MultimodalService.ts`, `MultimodalPage.tsx`, `MultimodalTranscript.tsx`, `MultimodalWebcam.tsx`, `imageFileValidation.ts`, `AppRouter.tsx`, `useSEOData.ts`, or any v1.0/v1.1 file.
4. **End-to-end flow (manual smoke, Chrome 148+):**
   - Visit `/multimodal` → tools row shows "Take photo" + "Live mode" pill buttons above the textarea.
   - Click "Take photo" → camera permission prompt → 240×180 video preview appears with Capture/Cancel overlay.
   - Click "Capture" → thumbnail attaches in the existing pendingImage slot; type a question; click Send → assistant streams response (Phase 10 flow unchanged).
   - Type a question → click "Live mode" → textarea disables (opacity-60); prompt-lock badge replaces send button; transcript hides; live response panel shows "Thinking…" → first frame streams in (`3s interval · last: N ms` badge bottom-right of video).
   - Click "Stop live" → transcript reappears; live response panel unmounts; "Take photo" + "Live mode" buttons re-enabled.
   - Deny camera permission → "Camera blocked" card with `chrome://settings/content/camera` click-to-copy code element.
5. **No new packages:** `git diff package.json package-lock.json` is empty.
</verification>

<success_criteria>
- All four phase requirement IDs (MULTI-04, MULTI-05, MULTI-08, POLISH-02) are end-to-end demo-able per the ROADMAP Phase 11 success criteria (lines 172–178).
- Single-frame snap reuses 100% of the Phase 10 send flow — no Blob path duplication.
- Live mode: 3s interval with `setInterval`, single-in-flight gating, 512×512 downsample, session reuse (free side-effect of module-scope `sessionPromise`), perf badge with time-to-first-token latency.
- Camera permission UX: error cards for `NotAllowedError`, `NotFoundError`/`OverconstrainedError`, `NotReadableError`, plus `unknown` fallback. `chrome://` URL rendered as click-to-copy `<code>` (never `<a href="chrome://...">`).
- Brownfield discipline: only `MultimodalChatPanel.tsx` + `MultimodalInput.tsx` modified in this plan; new file `MultimodalWebcam.tsx` and augmented `dom-chromium-ai.d.ts` came from Plan 11-01.
- Phase 11 ROADMAP entry can flip from `[ ]` to `[x]` once UAT (Phase 12 POLISH-01 rehearsal) signs off.
</success_criteria>

<output>
After completion, create `.planning/phases/11-webcam-capture/11-02-SUMMARY.md` with:
- Task commits + line-diff counts for both files
- Confirmation that all 4 REQ-IDs (MULTI-04, MULTI-05, MULTI-08, POLISH-02) are runtime-wired
- Typecheck/build status
- Cross-check table (the grep gates that passed)
- Per-frame replacement trade-off note (the within-session accumulation pattern documented in Task 2 Behavior Test 4)
- Phase 12 hand-off: confirm `MultimodalService.ts` is now exercised in three input modes (drag, paste, webcam-snap, webcam-live), readying the POLISH-01 5-cold-run rehearsal log
</output>
