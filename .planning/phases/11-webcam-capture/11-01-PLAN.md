---
phase: 11-webcam-capture
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - chat/src/app/types/dom-chromium-ai.d.ts
  - chat/src/app/components/Multimodal/MultimodalWebcam.tsx
autonomous: true
requirements: [MULTI-04, MULTI-05, MULTI-08, POLISH-02]
tags: [webcam, multimodal, image-capture, getUserMedia, type-augmentation, live-mode, single-in-flight]
must_haves:
  truths:
    - "TypeScript compiles when MultimodalWebcam.tsx calls imageCaptureRef.current.grabFrame() — grabFrame is declared on ImageCapture interface via dom-chromium-ai.d.ts augmentation"
    - "Clicking 'Take photo' (and only on the user click — never on mount) calls navigator.mediaDevices.getUserMedia({video:{width:{ideal:1280},height:{ideal:720},facingMode:'user'},audio:false}) and renders a 240x180 live video preview with Capture + Cancel overlay buttons"
    - "Clicking Capture sets canvas.width/canvas.height to video.videoWidth/videoHeight, draws the video, calls canvas.toBlob(cb, 'image/jpeg', 0.92), invokes onFrameAttach(blob) when the blob is non-null, stops all MediaStream tracks, and returns to idle mode"
    - "Clicking Cancel stops all MediaStream tracks and returns to idle mode without invoking onFrameAttach"
    - "Clicking 'Live mode' (when idle) opens a fresh MediaStream, constructs new ImageCapture(stream.getVideoTracks()[0]), creates a fresh AbortController (never reuses an aborted one), starts setInterval(captureCycle, 3000), calls setIsLiveActive(true), and renders the live video preview with the perf badge bottom-right"
    - "Each captureCycle awaits imageCaptureRef.current.grabFrame() then createImageBitmap(frame, {resizeWidth:512, resizeHeight:512}), explicitly calls frameBitmap.close() immediately after the downsample and downsampled.close() in the finally block, and posts the downsampled bitmap to promptWithImage(prompt, downsampled, {signal})"
    - "Live mode reads livePrompt via livePromptRef.current inside setInterval (not from the closure) — the prompt updates correctly across cycles when the parent passes a new livePrompt prop; empty trim falls back to 'Describe what you see in this image'"
    - "When inFlightRef.current is true at the start of a captureCycle, the cycle returns early, setWasSkipped(true) flashes the perf badge to '3s interval · skipped (in flight)' for 500ms, and no concurrent promptWithImage is issued"
    - "Streamed live response is read via reader.read() loop (NOT for-await-of), with reader.releaseLock() in finally; the first chunk records lastLatencyMs = round(performance.now() - t0); each chunk calls onLiveChunk(text); AbortError from cancellation is swallowed silently"
    - "Clicking 'Stop live' (or unmounting the component) clears the interval, aborts abortControllerRef, stops all stream tracks, nulls streamRef AND imageCaptureRef together, resets inFlightRef to false, calls setIsLiveActive(false), and returns to idle mode"
    - "getUserMedia DOMException is mapped: NotAllowedError -> 'blocked' (shows 'Camera blocked' card with chrome://settings/content/camera as a click-to-copy <code> element), NotFoundError/OverconstrainedError -> 'no-camera' ('No camera detected'), NotReadableError -> 'in-use' ('Camera in use'), else 'unknown'; the error card occupies the same slot as the video preview and has a dismiss × that returns to idle"
    - "Perf badge bottom-right of live video shows '3s interval · …' on first cycle, '3s interval · last: {N} ms' on subsequent cycles, '3s interval · skipped (in flight)' for 500ms when a cycle is dropped; classes are exactly 'absolute bottom-2 right-2 bg-black/70 text-white text-xs font-medium px-2 py-1 rounded'"
    - "Take photo and Live mode buttons are pill-style (px-3 py-2 rounded-lg, text-sm font-medium) with the focus ring focus:ring-primary-500 focus:ring-offset-2 dark:focus:ring-offset-gray-800; Live mode active state uses bg-primary-600 text-white"
    - "captureCycle returns early when pageState !== 'ready' (RESEARCH Pitfall 8 guard)"
  artifacts:
    - path: chat/src/app/types/dom-chromium-ai.d.ts
      provides: "Adds grabFrame(): Promise<ImageBitmap> to global ImageCapture interface via declaration merging — fixes TS 5.9.2 lib.dom.d.ts omission"
      contains: "interface ImageCapture"
    - path: chat/src/app/components/Multimodal/MultimodalWebcam.tsx
      provides: "Self-contained webcam component — owns MediaStream lifecycle, single-frame canvas capture path, live mode loop with single-in-flight gating + 512x512 downsample + bitmap close(), error cards, perf badge"
      exports: ["MultimodalWebcam", "MultimodalWebcamProps"]
      min_lines: 250
  key_links:
    - from: chat/src/app/components/Multimodal/MultimodalWebcam.tsx
      to: chat/src/app/services/MultimodalService.ts
      via: "import { promptWithImage } from '../../services/MultimodalService'"
      pattern: "promptWithImage\\(.*downsampled"
    - from: chat/src/app/components/Multimodal/MultimodalWebcam.tsx
      to: chat/src/app/types/dom-chromium-ai.d.ts
      via: "imageCaptureRef.current!.grabFrame() — type resolves through declaration merging"
      pattern: "grabFrame\\(\\)"
---

<objective>
Phase 11 hard-blocker plan: ship the `MultimodalWebcam.tsx` component and the
`ImageCapture.grabFrame()` TypeScript augmentation. This plan owns the webcam
lifecycle in isolation — no chat-panel wiring yet. After this plan ships, the
component is buildable and unit-importable; Plan 11-02 mounts it inside
`MultimodalChatPanel.tsx` and `MultimodalInput.tsx`.

Purpose: deliver MULTI-04 (single-frame snap), MULTI-05 (continuous live mode
with gating + downsample + session reuse), MULTI-08 (camera permission UX),
and POLISH-02 (perf indicator) as a self-contained component. The
`dom-chromium-ai.d.ts` augmentation is required FIRST because `grabFrame()`
is absent from TypeScript 5.9.2's `lib.dom.d.ts` — without this one line, the
component does not type-check (RESEARCH Pitfall 1).

Output:
- `chat/src/app/types/dom-chromium-ai.d.ts` extended with `interface ImageCapture { grabFrame(): Promise<ImageBitmap>; }`
- `chat/src/app/components/Multimodal/MultimodalWebcam.tsx` (NEW) — full state machine for `idle | preview | live | error` modes, with all 8 RESEARCH pitfalls mitigated
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-webcam-capture/11-CONTEXT.md
@.planning/phases/11-webcam-capture/11-RESEARCH.md
@.planning/phases/11-webcam-capture/11-PATTERNS.md
@.planning/phases/11-webcam-capture/11-UI-SPEC.md
@chat/src/app/services/MultimodalService.ts
@chat/src/app/components/Multimodal/MultimodalChatPanel.tsx
@chat/src/app/components/Multimodal/MultimodalPage.tsx
@chat/src/app/components/MissingFlagBanner.tsx
@chat/src/app/types/dom-chromium-ai.d.ts

<interfaces>
<!-- Contracts the executor needs without exploring the codebase. -->

From chat/src/app/services/MultimodalService.ts (UNCHANGED by this plan):
```typescript
export function promptWithImage(
  text: string,
  image: Blob | ImageBitmap,   // accepts both — webcam live passes ImageBitmap from grabFrame()
  opts?: { signal?: AbortSignal }
): Promise<ReadableStream<string>>;
```

From chat/src/app/components/Multimodal/MultimodalPage.tsx (UNCHANGED by this plan):
```typescript
export type PageState = 'idle' | 'unavailable' | 'downloading' | 'ready' | 'prompting' | 'error';
```

NEW PROPS contract (MultimodalWebcam — locked by CONTEXT.md):
```typescript
interface MultimodalWebcamProps {
  pageState: PageState;
  livePrompt: string;                        // textarea text (or '' for empty)
  onFrameAttach: (blob: Blob) => void;       // single-frame path — parent calls setPendingImage
  setIsLiveActive: (b: boolean) => void;     // notifies parent so textarea + send button can be disabled
  onLiveChunk: (text: string) => void;       // accumulates into liveResponse state in parent
}
```

EXISTING augmentation pattern (dom-chromium-ai.d.ts lines 1–198):
```typescript
declare global {
  interface LanguageModelParams { /* ... */ }
  // additional interfaces follow same shape
}
```

EXISTING streaming reader pattern (MultimodalChatPanel.tsx lines 48–61):
```typescript
const reader = stream.getReader();
try {
  while (true) {
    const { value, done } = await reader.read();
    if (done) break;
    // process value
  }
} finally {
  reader.releaseLock();
}
```

EXISTING DOMException AbortError pattern (MultimodalChatPanel.tsx lines 65–68):
```typescript
if (err instanceof DOMException && err.name === 'AbortError') {
  return; // silent — intentional cancel
}
```

EXISTING cleanup-only useEffect pattern (MultimodalChatPanel.tsx lines 85–89):
```typescript
useEffect(() => {
  return () => {
    abortControllerRef.current?.abort();
  };
}, []);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Augment ImageCapture interface with grabFrame() in dom-chromium-ai.d.ts</name>
  <files>chat/src/app/types/dom-chromium-ai.d.ts</files>
  <read_first>
    - chat/src/app/types/dom-chromium-ai.d.ts (READ FULL — see existing `declare global { interface ... }` blocks and the closing brace at line 198)
    - .planning/phases/11-webcam-capture/11-RESEARCH.md § Pattern 1 (grabFrame TypeScript Augmentation) — lines 218–235
    - .planning/phases/11-webcam-capture/11-PATTERNS.md § dom-chromium-ai.d.ts — lines 26–55
  </read_first>
  <behavior>
    - Before: `const ic = new ImageCapture(track); ic.grabFrame()` -> TS error "Property 'grabFrame' does not exist on type 'ImageCapture'"
    - After: `const ic = new ImageCapture(track); ic.grabFrame()` -> resolves to `Promise<ImageBitmap>`
    - Existing declarations (LanguageModel, Proofreader, Translator, etc.) remain typecheck-clean — declaration merging is additive, never replacing.
  </behavior>
  <action>
    Append a new `declare global` block AFTER the closing `}` of the existing `declare global` (currently at line 198 — i.e. after `interface Window { ... }`) that augments the `ImageCapture` interface with a single `grabFrame(): Promise<ImageBitmap>` member.

    The new block:
    1. Includes a comment block referencing why this augmentation is needed: `grabFrame()` is absent from TypeScript 5.9.2's lib.dom.d.ts; the `ImageCapture` constructor + takePhoto/getPhotoCapabilities/getPhotoSettings/track members live in the lib already, and this augmentation merges with them. Cite MDN URL `https://developer.mozilla.org/en-US/docs/Web/API/ImageCapture/grabFrame`.
    2. Uses TypeScript declaration merging: `declare global { interface ImageCapture { grabFrame(): Promise<ImageBitmap>; } }` — exactly one method, return type `Promise<ImageBitmap>`, no parameters.
    3. Do NOT touch any other interface in the file. Do NOT add `MultimodalLanguageModel` or any multimodal promptStreaming overload here (Phase 10 PATTERNS note 2 — that pattern stays inside MultimodalService.ts).
    4. The file already has a single `declare global { ... }` block (lines 2–198). You may EITHER (a) add the `interface ImageCapture { ... }` inside the existing `declare global` (before the closing `}`), or (b) append a NEW `declare global { interface ImageCapture { ... } }` block after the existing one. Either is valid TS declaration merging — pick (b) (less risk of touching unrelated code).

    Do not invent a fallback or stub — the augmentation must declare exactly `grabFrame(): Promise<ImageBitmap>`.
  </action>
  <verify>
    <automated>npx nx run chat:typecheck 2>&amp;1 | grep -i "grabFrame\|MultimodalWebcam" | grep -ic "error TS" | grep -q "^0$" || echo "OK_NO_ERRORS"; npx nx run chat:typecheck 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "grabFrame" chat/src/app/types/dom-chromium-ai.d.ts` returns at least 1
    - `grep -c "interface ImageCapture" chat/src/app/types/dom-chromium-ai.d.ts` returns at least 1
    - `grep -c "Promise<ImageBitmap>" chat/src/app/types/dom-chromium-ai.d.ts` returns at least 1
    - `npx nx run chat:typecheck` shows zero new errors compared to baseline (file is still legal TS)
    - No content removed from existing declarations (`grep -c "LanguageModel" chat/src/app/types/dom-chromium-ai.d.ts` unchanged; `grep -c "Proofreader" chat/src/app/types/dom-chromium-ai.d.ts` unchanged)
  </acceptance_criteria>
  <done>Augmentation present; existing typechecks still pass; Task 2 can call `grabFrame()` without TS errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create MultimodalWebcam.tsx with full state machine, capture paths, live loop, error cards, and perf badge</name>
  <files>chat/src/app/components/Multimodal/MultimodalWebcam.tsx</files>
  <read_first>
    - .planning/phases/11-webcam-capture/11-CONTEXT.md (FULL — all locked decisions)
    - .planning/phases/11-webcam-capture/11-RESEARCH.md § Patterns 1–6 + Pitfalls 1–8 (lines 218–505)
    - .planning/phases/11-webcam-capture/11-PATTERNS.md § MultimodalWebcam.tsx — lines 58–306
    - .planning/phases/11-webcam-capture/11-UI-SPEC.md § Component Specifications (lines 108–292) — tools row, video preview, perf badge, error cards
    - chat/src/app/components/Multimodal/MultimodalChatPanel.tsx (FULL — copy the reader.read() loop pattern lines 48–61, AbortError swallow lines 63–68, cleanup effect lines 85–89)
    - chat/src/app/components/Multimodal/MultimodalPage.tsx (READ — confirm PageState type export shape and import path `./MultimodalPage`)
    - chat/src/app/services/MultimodalService.ts (READ — confirm `promptWithImage(text, Blob | ImageBitmap, { signal }): Promise<ReadableStream<string>>` signature; the import path from a Multimodal/ subdir is `../../services/MultimodalService`)
    - chat/src/app/components/MissingFlagBanner.tsx (READ — the existing `<code>` chrome:// pattern; Phase 11 extends with `onClick` copy-to-clipboard)
  </read_first>
  <behavior>
    - Component exports `MultimodalWebcam` (named export) and `MultimodalWebcamProps` (named export interface).
    - Test 1 — Props contract: `MultimodalWebcamProps` matches the locked shape: `pageState`, `livePrompt`, `onFrameAttach`, `setIsLiveActive`, `onLiveChunk` (all required).
    - Test 2 — Default mode: on mount, mode is `'idle'` and NO camera is requested. `getUserMedia` is called only inside `handleTakePhoto` or `handleLiveMode` (user-click handlers).
    - Test 3 — Tools row visible in idle mode: two pill buttons render — "Take photo" and "Live mode". Both are disabled when `pageState !== 'ready'`. "Take photo" is also disabled when live mode is active. "Live mode" is also disabled when in preview mode.
    - Test 4 — Single-frame capture flow: clicking "Take photo" → mode becomes `'preview'` → video element shows live stream → clicking "Capture" sets `canvas.width = video.videoWidth`, `canvas.height = video.videoHeight`, draws the video, calls `canvas.toBlob((blob) => blob &amp;&amp; onFrameAttach(blob), 'image/jpeg', 0.92)`, stops the stream, returns to `'idle'`.
    - Test 5 — Cancel during preview: clicking "Cancel" stops stream, returns to `'idle'`, does NOT invoke `onFrameAttach`.
    - Test 6 — Live mode start: clicking "Live mode" (when `mode === 'idle'`) → opens fresh stream, constructs new ImageCapture, creates FRESH AbortController (never reuses), starts `setInterval(captureCycle, 3000)`, sets mode to `'live'`, calls `setIsLiveActive(true)`.
    - Test 7 — captureCycle gating: when `inFlightRef.current === true`, the cycle returns immediately, sets `wasSkipped` to true (with a 500ms auto-clear via setTimeout), and does NOT call `promptWithImage`.
    - Test 8 — captureCycle pipeline: when `inFlightRef.current === false` AND `pageState === 'ready'`, the cycle sets `inFlightRef.current = true`, captures `t0 = performance.now()`, awaits `grabFrame()`, awaits `createImageBitmap(frame, {resizeWidth:512, resizeHeight:512})`, immediately calls `frameBitmap.close()`, awaits `promptWithImage(promptText, downsampled, {signal})`, streams via reader.read() loop, sets `lastLatencyMs = round(performance.now() - t0)` on the first chunk, calls `onLiveChunk(text)` for each chunk, and in the `finally` block calls `downsampled?.close()` and resets `inFlightRef.current = false`.
    - Test 9 — Stale-closure mirror: `livePromptRef.current` is updated via a `useEffect(() => { livePromptRef.current = livePrompt; }, [livePrompt])`. Inside captureCycle, `promptText` reads `livePromptRef.current.trim() || 'Describe what you see in this image'` (NOT the prop directly).
    - Test 10 — Live mode stop: clicking "Stop live" → `clearInterval(intervalRef.current)`, `abortControllerRef.current.abort()`, `stream.getTracks().forEach(t => t.stop())`, null `streamRef.current` AND `imageCaptureRef.current` TOGETHER, `inFlightRef.current = false`, `setIsLiveActive(false)`, mode returns to `'idle'`.
    - Test 11 — Cleanup effect: single `useEffect(() => () => cleanup(), [])` with empty deps stops all tracks, aborts the controller, clears the interval, resets inFlightRef. Optional chaining handles the StrictMode null case.
    - Test 12 — Error mapping: `getUserMedia` catch — `DOMException.name === 'NotAllowedError'` → `setErrorState('blocked')`; `'NotFoundError'` or `'OverconstrainedError'` → `'no-camera'`; `'NotReadableError'` → `'in-use'`; anything else (including non-DOMException) → `'unknown'`. In all cases mode becomes `'error'`.
    - Test 13 — Error cards: each variant shows an icon, heading, body, and a dismiss × button. `'blocked'` variant ALSO renders `chrome://settings/content/camera` as a `<code>` element with `onClick={() => navigator.clipboard.writeText('chrome://settings/content/camera')}` (NOT an `<a href>`). Dismissing returns mode to `'idle'`.
    - Test 14 — Perf badge: bottom-right of live video; classes EXACTLY `absolute bottom-2 right-2 bg-black/70 text-white text-xs font-medium px-2 py-1 rounded`; content `'3s interval · …'` (first cycle), `'3s interval · last: {N} ms'` (subsequent), `'3s interval · skipped (in flight)'` (during 500ms skip flash). `aria-live="off"`.
    - Test 15 — AbortError swallow: if `promptWithImage` throws `AbortError` inside captureCycle (i.e. live mode was stopped while a frame was in flight), the catch returns silently without setting any error state.
    - Test 16 — Brownfield: imports only from `./MultimodalPage` (PageState type) and `../../services/MultimodalService` (promptWithImage). No imports from `MultimodalChatPanel.tsx`, `MultimodalTranscript.tsx`, `MultimodalInput.tsx`, or any v1.0/v1.1 files.
  </behavior>
  <action>
    Create `chat/src/app/components/Multimodal/MultimodalWebcam.tsx` as a NEW file. The component owns its full lifecycle. No other files are touched in this task.

    Required structure (compose from RESEARCH + PATTERNS — do NOT inline implementation code beyond named symbols and signatures below):

    1. Imports: `React`, `{ useCallback, useEffect, useRef, useState }` from `'react'`; `{ type PageState }` from `'./MultimodalPage'`; `{ promptWithImage }` from `'../../services/MultimodalService'`.

    2. Type exports (named, not default):
       - `interface MultimodalWebcamProps` with the locked five-prop shape above.
       - Local types (do NOT export): `type WebcamMode = 'idle' | 'preview' | 'live' | 'error'`; `type WebcamErrorState = 'blocked' | 'no-camera' | 'in-use' | 'unknown' | null`.

    3. Component declaration: `export const MultimodalWebcam: React.FC&lt;MultimodalWebcamProps&gt; = (...) =&gt; { ... }`.

    4. Ref inventory (use `useRef` for ALL non-reactive state):
       - `streamRef: MediaStream | null`
       - `videoRef: HTMLVideoElement | null`
       - `canvasRef: HTMLCanvasElement | null`
       - `imageCaptureRef: ImageCapture | null`
       - `intervalRef: ReturnType<typeof setInterval> | null`
       - `inFlightRef: boolean` (init `false`)
       - `abortControllerRef: AbortController | null`
       - `livePromptRef: string` (init `livePrompt`, mirrored via effect — RESEARCH Pitfall 5)

    5. State (use `useState` for render-driving values only):
       - `mode: WebcamMode` (init `'idle'`)
       - `errorState: WebcamErrorState` (init `null`)
       - `lastLatencyMs: number | null` (init `null`)
       - `wasSkipped: boolean` (init `false`)
       - `captureReady: boolean` (init `false`) — set true on `<video onLoadedMetadata>` (Pitfall 7 + RESEARCH Open Question 1)

    6. Effects:
       - Stale-closure mirror: `useEffect(() => { livePromptRef.current = livePrompt; }, [livePrompt]);`
       - Single cleanup effect with empty deps: `useEffect(() => () => { /* stopStream(); abort; clearInterval; reset inFlightRef */ }, [])` — see PATTERNS lines 113–127.

    7. Helpers (defined inside the component or as inner functions — do NOT export):
       - `stopStream()`: stops all tracks, nulls streamRef AND imageCaptureRef together (PATTERNS lines 201–207).
       - `mapMediaError(err: unknown): WebcamErrorState` per RESEARCH Example 3 — returns `'blocked' | 'no-camera' | 'in-use' | 'unknown'`.
       - `handleTakePhoto()`: async user-click handler. Calls `getUserMedia` with locked constraints (per CONTEXT.md § getUserMedia). On success: assigns `srcObject`, calls `play()`, sets mode `'preview'`. On error: sets errorState via mapMediaError, sets mode `'error'`.
       - `handleCapture()`: guards `if (!video.videoWidth || !video.videoHeight) return;` (Pitfall 7). Sets canvas dims, draws video, `canvas.toBlob((blob) => { if (blob) onFrameAttach(blob); stopStream(); setMode('idle'); }, 'image/jpeg', 0.92)`. NOTE: this is a callback, NOT a promise (Pitfall 3).
       - `handleCancel()`: stopStream(); setMode('idle').
       - `handleLiveModeStart()`: async user-click handler. Calls getUserMedia, builds ImageCapture, creates FRESH `new AbortController()` (Pitfall 6), starts `setInterval(captureCycle, 3000)`, calls `setIsLiveActive(true)`, sets mode `'live'`, resets lastLatencyMs to null and wasSkipped to false. On error: same mapping path as handleTakePhoto.
       - `handleLiveModeStop()`: `clearInterval`, `abortControllerRef.current?.abort()`, `stopStream()`, `inFlightRef.current = false`, `setIsLiveActive(false)`, setMode('idle'). NOTE: do NOT clear lastLatencyMs here so the last value lingers briefly if user immediately re-enters — that's fine; the next start resets to null.
       - `captureCycle()`: async; structure per CONTEXT.md § Live Mode Loop + RESEARCH Pattern 3:
         a. Early return if `inFlightRef.current === true` → setWasSkipped(true), setTimeout(setWasSkipped(false), 500).
         b. Early return if `pageState !== 'ready'` (Pitfall 8).
         c. `inFlightRef.current = true`; `const t0 = performance.now(); let downsampled: ImageBitmap | undefined;`
         d. try: `frameBitmap = await imageCaptureRef.current!.grabFrame(); downsampled = await createImageBitmap(frameBitmap, {resizeWidth:512, resizeHeight:512}); frameBitmap.close();`
         e. `const promptText = livePromptRef.current.trim() || 'Describe what you see in this image';`
         f. `const stream = await promptWithImage(promptText, downsampled, { signal: abortControllerRef.current?.signal });`
         g. Read via `reader.read()` loop with `releaseLock()` in finally (Pitfall 1 — copy from MultimodalChatPanel.tsx lines 48–61). On first chunk: `setLastLatencyMs(Math.round(performance.now() - t0))`. Each chunk: `onLiveChunk(value)`.
         h. catch (err): if `err instanceof DOMException && err.name === 'AbortError'` → return silently. Otherwise: log (do NOT throw); planner may optionally call `setIsLiveActive(false)` + `handleLiveModeStop()` on non-AbortError per RESEARCH Pitfall 8 recommendation.
         i. finally: `downsampled?.close(); inFlightRef.current = false;`
       - `handleDismissError()`: setErrorState(null); setMode('idle').
       - `handleCopyChromeUrl()`: `navigator.clipboard.writeText('chrome://settings/content/camera')`.

    8. JSX (per UI-SPEC lines 108–292):
       - Outer wrapper: a `<div>` (no fixed sizing on the wrapper — children own sizing). Order of children:
         a. Tools row (always rendered) — `<div className="flex items-center gap-2 mb-2">` with two `<button>` elements: "Take photo" pill (camera SVG `w-4 h-4 aria-hidden` + label "Take photo") and Live mode pill (red-circle SVG `w-3 h-3 aria-hidden` + label `"Live mode"` when mode !== 'live', `"Stop live"` when mode === 'live'). Active live state uses `bg-primary-600 text-white` (UI-SPEC Color § 8); idle uses `bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300`. Both buttons include the focus ring `focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 dark:focus:ring-offset-gray-800` and `transition-colors duration-200`. Take photo disabled when `mode !== 'idle' || pageState !== 'ready'`. Live mode disabled when `mode === 'preview' || pageState !== 'ready'`.
         b. Video preview slot — rendered when `mode === 'preview' || mode === 'live'`. Container `<div className="relative w-60 h-44 mb-2 rounded-lg overflow-hidden bg-gray-100 dark:bg-gray-700">` with `<video ref={videoRef} className="w-full h-full object-cover animate-fade-in" autoPlay muted playsInline aria-label="Webcam live preview" onLoadedMetadata={() => setCaptureReady(true)} />`. Capture/Cancel overlay rendered when `mode === 'preview'` (UI-SPEC § Video Preview lines 159–175). Perf badge rendered when `mode === 'live'` per badge classes above; `aria-live="off"`.
         c. Hidden canvas — `<canvas ref={canvasRef} className="hidden" aria-hidden="true" />` (used by handleCapture).
         d. Error card slot — rendered when `mode === 'error' && errorState !== null`, occupies the SAME 240-wide slot as the video preview. Container `<div className="w-60 mb-2 p-4 rounded-lg bg-gray-100 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 relative animate-fade-in">`. Dismiss × button absolute top-2 right-2 with `aria-label="Dismiss camera error"`. Variants (UI-SPEC table lines 226–233):
            - `'blocked'`: heading "Camera blocked" + body "Enable in browser settings" + the `<code>` block with `onClick={handleCopyChromeUrl}` showing `chrome://settings/content/camera`, classes per PATTERNS lines 244–256.
            - `'no-camera'`: heading "No camera detected" + body "Connect a camera and try again".
            - `'in-use'`: heading "Camera in use" + body "Close other apps using the camera and try again".
            - `'unknown'`: heading "Camera error" + body `{err.message}` truncated to 80 chars (or a generic "Unknown camera error" if no message captured).
         e. NO transcript or live response panel here — those live in MultimodalChatPanel.tsx (Plan 11-02).

    9. Type rules (CLAUDE.md):
       - No `: any`, no `as any`. The `imageCaptureRef.current!` non-null assertion is allowed inside captureCycle since the cycle only fires after `handleLiveModeStart` constructs the ImageCapture.
       - `ImageCapture` symbol comes from the lib (constructor) + Task 1 augmentation (grabFrame); no local `interface ImageCapture` declaration.

    10. Accessibility (UI-SPEC § Accessibility Notes lines 506–517):
        - All buttons have visible text or `aria-label`.
        - `<video>` has `aria-label="Webcam live preview"`; container is NOT aria-hidden.
        - Perf badge: `aria-live="off"`.
        - Dismiss × button: `aria-label="Dismiss camera error"`.
        - chrome:// code: `title="Click to copy"` + `aria-label="Copy chrome://settings/content/camera to clipboard"`.

    Do NOT introduce new Tailwind tokens — every class must already exist in `chat/tailwind.config.js` or in the Phase 10 ChatPanel. Do NOT add `animate-` keyframes (use existing `animate-fade-in` and `animate-pulse`). Do NOT add new npm packages.

    Do NOT modify `MultimodalChatPanel.tsx`, `MultimodalInput.tsx`, `MultimodalService.ts`, or any other existing file in this task — Plan 11-02 owns those edits.
  </action>
  <verify>
    <automated>npx nx run chat:typecheck 2>&amp;1 | tail -5; F=chat/src/app/components/Multimodal/MultimodalWebcam.tsx; test -f "$F" || { echo "MISSING"; exit 1; }; echo "--- grep gates (each &gt;= 1):"; for p in "export const MultimodalWebcam" "export interface MultimodalWebcamProps" "getUserMedia" "new ImageCapture" "grabFrame" "createImageBitmap" "resizeWidth: 512" "resizeHeight: 512" "frameBitmap.close" "downsampled?.close\\|downsampled.close" "inFlightRef.current" "livePromptRef.current" "setInterval" "clearInterval" "abortControllerRef.current" "reader.read" "releaseLock" "AbortError" "NotAllowedError" "NotFoundError\\|OverconstrainedError" "NotReadableError" "navigator.clipboard.writeText" "'chrome://settings/content/camera'" "absolute bottom-2 right-2 bg-black/70" "onLoadedMetadata" "performance.now()" "Describe what you see in this image" "playsInline" "aria-label=\"Webcam live preview\"" "aria-label=\"Dismiss camera error\""; do c=$(grep -c "$p" "$F" 2&gt;/dev/null || echo 0); echo "$c  $p"; done; echo "--- forbidden (each == 0):"; for p in ": any" " as any" "for await" "<a href=\"chrome://"; do c=$(grep -v '^ *//\\|^ *\\*' "$F" | grep -c "$p" 2&gt;/dev/null || echo 0); echo "$c  $p"; done</automated>
  </verify>
  <acceptance_criteria>
    - File `chat/src/app/components/Multimodal/MultimodalWebcam.tsx` exists and is at least 250 lines.
    - `grep -c "export const MultimodalWebcam" $F` returns 1; `grep -c "export interface MultimodalWebcamProps" $F` returns 1.
    - `grep -c "getUserMedia" $F` returns at least 2 (one in handleTakePhoto, one in handleLiveModeStart).
    - `grep -c "new ImageCapture" $F` returns at least 1 (live mode start only).
    - `grep -c "grabFrame" $F` returns at least 1 (in captureCycle).
    - `grep -c "createImageBitmap" $F` returns at least 1; `grep -c "resizeWidth: 512" $F` returns 1; `grep -c "resizeHeight: 512" $F` returns 1.
    - `grep -c "frameBitmap.close" $F` returns at least 1.
    - `grep -cE "downsampled\\??\\.close" $F` returns at least 1 (inside `finally`).
    - `grep -c "inFlightRef.current" $F` returns at least 3 (gate read, set true, set false in finally).
    - `grep -c "livePromptRef.current" $F` returns at least 1.
    - `grep -c "setInterval" $F` returns at least 1; `grep -c "clearInterval" $F` returns at least 1.
    - `grep -c "abortControllerRef.current" $F` returns at least 3 (signal pass, abort on stop, abort in cleanup).
    - `grep -c "new AbortController()" $F` returns at least 1 (fresh per live-start — RESEARCH Pitfall 6).
    - `grep -c "reader.read" $F` returns at least 1; `grep -c "releaseLock" $F` returns at least 1 (inside `finally`).
    - `grep -cE "for await" $F` returns 0 (RESEARCH anti-pattern).
    - `grep -c "AbortError" $F` returns at least 1 (silent swallow).
    - `grep -c "NotAllowedError" $F` returns at least 1; `grep -cE "NotFoundError|OverconstrainedError" $F` returns at least 1; `grep -c "NotReadableError" $F` returns at least 1.
    - `grep -c "navigator.clipboard.writeText" $F` returns at least 1; `grep -c "'chrome://settings/content/camera'" $F` returns at least 1.
    - `grep -cE '&lt;a [^&gt;]*href="chrome://' $F` returns 0 (browser blocks JS chrome:// nav — must be `&lt;code&gt;` copy element).
    - `grep -F "absolute bottom-2 right-2 bg-black/70" $F` matches at least 1 line (perf badge exact classes).
    - `grep -c "onLoadedMetadata" $F` returns at least 1 (Pitfall 7 — captureReady gating).
    - `grep -c "Describe what you see in this image" $F` returns at least 1 (livePrompt empty fallback).
    - `grep -cE '(: any|as any)' "$F"` returns 0 ignoring comments — use `grep -v '^ *//\\|^ *\\*' $F | grep -cE '(: any|as any)'` (CLAUDE.md TS strict).
    - `npx nx run chat:typecheck` exits 0 (zero new errors); `npx nx build chat` succeeds.
  </acceptance_criteria>
  <done>
    `MultimodalWebcam.tsx` compiles and builds cleanly. Component encapsulates all camera lifecycle, single-frame canvas capture, live mode loop with single-in-flight gating + 512x512 downsample + explicit bitmap close, error cards with chrome:// copy pattern, and perf badge. No edits to any other Phase 10 file — Plan 11-02 wires it in next.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Typecheck:** `npx nx run chat:typecheck` exits 0. Specifically, the `grabFrame` augmentation enables `imageCaptureRef.current!.grabFrame()` to resolve to `Promise<ImageBitmap>` without error.
2. **Build:** `npx nx build chat` exits 0. The new component is included in the bundle (chunk name may vary).
3. **Brownfield discipline:** `git diff --name-only` shows EXACTLY two changed paths:
   - `chat/src/app/types/dom-chromium-ai.d.ts` (additive — declaration merging)
   - `chat/src/app/components/Multimodal/MultimodalWebcam.tsx` (new file)
   No edits to `MultimodalChatPanel.tsx`, `MultimodalInput.tsx`, `MultimodalService.ts`, `MultimodalPage.tsx`, `MultimodalTranscript.tsx`, `imageFileValidation.ts`, or any v1.0/v1.1 files (no `/webmcp` or `/generative-ui` modifications).
4. **Type strictness:** `grep -v '^ *//\|^ *\*' chat/src/app/components/Multimodal/MultimodalWebcam.tsx | grep -cE '(: any| as any)'` returns 0.
5. **No new dependencies:** `git diff package.json package-lock.json` shows no changes.

`/multimodal` is NOT runtime-usable for webcam yet — the component is built but unmounted. Plan 11-02 mounts it in MultimodalChatPanel and adds the prompt-lock gating to MultimodalInput.
</verification>

<success_criteria>
- Phase 11 hard blocker (grabFrame TS error) is eliminated for the entire codebase.
- `MultimodalWebcam.tsx` is a self-contained component implementing MULTI-04 (single-frame snap), MULTI-05 (continuous live mode + downsample + single-in-flight + session reuse), MULTI-08 (camera permission UX with error cards + chrome:// copy), and POLISH-02 (perf badge with `performance.now()` time-to-first-token) at the unit level.
- All 8 RESEARCH pitfalls are mitigated in code (verifiable via the grep gates in Task 2 acceptance criteria).
- Plan 11-02 can import `MultimodalWebcam` and `MultimodalWebcamProps` from `./MultimodalWebcam` without further refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/11-webcam-capture/11-01-SUMMARY.md` with:
- Task commits + line counts
- Confirmation of all 8 pitfall mitigations (greppable evidence)
- Typecheck/build status
- Confirmation that Plan 11-02's contract surface (the 5-prop MultimodalWebcamProps + the two named exports) is satisfied
- Phase 11-02 hand-off note: the precise import line `import { MultimodalWebcam } from './MultimodalWebcam';` and the precise mount JSX template (without parent state — Plan 11-02 fills those in)
</output>
