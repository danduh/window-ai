---
phase: 07-docs-seo-demo-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - chat/src/app/docs/Generative-UI-API.md
  - chat/src/app/AppRouter.tsx
  - chat/src/app/hooks/useSEOData.ts
  - chat/scripts/prerender-react.js
  - chat/src/app/components/GenerativeUIPage.tsx
autonomous: true
requirements: [GENUI-12, GENUI-13]
must_haves:
  truths:
    - "Navigating to /generative-ui/docs renders the Generative-UI-API.md markdown via DocsRenderer"
    - "A Tabs strip on /generative-ui toggles between Docs (first) and Workbench (second), and the URL stays in sync"
    - "Both /generative-ui and /generative-ui/docs set page-specific titles + meta descriptions through SEOProvider"
    - "seoConfigs.generativeUIDocs entry in useSEOData.ts is byte-identical to the entry in prerender-react.js"
    - "Generative-UI-API.md contains four code samples drawn from real GENUI_TOOLS + bridge sources"
  artifacts:
    - path: "chat/src/app/docs/Generative-UI-API.md"
      provides: "MCP Apps SEP-1865 markdown explainer (6 sections + 4 code samples)"
      contains: "MCP Apps SEP-1865"
    - path: "chat/src/app/AppRouter.tsx"
      provides: "/generative-ui/docs route mounting GenerativeUIPage"
      contains: "path=\"/generative-ui/docs\""
    - path: "chat/src/app/hooks/useSEOData.ts"
      provides: "seoConfigs.generativeUIDocs entry"
      contains: "generativeUIDocs"
    - path: "chat/scripts/prerender-react.js"
      provides: "Byte-identical /generative-ui/docs SEO entry + routes array"
      contains: "/generative-ui/docs"
    - path: "chat/src/app/components/GenerativeUIPage.tsx"
      provides: "Tabs (Docs first / Workbench second) with location-pathname switching"
      contains: "Tabs"
  key_links:
    - from: "chat/src/app/AppRouter.tsx"
      to: "chat/src/app/components/GenerativeUIPage.tsx"
      via: "Route element"
      pattern: "path=\"/generative-ui/docs\" element=\\{<GenerativeUIPage"
    - from: "chat/src/app/components/GenerativeUIPage.tsx"
      to: "chat/src/app/docs/Generative-UI-API.md"
      via: "DocsRenderer docFile prop"
      pattern: "DocsRenderer.*Generative-UI-API\\.md"
    - from: "chat/src/app/hooks/useSEOData.ts"
      to: "chat/scripts/prerender-react.js"
      via: "Byte-identical title + description + keywords for /generative-ui/docs"
      pattern: "Generative UI Docs"
---

<objective>
Author the `/generative-ui/docs` markdown explainer, wire the route + SEO mirror, and add a path-aware Tabs strip to GenerativeUIPage so users can toggle between the Workbench chat and the docs without leaving the page.

Purpose: Closes GENUI-12 (SEO byte-identical) and GENUI-13 (docs explainer with ≥2 typed code samples — we ship 4 per CONTEXT.md). Makes the v1.1 milestone educational, not just demo-able.
Output: 1 new markdown file, 4 modified files, all under `chat/src/app/...` and `chat/scripts/...`. No edits outside `chat/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-docs-seo-demo-polish/07-CONTEXT.md
@.planning/phases/04-v1-1-foundation-page-shell-store-seed/04-VERIFICATION.md
@chat/src/app/components/RecipeWorkbenchPage.tsx
@chat/src/app/components/Tabs.tsx
@chat/src/app/docs/WebMCP-API.md
@chat/src/app/tools/DocsRenderer.tsx
@chat/src/app/AppRouter.tsx
@chat/src/app/hooks/useSEOData.ts
@chat/scripts/prerender-react.js
@chat/src/app/components/GenerativeUIPage.tsx
@chat/src/app/services/genUITools.ts
@chat/src/app/components/GenerativeUI/iframe/bridge.ts
@chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts
@chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx

<interfaces>
<!-- Key contracts the executor needs (extracted from codebase). No exploration required. -->

From chat/src/app/components/Tabs.tsx:
  Default export: Tabs (named export does NOT exist — import as default).
  Props: { tabs: Tab[], defaultTab?: string, basePath?: string }
  Tab: { id: string; label: string; content: React.ReactNode; path?: string }
  URL sync: when basePath is set, Tabs reads location.pathname and picks the first tab where currentPath.includes(tab.path). Empty-string path matches everything — so the workbench tab (path: '') MUST come second in the array, after the docs tab (path: '/docs').

From chat/src/app/tools/DocsRenderer.tsx:
  Named export: DocsRenderer
  Props: { docFile: string; initOpen?: boolean }
  - docFile is the FILENAME within chat/src/app/docs/ (md-loader resolves it). Pass 'Generative-UI-API.md', NOT 'docs/Generative-UI-API.md'.
  - initOpen={true} renders the docs inline immediately (no Show/Hide toggle). Always pass initOpen={true} for the docs tab.

From chat/src/app/hooks/useSEOData.ts:
  export const useSEOData = (config: SEOConfig, path?: string) => void
  export const seoConfigs = { ..., generativeUI: {...}, webmcpDocs: {...} } as const
  - Pattern to follow: see existing webmcpDocs entry (lines 73-79) for the byte-identical-mirror comment + entry shape (title, description, keywords).
  - Path-aware SEO call: see RecipeWorkbenchPage.tsx:106-119 — branch on isDocs, pass the matching seoConfig and explicit path.

From chat/src/app/services/RecipePersistence.ts:
  export interface Recipe { id, title, servings, ingredients, steps, totalMinutes?, searchableIngredients? }

From chat/src/app/services/genUITools.ts (lines 86-139): searchRecipes tool descriptor — Sample 1 source.
From chat/src/app/services/genUITools.ts (lines 56-86): commitRecipeToPlan tool with annotations.visibility:['app'] — Sample 2 source for the bidirectional/hidden-helper pattern.
From chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts (lines 154-176): iframe-side delegated click → sendRequest('tools/call', { name: 'commitRecipeToPlan', arguments: { recipeId } }) — Sample 3 source.
From chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx (lines 323-365): _meta interceptor + ChatBox uiResourceUri branch + stripped session.prompt feedback — Sample 4 source.
</interfaces>

<seo_entries_to_mirror>
<!-- Locked title/description/keywords per CONTEXT.md D-08/D-12 lesson. Copy verbatim into BOTH useSEOData.ts AND prerender-react.js. -->

title:       Generative UI Docs — MCP Apps wire format + bidirectional pattern | Chrome AI APIs
description: How to register UI-returning tools and hidden helpers with navigator.modelContext, sandboxed iframes, and JSON-RPC postMessage bridge — SEP-1865 reference.
keywords:    MCP Apps documentation, SEP-1865, navigator.modelContext, registerTool, _meta.ui.resourceUri, sandboxed iframe, JSON-RPC postMessage, visibility annotation, hidden helpers, recipe carousel

structuredData (prerender-react.js only):
  '@context': 'https://schema.org', '@type': 'TechArticle',
  name: 'Generative UI API Documentation',
  description: 'Technical documentation for the MCP Apps Generative UI pattern on navigator.modelContext.'
</seo_entries_to_mirror>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Generative-UI-API.md (6 sections, 4 typed code samples)</name>
  <read_first>
    - chat/src/app/docs/WebMCP-API.md (closest analog — match prose density, header levels, code-fence style)
    - chat/src/app/services/genUITools.ts (lines 55-140) — Sample 1 + Sample 2 source
    - chat/src/app/components/GenerativeUI/iframe/iframeBridgeScript.ts (lines 78-188) — Sample 3 source (the inner-iframe Pick → tools/call flow)
    - chat/src/app/components/GenerativeUI/GenUIChatPanel.tsx (lines 323-365) — Sample 4 source (the _meta interceptor)
    - chat/src/app/components/GenerativeUI/bridge.ts (lines 165-437) — for the host-side bridge factory shape that Sample 3 calls into
  </read_first>
  <files>chat/src/app/docs/Generative-UI-API.md</files>
  <action>
    Create the markdown explainer at chat/src/app/docs/Generative-UI-API.md per the locked 6-section structure from 07-CONTEXT.md > Implementation Decisions > Docs structure + content.

    Section titles and intent:
    1. "What is MCP Apps (SEP-1865)?" — one paragraph; ≥1 link to spec (https://github.com/modelcontextprotocol/specification — SEP-1865 if the URL is in WebMCP-API.md references, otherwise link the W3C WebMCP draft already referenced in WebMCP-API.md). Explain the wire format in two sentences: tools return rich content via `_meta.ui.resourceUri`, the host renders that URI as a sandboxed iframe inside the chat bubble. Avoid marketing prose.
    2. "Tool result wire format" — TypeScript interface block for the result shape, plus a JSON literal example matching what searchRecipes returns at runtime. Use this exact TS interface (drawn verbatim from genUITools.ts:131-137):
       interface ToolResultWithUIResource {
         content: Array<{ type: 'text'; text: string }>;
         _meta?: { 'ui.resourceUri'?: string; [key: string]: unknown };
       }
       Then a sibling JSON block showing the real searchRecipes return shape (`content: [{ type:'text', text:'Found N recipes' }]`, `_meta: { 'ui.resourceUri': 'ui://gen-ui/carousel/<token>', 'genUI.recipeCount': N }`).
    3. "Sandbox model" — double-iframe (outer `sandbox="allow-scripts allow-same-origin"` on a disposable null origin, inner `sandbox="allow-scripts"` only) + CSP `default-src 'none'` baseline with `style-src 'unsafe-inline'; script-src 'unsafe-inline'; img-src data:`. Explain WHY (so a malicious tool result can't break out into the host). Reference the existing carouselTemplate.ts inline CSS + CSP meta tag as the in-repo proof.
    4. "postMessage protocol (JSON-RPC 2.0)" — 4-row table of methods used in this app:
       | Direction | Method | Payload (params) | Response |
       | iframe → host (request) | `ui/initialize` | `{ protocolVersion, clientInfo }` | `{ protocolVersion, hostInfo, hostContext }` |
       | iframe → host (notification) | `ui/notifications/initialized` | `{}` | — |
       | iframe → host (notification) | `ui/notifications/size-changed` | `{ height }` | — |
       | host → iframe (notification) | `ui/notifications/host-context-changed` | `{ theme, displayMode?, containerDimensions? }` | — |
       | iframe → host (request) | `tools/call` | `{ name, arguments }` | result of `tool.execute(arguments)` |
       Brief paragraph after the table explaining the pre-load outbound queue (host queues messages until iframe `load` fires) and the source-equality guard (`event.source === outerIframe.contentWindow`) — both are visible in bridge.ts.
    5. "Bidirectional pattern (UI-returning tools + hidden helpers)" — explain the two-tool design:
       - The page registers a UI-returning tool (`searchRecipes`) that returns `_meta.ui.resourceUri` → host renders carousel.
       - The page also registers a hidden helper (`commitRecipeToPlan`) with `annotations.visibility: ['app']` → invisible to the LLM but reachable from the iframe via `tools/call`.
       The pick button inside the iframe invokes the helper, not the LLM. This is what makes the carousel "interactive" without going back through the model.
    6. "Try it" — link to `/generative-ui` (relative path) with a one-line invitation. Reference `chat/src/app/components/RecipeWorkbench/MissingFlagBanner.tsx` for the Chrome 146 Canary flag requirement.

    Code samples (≥4, all drawn from real source — DO NOT fabricate signatures):

    Sample 1 (under section 2 or as its own subsection): `searchRecipes` tool registration shape — copy from genUITools.ts lines 87-139 as a TypeScript fenced block. Trim to the descriptor fields (name, description, inputSchema, execute body) so the snippet is self-contained reading. Keep the actual `_meta` return shape intact.

    Sample 2 (under section 5): `commitRecipeToPlan` with `annotations: { visibility: ['app'] }` — copy from genUITools.ts lines 56-86. Show how visibility:['app'] hides it from the LLM's tool catalog while still being reachable via the bridge.

    Sample 3 (under section 4): the iframe-side `tools/call` JSON-RPC payload — extract from iframeBridgeScript.ts lines 154-176, but show it as a TS-flavored sketch of the JSON-RPC payload + the inner-iframe Pick click handler. Important: include `window.parent.postMessage({ jsonrpc: '2.0', id, method: 'tools/call', params: { name: 'commitRecipeToPlan', arguments: { recipeId } } }, '*')` as the literal request shape so readers see the wire-level JSON.

    Sample 4 (under section 2 or 5): host-side `_meta` interceptor — extract from GenUIChatPanel.tsx lines 323-365. Show the runtime invariant: after `tool.execute()`, if `result._meta?.['ui.resourceUri']` is set, render an iframe bubble AND feed back ONLY `JSON.stringify({ content: result.content })` to `session.prompt()` so `ui://` never leaks into model context. Keep the `console.assert(!promptText.includes('ui://'), ...)` line — it makes the GENUI-10 invariant visible to readers.

    Prose density target: match `WebMCP-API.md` — terse, code-forward, no marketing. Use fenced TS/JSON blocks (` ```typescript`, ` ```json`) so DocsRenderer's SyntaxHighlighter renders them with the dracula theme.

    Do not add a "Limitations" section that contradicts the live demo (it works on Chrome 146 Canary). Do mention native-only / no polyfill / spec is draft, mirroring WebMCP-API.md's "Production readiness" + "Limitations" closing pattern.
  </action>
  <acceptance_criteria>
    - File exists at the exact path `chat/src/app/docs/Generative-UI-API.md`
    - Markdown has exactly 6 top-level (`##` or `#`) sections matching the locked structure above
    - At least 4 distinct fenced code blocks (count via `grep -c "^\`\`\`" chat/src/app/docs/Generative-UI-API.md` returns ≥ 8 (open + close per block))
    - The literal strings `searchRecipes`, `commitRecipeToPlan`, `visibility: ['app']`, `ui.resourceUri`, `tools/call`, `ui/initialize`, `ui/notifications/size-changed` each appear at least once
    - The literal string `_meta` appears at least 3 times (sample 1 result shape, section 2 interface, sample 4 interceptor)
    - No fabricated tool names, no invented method names — every code identifier matches one in genUITools.ts, bridge.ts, iframeBridgeScript.ts, or GenUIChatPanel.tsx
  </acceptance_criteria>
  <verify>
    <automated>test -f chat/src/app/docs/Generative-UI-API.md && grep -c '^```' chat/src/app/docs/Generative-UI-API.md | awk '{ if ($1 &gt;= 8) print "PASS code-fence count="$1; else { print "FAIL code-fence count="$1; exit 1 } }' &amp;&amp; for needle in 'searchRecipes' 'commitRecipeToPlan' "visibility: \['app'\]" 'ui.resourceUri' 'tools/call' 'ui/initialize' 'ui/notifications/size-changed'; do grep -qF "$needle" chat/src/app/docs/Generative-UI-API.md || { echo "MISSING: $needle"; exit 1; }; done; echo OK</automated>
  </verify>
  <done>Generative-UI-API.md exists with 6 sections, ≥4 typed code samples drawn from real source, and every required identifier present.</done>
</task>

<task type="auto">
  <name>Task 2: Wire /generative-ui/docs route, mirror SEO entry, and add path-aware Tabs to GenerativeUIPage</name>
  <read_first>
    - chat/src/app/AppRouter.tsx (lines 238-243 — existing /webmcp + /webmcp/docs registration block; existing /generative-ui registration at line 243)
    - chat/src/app/hooks/useSEOData.ts (lines 73-86 — existing webmcpDocs + generativeUI entries; copy the byte-identical-mirror comment style)
    - chat/scripts/prerender-react.js (lines 55-62 routes array; lines 378-399 seoConfigs map — existing /webmcp/docs and /generative-ui entries; mirror exactly)
    - chat/src/app/components/RecipeWorkbenchPage.tsx (lines 106-119 path-aware SEO call; lines 254-288 Tabs ordering with Docs FIRST + workbench path '')
    - chat/src/app/components/GenerativeUIPage.tsx (current shape — useSEOData(seoConfigs.generativeUI) is the only SEO call; wrap the existing two-column layout into the Workbench tab body)
    - chat/src/app/components/Tabs.tsx (whole file — confirms `Tab.path = ''` matches everywhere via `currentPath.includes('')`, hence the Docs-first ordering)
  </read_first>
  <files>chat/src/app/AppRouter.tsx, chat/src/app/hooks/useSEOData.ts, chat/scripts/prerender-react.js, chat/src/app/components/GenerativeUIPage.tsx</files>
  <action>
    Four sub-edits, all additive.

    (A) chat/src/app/AppRouter.tsx — Register the docs route.
    Find the existing `Generative UI routes` comment block at line 242 with `<Route path="/generative-ui" element={<GenerativeUIPage/>}/>`. Add a sibling line immediately after it:
      <Route path="/generative-ui/docs" element={<GenerativeUIPage/>}/>
    Mirror the v1.0 pattern at lines 239-240 verbatim (same `<RecipeWorkbenchPage>` element for both `/webmcp` and `/webmcp/docs`). Do NOT add a new top-nav `/generative-ui/docs` link — the in-page Tabs strip is the discovery surface per D-Tabs.

    (B) chat/src/app/hooks/useSEOData.ts — Add `seoConfigs.generativeUIDocs` entry.
    Append a new entry to the `seoConfigs` object after the existing `generativeUI` entry. Use the locked title/description/keywords from <seo_entries_to_mirror> above. Add a comment header above the entry matching the existing `webmcpDocs` comment style (lines 73-74):
      // Must match prerender-react.js seoConfigs['/generative-ui/docs'] verbatim.
      // See Phase 3 D-08 + D-12 — prerender drift caused a Phase 3 hotfix.
    Do NOT change the type — `seoConfigs` is `as const`; the entry must conform to the existing SEOConfig shape (`title`, `description`, `keywords`).

    (C) chat/scripts/prerender-react.js — Mirror the SEO entry BYTE-IDENTICALLY + add to routes array.
    Two sub-edits:
      (C1) Append `{ path: '/generative-ui/docs', filename: 'generative-ui-docs.html' },` to the `routes` array (after line 61, after the existing `/generative-ui` entry). Mirror the existing `/webmcp/docs` line 58 pattern.
      (C2) Append a new key `'/generative-ui/docs': { ... }` to the `seoConfigs` map (after the existing `/generative-ui` entry around line 389). Title, description, keywords MUST be byte-identical to (B). Add the structuredData block from <seo_entries_to_mirror>.
    Critical: the title/description/keywords STRINGS in this file must match the strings in useSEOData.ts character-for-character (the verification gate is `grep -F`).

    (D) chat/src/app/components/GenerativeUIPage.tsx — Add path-aware SEO + Tabs (Docs first, Workbench second).
    Refactor the existing component:
      1. Import `useLocation` from `react-router-dom`, `Tabs` (default export) from `./Tabs`, and `DocsRenderer` from `../tools/DocsRenderer`.
      2. Inside the component, call `const location = useLocation();` and compute `const isDocs = location.pathname.startsWith('/generative-ui/docs');` — use `startsWith`, not `includes`, per the v1.0 lesson (RecipeWorkbenchPage.tsx line 115 comment).
      3. Replace the existing `useSEOData(seoConfigs.generativeUI)` line with the branched call: `useSEOData(isDocs ? seoConfigs.generativeUIDocs : seoConfigs.generativeUI, isDocs ? '/generative-ui/docs' : '/generative-ui');`. Keep this as the first useSEOData call so Rules of Hooks order remains stable.
      4. Extract the existing two-column body (the `<div className="flex flex-col lg:flex-row gap-6">...</div>` block with GenUIChatPanel + MealPlanColumn) into a local const `workbenchContent` rendered as JSX.
      5. Build a `tabs` array with Docs FIRST and Workbench SECOND:
           [
             { id: 'docs', label: 'Docs', path: '/docs', content: <div className="max-w-none"><DocsRenderer docFile="Generative-UI-API.md" initOpen={true} /></div> },
             { id: 'workbench', label: 'Workbench', path: '', content: workbenchContent },
           ]
         Memoize with `useMemo` (deps: empty — these are stable). The Docs-first ordering is mandatory: with path '' the workbench would otherwise win `currentPath.includes('')` first.
      6. Render `<Tabs basePath="/generative-ui" defaultTab="workbench" tabs={tabs} />` below the `MissingFlagBanner` + `GenerativeUIHeader`. Keep both useEffect hooks (seedIfMissing + registerGenUITools) unchanged.
      7. KEEP the existing `<MissingFlagBanner />` and `<GenerativeUIHeader />` rendering outside the Tabs strip — they should appear on both tabs.

    Brownfield boundary: do not modify any file outside the four listed in <files> for this plan. Specifically, do NOT touch chat/src/app/components/GenerativeUI/* (deferred to plan 07-02), chat/src/app/components/RecipeWorkbench/*, chat/src/app/services/*, or anything in `/webmcp/`, `mcp/`, `mcp-client/`, `devops/awsweb/`.
  </action>
  <acceptance_criteria>
    - `grep -c 'path="/generative-ui/docs"' chat/src/app/AppRouter.tsx` returns 1
    - `grep -c 'generativeUIDocs:' chat/src/app/hooks/useSEOData.ts` returns 1
    - `grep -c "'/generative-ui/docs'" chat/scripts/prerender-react.js` returns 2 (routes array + seoConfigs key)
    - `grep -c "filename: 'generative-ui-docs.html'" chat/scripts/prerender-react.js` returns 1
    - The title string `Generative UI Docs — MCP Apps wire format + bidirectional pattern | Chrome AI APIs` appears in BOTH useSEOData.ts AND prerender-react.js (count must be exactly 2 across `chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js`)
    - The description string `How to register UI-returning tools and hidden helpers with navigator.modelContext, sandboxed iframes, and JSON-RPC postMessage bridge — SEP-1865 reference.` appears in both files (count = 2)
    - The keywords string `MCP Apps documentation, SEP-1865, navigator.modelContext, registerTool, _meta.ui.resourceUri, sandboxed iframe, JSON-RPC postMessage, visibility annotation, hidden helpers, recipe carousel` appears in both files (count = 2)
    - `grep -c 'import Tabs' chat/src/app/components/GenerativeUIPage.tsx` returns 1
    - `grep -c 'DocsRenderer' chat/src/app/components/GenerativeUIPage.tsx` returns ≥2 (import + usage)
    - `grep -c "docFile=\"Generative-UI-API.md\"" chat/src/app/components/GenerativeUIPage.tsx` returns 1
    - `grep -c "isDocs" chat/src/app/components/GenerativeUIPage.tsx` returns ≥2 (declaration + branched useSEOData)
    - In the tabs array within GenerativeUIPage.tsx, `id: 'docs'` appears at a LOWER line number than `id: 'workbench'` (Docs first)
    - No nav `<Link to="/generative-ui/docs">` is added to AppRouter.tsx (Tabs is the discovery surface)
    - npx nx typecheck chat passes; npx nx lint chat passes (warnings allowed, no errors)
  </acceptance_criteria>
  <verify>
    <automated>set -e; cd /Users/danielos/dev/window-ai; \
      test "$(grep -c 'path=\"/generative-ui/docs\"' chat/src/app/AppRouter.tsx)" = "1" || { echo FAIL route; exit 1; }; \
      test "$(grep -c 'generativeUIDocs:' chat/src/app/hooks/useSEOData.ts)" = "1" || { echo FAIL seo entry; exit 1; }; \
      test "$(grep -c \"'/generative-ui/docs'\" chat/scripts/prerender-react.js)" = "2" || { echo FAIL prerender count; exit 1; }; \
      test "$(grep -lF 'Generative UI Docs — MCP Apps wire format + bidirectional pattern | Chrome AI APIs' chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js | wc -l | tr -d ' ')" = "2" || { echo FAIL title drift; exit 1; }; \
      test "$(grep -lF 'How to register UI-returning tools and hidden helpers with navigator.modelContext, sandboxed iframes, and JSON-RPC postMessage bridge — SEP-1865 reference.' chat/src/app/hooks/useSEOData.ts chat/scripts/prerender-react.js | wc -l | tr -d ' ')" = "2" || { echo FAIL desc drift; exit 1; }; \
      grep -nF 'import Tabs' chat/src/app/components/GenerativeUIPage.tsx &gt; /dev/null || { echo FAIL Tabs import; exit 1; }; \
      grep -nF 'docFile="Generative-UI-API.md"' chat/src/app/components/GenerativeUIPage.tsx &gt; /dev/null || { echo FAIL docFile; exit 1; }; \
      DOCS_LINE=$(grep -n "id: 'docs'" chat/src/app/components/GenerativeUIPage.tsx | head -1 | cut -d: -f1); \
      WORK_LINE=$(grep -n "id: 'workbench'" chat/src/app/components/GenerativeUIPage.tsx | head -1 | cut -d: -f1); \
      test -n "$DOCS_LINE" -a -n "$WORK_LINE" -a "$DOCS_LINE" -lt "$WORK_LINE" || { echo "FAIL tab order docs=$DOCS_LINE work=$WORK_LINE"; exit 1; }; \
      npx nx typecheck chat 2&gt;&amp;1 | tail -10; \
      npx nx lint chat 2&gt;&amp;1 | tail -10; \
      echo OK</automated>
  </verify>
  <done>Route registered, SEO mirror byte-identical (verified by grep -F), Tabs (Docs first / Workbench second) on GenerativeUIPage switches via location.pathname, typecheck + lint clean.</done>
</task>

</tasks>

<verification>
- All acceptance criteria from both tasks pass via the embedded `<automated>` gates.
- `npx nx build chat` produces a clean production build (no missing-route warnings, no missing-md errors).
- Browser-side smoke check (manual, recorded in execute-phase SUMMARY):
  - Navigate to /generative-ui → Workbench tab active by default, GenUIChatPanel + MealPlanColumn render
  - Click "Docs" tab → URL becomes /generative-ui/docs, DocsRenderer renders Generative-UI-API.md inline
  - Hard-reload /generative-ui/docs → Docs tab is active on first paint (path-aware initial tab)
  - View source on /generative-ui/docs → title contains "Generative UI Docs — MCP Apps wire format"
  - /generative-ui still works regression-free (no v1.0 regression to /webmcp either)
</verification>

<success_criteria>
GENUI-12 closed: seoConfigs.generativeUIDocs is byte-identical between useSEOData.ts and prerender-react.js, verified by `grep -F` cross-check on title + description + keywords.
GENUI-13 closed: Generative-UI-API.md exists at chat/src/app/docs/, renders via DocsRenderer, covers the 6 locked sections, contains 4 typed code samples drawn from real GENUI_TOOLS + bridge sources.
Brownfield boundary respected: only the 5 files in `files_modified` (1 new + 4 modified) are touched; nothing under `/webmcp/`, `mcp/`, `mcp-client/`, `devops/awsweb/`, or `chat/src/app/components/RecipeWorkbench/*` changes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-docs-seo-demo-polish/07-01-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Include: SEO drift-check command output (the two `wc -l = 2` lines), the resolved file paths, and the byte-count of Generative-UI-API.md for the next planner.
</output>
